{-
   The Linux system-call layer, for x86-64.

   **`.x64.yana`, and that is the point of the name** - Analysis-Modules.md §2.5 and §4.2.1. The call
   numbers below are the x86-64 table and nothing else: on arm64 `read` is 63 rather than 0, and
   every other number here differs too. Until a file could say which machine it was for, this said
   it in a comment and was compiled for every one of them - including JS, where a syscall is not a
   thing that exists. An architecture selector excludes a JS build for the same reason it excludes
   arm64, so the one word replaces both facts.

   An arm64 port is `Linux.arm64.yana` beside this, and nothing else has to change to accept it.
-}

{-
   The amd64 Linux call numbers, as a type - and for the reason `Errno` below is one.

   The argument that section makes applies here unchanged and is not about brevity: a set of `let`s
   is a set of integers that happen to be spelled with names, and nothing relates them or says what
   a position holding one will accept. A type says both, and the compiler checks it.

   What it buys that `Errno` does not need is the *other* generated direction. `nameOf` is a literal
   for a known constructor, so a tracing build gets "openat" out of the same declaration that gets
   the kernel 257, with no table to write and none to keep in step - see `numberOf`, which is the one
   place a call's number is produced and therefore the one place such a mode would print from.

   The set is what this library calls and not the kernel's whole table, which is three hundred long
   and mostly about facilities nothing here has. A module that needs one more adds it here, and
   `SysFutex` is the example worth stating: it would sit perfectly comfortably beside the futex
   operations in `Thread/Linux/Futex.linux.yana`, and it does not belong there. A call number is not
   a fact about futexes - it is a fact about *this kernel on this architecture*, which is what this
   file is, and it varies in the two dimensions the file name states rather than in the one that
   module has. Everything else about a futex - which operation, which flags, what the answers mean -
   is genuinely that module's.
-}
pub data Syscall
  = @value(0)   SysRead
  | @value(1)   SysWrite
  | @value(2)   SysOpen
  | @value(3)   SysClose
  | @value(4)   SysStat
  | @value(5)   SysFileStat
  | @value(6)   SysLinkStat
  | @value(8)   SysSeek
  | @value(9)   SysMmap
  | @value(11)  SysMunmap
  | @value(17)  SysReadAt
  | @value(18)  SysWriteAt
  | @value(22)  SysPipe
  | @value(33)  SysDuplicate
  | @value(39)  SysProcessId
  | @value(57)  SysFork
  | @value(59)  SysExec
  | @value(60)  SysExit
  | @value(61)  SysWait
  | @value(62)  SysKill
  | @value(74)  SysSync
  | @value(77)  SysTruncate
  | @value(82)  SysRename
  | @value(83)  SysMakeDirectory
  | @value(84)  SysRemoveDirectory
  | @value(87)  SysUnlink
  | @value(202) SysFutex
  | @value(217) SysDirectoryEntries
  | @value(228) SysClockTime
  | @value(318) SysGetRandom

{-
   The number the machine is actually given, which is the constructor's own `@value`.

   Free at every call: `valueOf` of a known constructor folds to its literal, so `syscall1(numberOf(
   SysClose), handle)` reaches the backend as `syscall 3` exactly as the `let` did. What it is not is
   free to *get wrong* - a position taking a `Syscall` will not accept 3, and 3 was what the previous
   spelling accepted from anywhere.

   One function rather than a conversion at each site, because it is also the seam: a tracing mode
   has one body to add `nameOf(call)` to, and nothing else in the file has to know it exists.
-}
pub fn numberOf(call: Syscall) -> USize =
    truncate(valueOf(call)) :: USize

-- PROT_READ | PROT_WRITE, and MAP_PRIVATE | MAP_ANONYMOUS.
--
-- Left as constants, deliberately, where the call numbers became a type: these are *masks*, and the
-- values a position accepts are the sums of subsets rather than the named ones. An enumeration of
-- them would be an enumeration of the combinations somebody happened to write down.
let protReadWrite = 3 :: USize
let mapPrivateAnonymous = 34 :: USize

-- Maps `size` bytes of zeroed, readable and writable address space, or null if the kernel
-- refused. mmap reports failure as a small negative value rather than as an error flag, which is
-- why the result is checked as a number before it becomes a pointer.
pub fn mapMemory(size: Size) -> %U8:
    -- The descriptor is -1 as a *word* rather than as a number: every argument position is a word,
    -- and this is the one place the kernel's "no file" is a bit pattern instead of a count. Written
    -- out here because it cannot be a `let` - a global's initializer is a literal, and the all-ones
    -- word of a width no stage above `repr` has is not one.
    let anonymous = bitcast(0 - 1 :: Size) :: USize
    let result = syscall6(numberOf(SysMmap), 0, bitcast(size) :: USize, protReadWrite, mapPrivateAnonymous, anonymous, 0)
    if result < 0 then return null()

    return bitcast(result) :: %U8

pub fn unmapMemory(from: %U8, size: Size) -> Size =
    syscall2(numberOf(SysMunmap), bitcast(from) :: USize, bitcast(size) :: USize)

{-
   The file calls, and what they all have in common - Analysis-Library.md §2.9.

   **Every one of them answers the kernel's own number**, which is a count or a descriptor when it
   worked and the negated errno when it did not. Nothing here turns that into a `Result`, and that
   is the line this file has always drawn: it knows one kernel's call numbers and flag values and
   nothing about what a program wants them to mean. `File` is where a negative answer becomes a
   `FileError`, and it is the only reader of the `err*` numbers below.

   **A path is an address here rather than a `String`**, for the same reason. What the kernel takes
   is a NUL-terminated byte sequence, which is not what a `String` is - a run and a live prefix, with
   nothing after it - so somebody has to make the terminated copy, and that somebody has to be the
   layer that has a string type to copy *into*. See `terminated` in `File`.
-}
{-
   `open`'s flag bits, as a type rather than as nineteen bare integers - Analysis-Language.md §5.2.

   **This is deliberately not a record of `Bool` fields with pinned bit positions**, which is the
   feature the complaint about bare `I64`s reads as asking for and which could not say what `open`
   takes. `readOnly`/`writeOnly`/`readWrite` at 0, 1 and 2 are a two-bit *enumeration* in the low
   bits rather than three independent flags, and `openTemporary` is 4259840, which is
   `__O_TMPFILE | O_DIRECTORY` - a multi-bit combination no bit position names. A bit-position
   feature would have produced a type that quietly cannot hold what the syscall wants.

   A newtype over the integer says everything that was missing and nothing that is false: the
   constants are distinct from every other number in the program, `or` and `and` compose them
   through `Bitwise`, `if flags:` asks `Truth`, and Repr unwraps the whole thing so it costs nothing.

   `Bitwise` and not `Integral`, which is the distinction the two classes exist to draw: `Integral`'s
   superclass chain is `Num` and then `FromInt`, so joining it to get four bitwise names would also
   mean saying what `flags * flags` and the literal `3` are - and there is no answer, which is the
   whole reason this is a newtype rather than an `I64`.

   The two instances are *derived* - Analysis-Derive.md §3's `newtype` shape. What the clause expands
   to is four unwrap-apply-rewrap bodies and one truthiness test, and the reason it can be a clause
   rather than a compiler rule is that whether a wrapper
   is transparent to a class is the class's decision: `Bitwise` over a newtype is the content's
   `Bitwise` with the name put back on, and that sentence is true of `Bitwise` rather than of
   `OpenFlags`.

   `!flags` is deliberately not a thing this type has. `!` asks `Truth` and answers `Bool`, so it
   means "no bits set" wherever it appears; the complement is `not(flags)`, which is a different
   question with a different name. One operator for both is what makes `if empty` False while
   `if !empty` is True.
-}
pub alias qualified OpenFlags = USize deriving (Bitwise, Truth)

-- The low two bits are the access mode and are a small enumeration rather than a set - `readOnly`
-- is zero, so an open that asks for nothing asks for reading - and everything above them is a flag
-- that ors in. `File`'s `FileAccess` and `CreateMode` are what a program writes instead; these are
-- what those become.
pub let openReadOnly = OpenFlags(0)
pub let openWriteOnly = OpenFlags(1)
pub let openReadWrite = OpenFlags(2)
pub let openCreate = OpenFlags(64)
pub let openExclusive = OpenFlags(128)
pub let openTruncate = OpenFlags(512)
pub let openAppend = OpenFlags(1024)
pub let openSync = OpenFlags(4096)
pub let openDirect = OpenFlags(16384)
pub let openDirectory = OpenFlags(65536)
pub let openTemporary = OpenFlags(4259840)

-- The permission bits a created file gets, which the same call takes in its third position and
-- which mean nothing at all unless the open creates something. A second newtype rather than a
-- second use of the first: handing `open` its flags where it wanted its mode is exactly the mistake
-- two bare `I64`s in a row invite.
pub alias qualified FileMode = USize deriving (Bitwise, Truth)

pub fn fileMode(bits: USize) -> FileMode = FileMode(bits)
pub fn modeShifted(mode: FileMode, by: USize) -> FileMode = FileMode(mode.0 `shl` by)

pub fn openPath(path: %U8, flags: OpenFlags, mode: FileMode) -> Size =
    syscall3(numberOf(SysOpen), bitcast(path) :: USize, flags.0, mode.0)

pub fn closeFile(handle: Size) -> Size =
    syscall1(numberOf(SysClose), bitcast(handle) :: USize)

pub fn readFile(handle: Size, to: %U8, count: Size) -> Size =
    syscall3(numberOf(SysRead), bitcast(handle) :: USize, bitcast(to) :: USize, bitcast(count) :: USize)

pub fn writeFile(handle: Size, from: %U8, count: Size) -> Size =
    syscall3(numberOf(SysWrite), bitcast(handle) :: USize, bitcast(from) :: USize, bitcast(count) :: USize)

{-
   A 64-bit file offset, in a syscall argument register.

   `pread64`, `pwrite64`, `ftruncate` and `lseek` all take an `off_t`, which is sixty-four bits on
   thirty-two-bit Linux too - Analysis-Modules.md §4.2.1's file-offset row, which is the counterexample
   that makes the `Size` audit a per-parameter job rather than a rewrite. So the offset stays `I64` in
   every signature here and is narrowed where it becomes a machine word, written out at each call
   rather than behind a name: `truncate(at) :: USize` says what it does, and the `offsetWord` that
   used to wrap it said only that somebody had wrapped it.

   **The narrowing is the identity, because this file is x86-64.** A word is sixty-four bits on this
   architecture and an offset fits one; the compiler cannot know that, because `Size` is deliberately
   a width no stage above `repr` has - so what would otherwise be an unstated assumption is written
   out, once, with its reason. A thirty-two-bit port does not copy this line: that ABI splits an
   offset across *two* argument registers, which is a different call rather than a narrower one, and
   the file that makes it is `Linux.x86.yana` beside this one.
-}

-- The positional pair, which is `pread64`/`pwrite64` and not a seek and a read. The file position
-- is untouched by both, which is the whole of what makes them a second pair rather than a
-- convenience over the first.
pub fn readFileAt(handle: Size, to: %U8, count: Size, at: I64) -> Size =
    syscall4(numberOf(SysReadAt), bitcast(handle) :: USize, bitcast(to) :: USize, bitcast(count) :: USize, truncate(at) :: USize)

pub fn writeFileAt(handle: Size, from: %U8, count: Size, at: I64) -> Size =
    syscall4(numberOf(SysWriteAt), bitcast(handle) :: USize, bitcast(from) :: USize, bitcast(count) :: USize, truncate(at) :: USize)

pub fn syncFile(handle: Size) -> Size =
    syscall1(numberOf(SysSync), bitcast(handle) :: USize)

pub fn truncateFile(handle: Size, size: I64) -> Size =
    syscall2(numberOf(SysTruncate), bitcast(handle) :: USize, truncate(size) :: USize)

{-
   `struct stat`, as the record the kernel fills - Analysis-Language.md §5.2.

   What this replaces is six offsets and a `Run(U8)` read with `wordAt`: a C structure layout
   described as arithmetic, in the one place in the library where the layout is an ABI fact rather
   than a call number. `@layout(c)` is what makes the record *be* that layout - declaration order,
   one group, C's own padding - so the sizes and the offsets are the compiler's answer rather than
   six constants somebody has to keep in step with a kernel header.

   A hundred and forty-four bytes on amd64, which is exactly the `statBytes` the arithmetic used.
   Every field defaults to zero, so `FileStatus {}` is the buffer to hand the kernel; the three
   reserved words at the end are the kernel's own `__unused`, named rather than left as a hole
   because a field somebody can see is a field nobody re-derives.

   The three times are a pair of words each - seconds and nanoseconds - and the mode is a `U32`
   holding the type bits above the permission ones.
-}
@layout(c)
pub data FileStatus {
    device: U64 = 0,
    inode: U64 = 0,
    links: U64 = 0,
    mode: U32 = 0,
    userId: U32 = 0,
    groupId: U32 = 0,
    padding: U32 = 0,
    representedDevice: U64 = 0,
    size: I64 = 0,
    blockSize: I64 = 0,
    blocks: I64 = 0,
    accessedSeconds: I64 = 0,
    accessedNanos: I64 = 0,
    modifiedSeconds: I64 = 0,
    modifiedNanos: I64 = 0,
    changedSeconds: I64 = 0,
    changedNanos: I64 = 0,
    reserved0: I64 = 0,
    reserved1: I64 = 0,
    reserved2: I64 = 0
}

{-
   The three `stat` calls, into a record the caller supplies.

   Three and not one, because which of them a program wants is a question about symbolic links and
   the answer differs per call site: `statPath` follows them, `statLink` reports the link itself, and
   `statFile` asks about an open descriptor and so has no link to have an opinion about. Without
   `statLink` there is no way to answer `isLink` at all - see `linkInfo` in `File`.

   `&into` rather than an address, which is what moves the last piece of layout arithmetic out of
   `File`: the caller writes `let &status = FileStatus {}` and reads named fields off it afterwards,
   and the one place that still knows the buffer is bytes is the line below that takes its address.
-}
fn statusAddress(&into: FileStatus) -> USize = bitcast(addressOf(into)) :: USize

pub fn statPath(path: %U8, &into: FileStatus) -> Size =
    syscall2(numberOf(SysStat), bitcast(path) :: USize, statusAddress(into))

pub fn statLink(path: %U8, &into: FileStatus) -> Size =
    syscall2(numberOf(SysLinkStat), bitcast(path) :: USize, statusAddress(into))

pub fn statFile(handle: Size, &into: FileStatus) -> Size =
    syscall2(numberOf(SysFileStat), bitcast(handle) :: USize, statusAddress(into))

{-
   What the mode's type bits say a directory entry is - Analysis-Language.md §5.1.

   `S_IFMT` is the mask and these are the three kinds anything here asks about. A pinned value each,
   because these are the kernel's numbers and not a declaration order that happens to agree with
   them, and `fromValue` is how the masked mode becomes one - a mode naming a socket or a device is
   a kind this set does not have, which is what `Maybe` says.
-}
pub data FileKind = @value(16384) DirectoryKind | @value(32768) RegularKind | @value(40960) LinkKind

pub let modeTypeMask = FileMode(61440)

-- Straight into the word now that the word a mode is held in is unsigned, which is what these bits
-- always were. It used to go through `I32` first, because a `U32` does not widen into a *signed*
-- word - on a target whose word is thirty-two bits its top half would not fit - and that detour is
-- exactly the sort of thing the signed spelling was costing.
pub fn kindOf(mode: U32) -> Maybe(FileKind) =
    fromValue(bitcast((FileMode(widen(mode) :: USize) `and` modeTypeMask).0) :: Size)

pub fn removePath(path: %U8) -> Size =
    syscall1(numberOf(SysUnlink), bitcast(path) :: USize)

pub fn renamePath(from: %U8, to: %U8) -> Size =
    syscall2(numberOf(SysRename), bitcast(from) :: USize, bitcast(to) :: USize)

pub fn makeDirectory(path: %U8, mode: FileMode) -> Size =
    syscall2(numberOf(SysMakeDirectory), bitcast(path) :: USize, mode.0)

pub fn removeDirectoryPath(path: %U8) -> Size =
    syscall1(numberOf(SysRemoveDirectory), bitcast(path) :: USize)

-- `getdents64`, which is the only reading call here that answers a *block* of variable-length
-- records rather than bytes: zero means the directory ended, and a positive answer is how many
-- bytes of `into` the kernel filled. The record layout is `DirectoryRecord` below.
pub fn readDirectoryEntries(handle: Size, into: %U8, count: Size) -> Size =
    syscall3(numberOf(SysDirectoryEntries), bitcast(handle) :: USize, bitcast(into) :: USize, bitcast(count) :: USize)

{-
   `dirent64`'s fixed head, by the same rule `FileStatus` is a record: the inode number, the offset
   of the next record, this record's length, and one byte of type.

   The NUL-terminated name follows immediately and is *not* a field, because its length is not one:
   `length` covers the padding after the name, so the name is read by scanning from the end of this
   head. That one piece of arithmetic is what a variable-length record costs and it is all that is
   left of the six offsets this replaced.
-}
@layout(c)
pub data DirectoryRecord {inode: U64, nextOffset: I64, length: U16, kind: U8}

-- Where the name starts, in bytes from the start of the record. One past the head, which is what
-- `dirent64` says and what `@layout(c)` makes the compiler agree with.
pub let entryNameOffset = 19 :: Size

-- `DT_DIR` and the two other kinds `getdents64` reports that anything here asks about. `DT_UNKNOWN`
-- is 0 and is what a file system that does not fill the byte answers, which is why this is partial.
pub data EntryKind = @value(4) EntryDirectory | @value(8) EntryRegular | @value(10) EntryLink

{-
   `lseek`'s three bases, as the enumeration they are - Analysis-Language.md §5.1.

   `data Whence = FromStart | FromCurrent | FromEnd` lowers to 0, 1 and 2 in declaration order on
   its own; what it cannot do without `@value` is *say* so, and the cost of that shows at exactly
   this boundary. Turning one into the number the syscall takes is then two compares and two selects
   computing the identity function, because the mapping lives in a `match` nothing has reason to
   recognise. `@value` pins it, `valueOf` is a cast, and `seekFile` cannot be handed the wrong
   integer.
-}
pub data Whence = @value(0) FromStart | @value(1) FromCurrent | @value(2) FromEnd

pub fn seekFile(handle: Size, offset: I64, whence: Whence) -> I64 =
    syscall3(numberOf(SysSeek), bitcast(handle) :: USize, truncate(offset) :: USize, truncate(valueOf(whence)) :: USize) :: I64

{-
   The errno numbers `File` maps, as one type - Analysis-Language.md §5.1.

   They are one kernel's, exactly as the call numbers above are: `ENOENT` is 2 on Linux and 2 on
   every other Unix by accident of history rather than by agreement, and the numbers that matter
   most here - `EDQUOT` at 122, `ENAMETOOLONG` at 36 - differ between them. So the whole of what a
   second platform has to restate lives in this file, and `File` names these rather than integers.

   One type rather than nineteen `let`s, and what that buys is not brevity: mapping errno onto
   `File`'s own closed set from bare numbers takes nineteen `if code == err...` comparisons, because
   a bare name in a pattern *binds* and `match code: errNotFound -> ...` would match everything.
   With a type there is a `match` to write, and the compiler checks it covers what it says it does.

   The set is open in practice - the kernel has two hundred numbers and this names nineteen - which
   is what makes `fromValue` the reading direction and `Maybe` its answer.
-}
pub data Errno
  = @value(1)   NotPermitted
  | @value(2)   NoEntry
  | @value(9)   BadHandle
  | @value(12)  OutOfMemory
  | @value(13)  PermissionDenied
  | @value(14)  BadAddress
  | @value(16)  DeviceBusy
  | @value(17)  AlreadyExists
  | @value(20)  NotADirectory
  | @value(21)  IsADirectory
  | @value(22)  InvalidArgument
  | @value(23)  TableFull
  | @value(24)  TooManyOpen
  | @value(27)  FileTooLarge
  | @value(28)  NoSpace
  | @value(30)  ReadOnlyFileSystem
  | @value(36)  NameTooLong
  | @value(39)  NotEmpty
  | @value(122) QuotaExceeded

-- The kernel's own number as one of these, or nothing where it is a case this set does not name.
pub fn errnoOf(code: I64) -> Maybe(Errno) = fromValue(code)

{-
   ==========================================================================================
   The clocks - Analysis-Library.md §3.1.
   ==========================================================================================

   `struct timespec`, as the record the kernel fills, on `FileStatus`'s pattern above and for the
   same reason: two words at an ABI-fixed layout, so `@layout(c)` is what holds it rather than two
   offset constants somebody keeps in step with a kernel header. The three times in `FileStatus` are
   this pair inlined; this is the one the kernel writes on its own.

   Nanoseconds, which is the resolution `clock_gettime` reports at and *not* the one `Timestamp`
   carries - `Date` divides. Doing it here would throw away the only clock a program can measure a
   short operation with.
-}
@layout(c)
pub data ClockTime {seconds: I64 = 0, nanos: I64 = 0}

fn clockAddress(&into: ClockTime) -> USize = bitcast(addressOf(into)) :: USize

{-
   The two clocks a program has a reason to ask for, as the kernel's own numbers.

   `clockRealtime` is the wall clock: it is what "now" means to anything outside this process, and
   it steps when the machine's own idea of the time is corrected. `clockMonotonic` never steps
   backwards and has no defined base, which is what makes it the one to measure *with* and the one
   that cannot be shown to anybody.

   `CLOCK_MONOTONIC` rather than `CLOCK_MONOTONIC_RAW`, which is what the C++ this ports asked for.
   Raw is unslewed by NTP, and being unslewed is a *drift* against real seconds - about 50 parts per
   million on a machine whose clock is being corrected. The adjusted one is what "a second" means
   here, and both are equally free of steps. The raw one also had no vDSO implementation for years,
   so asking for it used to cost a system call where this does not.
-}
pub let clockRealtime = 0 :: USize
pub let clockMonotonic = 1 :: USize

-- The kernel's own number, which is 0 or the negated errno - the line every call in this file
-- draws. `Native.Clock` is what turns a failure into a fallback.
pub fn readClock(clock: USize, &into: ClockTime) -> Size =
    syscall2(numberOf(SysClockTime), clock, clockAddress(into))

{-
   ==========================================================================================
   The kernel's entropy pool - Analysis-Library.md §2.7.
   ==========================================================================================

   `getrandom`, which is the one way in this file's vocabulary to ask for bytes nobody can predict.
   Reading `/dev/urandom` is the older spelling and is three calls rather than one, each of which can
   fail differently - a chroot with no `/dev`, a process out of descriptors - and the descriptor is
   the whole of what the system call exists to remove.

   Call number 318, which is 2014's and is therefore the *newest* number this file names: a kernel
   older than 3.17 answers `ENOSYS` and `Native.Entropy` is what turns that into a "no".

   The flags are the kernel's own two bits and only one of them is written here. `GRND_NONBLOCK` is
   what stops a call at boot time, before the pool is initialized, from parking the process until it
   is; the answer then is `EAGAIN`, which is a failure a caller can do something about where an
   unbounded wait is not. `GRND_RANDOM` selects the blocking pool and is deliberately absent - the
   two pools have been the same generator since 5.6, and asking for it on an older kernel is the
   unbounded wait again.

   Answers the kernel's own number - the count it filled or the negated errno - which is the line
   every call in this file draws. **A short answer is not an error**: the call is documented to fill
   at most 256 bytes without being interruptible and to stop early for a signal past that, so a
   caller that wants a buffer full has to ask again for the rest. `Native.Entropy` is that loop.
-}
pub let randomNonBlocking = 1 :: USize

pub fn readRandom(to: %U8, count: Size, flags: USize) -> Size =
    syscall3(numberOf(SysGetRandom), bitcast(to) :: USize, bitcast(count) :: USize, flags)

-- This process's own identifier, which cannot fail and so is the one call here that answers no
-- error. What wants it is a program that has to name something no other process will name at the
-- same time - a lock file, a scratch directory - and `test/lib/File.yana` is the first.
pub fn processId() -> Size = syscall0(numberOf(SysProcessId))

pub fn exitProcess(status: Size) -> {}:
    syscall1(numberOf(SysExit), bitcast(status) :: USize)

{-
   ==========================================================================================
   Processes - Design-Test.md §11.2's F4.
   ==========================================================================================

   The five calls a harness is made of, and nothing above them: a fork, an exec, a wait, a pipe and
   a signal. What each one *means* - how an argument vector is built, which end of a pipe the child
   keeps, what a status word decodes to - is `Process.yana`'s, on the line this whole file draws:
   here is the kernel's number and the kernel's answer, and every interpretation is somewhere else.

   **The order matters and is the caller's to get right.** Everything a child needs has to exist
   before the fork, because between the fork and the exec a child may do only what is
   async-signal-safe: `duplicateHandle`, `closeFile`, `execProcess` and `exitProcess`, and nothing
   that allocates. `Process.yana` is where that sequence is written once.
-}

{-
   `fork` - 0 in the child, the child's identifier in the parent, and a negated errno on failure.

   `fork` rather than `clone` or `posix_spawn`, and the argument is what the two alternatives cost.
   `clone` is the same call with a flag word this has no use for, and `posix_spawn` is libc's, which
   is not something this library has. What fork gives up is a guarantee about a *threaded* parent,
   and the answer to that is the rule above: the child does nothing between the two calls.
-}
pub fn forkProcess() -> Size = syscall0(numberOf(SysFork))

{-
   `execve` - replaces this process, so it answers only when it fails.

   `arguments` and `environment` are NUL-terminated arrays of NUL-terminated byte strings, and the
   first argument is conventionally the program's own name. Nothing here builds one: an array of
   pointers into strings that have to outlive it is exactly the lifetime the caller is holding, and
   a helper here would take a `[String]` this file cannot name.
-}
pub fn execProcess(path: %U8, arguments: Ptr(Ptr(U8)), environment: Ptr(Ptr(U8))) -> Size =
    syscall3(numberOf(SysExec), bitcast(path) :: USize, bitcast(arguments) :: USize,
             bitcast(environment) :: USize)

{-
   `wait4` - the child's status word, written through `into`.

   The status is not the exit code: it is the packed word `waitpid` reports, and what it decodes to
   is `Process.yana`'s. Bounded by the identifier rather than by -1, so that waiting for one child
   does not reap another the caller has not asked about yet.
-}
pub fn waitProcess(process: Size, into: %Int, options: USize) -> Size =
    syscall4(numberOf(SysWait), bitcast(process) :: USize, bitcast(into) :: USize, options, 0)

pub let waitNonBlocking = 1 :: USize

{-
   `pipe` - two descriptors written into a two-element array, read end first.

   The raw form, taking the address of the pair rather than answering them: the kernel writes two
   `int`s and there is nothing here to build a record out of them with. `Process.yana` reads them
   back and gives each one a `File` that owns it.
-}
pub fn makePipe(into: %Int) -> Size = syscall1(numberOf(SysPipe), bitcast(into) :: USize)

-- `dup2` - what a child does to put a pipe end on descriptor 0, 1 or 2 before it execs. One of the
-- four calls that are safe between a fork and an exec.
pub fn duplicateHandle(from: Size, to: Size) -> Size =
    syscall2(numberOf(SysDuplicate), bitcast(from) :: USize, bitcast(to) :: USize)

-- `kill`. A timeout is what wants it, which is the harness's own reason for existing: a case that
-- hangs has to be stopped and attributed rather than taking the run down with it.
pub fn signalProcess(process: Size, signal: Int) -> Size =
    syscall2(numberOf(SysKill), bitcast(process) :: USize, bitcast(signal :: Size) :: USize)

{-
   ==========================================================================================
   The command line and the environment - the other half of F4.
   ==========================================================================================

   **The kernel hands these to the process on its stack and to nothing else**, so the only place
   they can be read is the entry stub - a hand-written sequence of bytes in the amd64 backend, and
   the LLVM backend's own `_start` in the other. Both write these three globals before the program's
   own entry runs, both read them off the same stack the kernel left, and both take the numbers they
   align and exit with from `compiler/codegen/entry.h`.

   That sidesteps rather than fixes "the entry point cannot take arguments yet", which is the honest
   description: `main` still takes none, and what a program reads instead is a global that was filled
   before it started. Making the entry take them is a change to what an entry *is* on both backends
   and is a larger thing than the harness needs.

   Zero and null in a build whose stub did not write them - which is every build where nothing reads
   them, since a global nothing reaches is not emitted and the stub has nothing to store into.
   `Environment.yana` reads that as "no arguments", which is what it is.

   **`@builtin(role)` is what links each of these to the compiler**, and the role is the compiler's
   name rather than this one: the three declarations below can be renamed, moved to another file or
   made private without the entry sequence noticing, and a role the compiler does not have is
   reported where it is written. The roles are listed in `compiler/compiler/builtin.h`, which is the
   only place they are written down.
-}
@builtin(commandLineCount)
pub let &commandLineCount = 0 :: Size

@builtin(commandLineValues)
pub let &commandLineValues = 0 :: Ptr(Ptr(U8))

@builtin(commandLineEnvironment)
pub let &commandLineEnvironment = 0 :: Ptr(Ptr(U8))

{-
   ==========================================================================================
   The auxiliary vector.
   ==========================================================================================

   **The third thing the kernel leaves on the stack**, after the arguments and the environment: a
   list of (type, value) word pairs terminated by a type of zero, saying what the machine and the
   process are. Nobody passes a pointer to it - it simply *follows the environment's null
   terminator*, which is why nothing in the compiler had to be taught about it. `commandLineEnvironment`
   is where the environment begins, the walk below finds where it ends, and the vector is what is
   after that.

   **That is why this is a library function and not a fourth `@builtin` role.** A role would have had
   the entry stub scan the environment to find the end of it - the same loop, written twice in
   assembly instead of once here, to save a walk of about fifty words that happens when something
   asks. What the compiler has to supply is what a program cannot work out; this is not that.

   Both backends reach it identically: the amd64 entry stub computes the environment pointer from the
   initial stack pointer, and the LLVM one is handed the same pointer as `main`'s third parameter.

   The types are `AT_*` from the kernel's `auxv.h`, and only the ones something here asks for are
   named - an unknown type is skipped by the walk rather than being a name this file has to have.
-}
pub let atNull = 0 :: USize
pub let atPageSize = 6 :: USize
pub let atClockTick = 17 :: USize
pub let atSecure = 23 :: USize
pub let atRandom = 25 :: USize
pub let atHwCap = 16 :: USize
pub let atHwCap2 = 26 :: USize

{-
   One entry of it, by type.

   `Nothing` where the kernel did not supply that type, where the environment pointer was never
   filled - a build in which nothing reads it, see above - and where the walk would run off a vector
   that is not there. A caller that has a sensible answer without the kernel's should use it: this is
   a question about the machine, and the machine is entitled not to say.

   The walk is linear over a vector of about twenty entries and is not cached. What reads this reads
   it once at startup, and a cache would be a global to initialize before the thing it exists for.
-}
pub fn auxiliaryValue(kind: USize) -> Maybe(USize):
    let &at = commandLineEnvironment
    if isNull(at) then return Nothing

    -- Past the environment: its own null terminator is what the vector begins after.
    while (bitcast(*at) :: USize) != 0:
        at = at + 1

    let &entry = bitcast(at + 1) :: Ptr(USize)

    while *entry != atNull:
        if *entry == kind then return Just(*(entry + 1))
        entry = entry + 2

    return Nothing

{-
   The page the kernel is actually using, as opposed to the one the compiler stated.

   `pageBytes` is a compile-time lower bound and is the right answer for rounding a mapping up or
   sizing a tail guard - see its declaration. This is the exact number, and it is what a caller that
   means to `mprotect` a page, or to report one, has to ask for. On this target the two are the same
   4096 and there is nothing to choose between them; on a kernel built for 16K or 64K pages they are
   not, and the difference is only visible from here.

   Falls back to `pageBytes` where the vector has no `AT_PAGESZ`, which is the honest answer rather
   than a zero: the bound is still true when the exact number cannot be had.
-}
pub fn systemPageBytes() -> Size:
    match auxiliaryValue(atPageSize):
        Just(page) -> return bitcast(page) :: Size
        Nothing -> return pageBytes
