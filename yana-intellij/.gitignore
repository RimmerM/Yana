build/
.gradle/
.idea/

# The IDE the plugin is built against, unpacked by the IntelliJ Platform Gradle plugin, and the
# Kotlin compiler's session directory. Both are caches keyed off gradle.properties.
.intellijPlatform/
.kotlin/
