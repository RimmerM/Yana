-- Multi-way if, with a wildcard fallback.
fn f(a: Int) = if:
  a > 0 -> 1
  a < 0 -> -1
  _ -> 0

-- Multi-way if, with an 'else' fallback.
fn g(a: Int) = if:
  a == 0 -> "zero"
  else -> "nonzero"

-- Multi-way if, single case plus fallback.
fn h(a: Bool) = if:
  a -> 1
  else -> 0
