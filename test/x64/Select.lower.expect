
merged<complex>(%v: Int, %a: Int, %b: Int): Int {
  b_0 {
    %cmp = cmp_eq %v, 1: Int
    %r = select %a, %b, %cmp: Int
    ret %r
  }
}

materialized<complex>(%v: Int, %a: Int, %b: Int): Int {
  b_0 {
    %cmp = cmp_ilt %v, 0: Int
    %x = add %a, 1: Int
    %r = select %x, %b, %cmp: Int
    ret %r
  }
}

crossBlock<complex>(%v: Int, %a: Int, %b: Int): Int {
  start {
    %cmp = cmp_ige %v, 10: Int
    jmp next
  }

  next {
    %r = select %a, %b, %cmp: Int
    ret %r
  }
}


--- codegen: merged ---
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  imm uses=[] creates=[-]  => 
  cmp_eq uses=[rdi, -] creates=[-]  => 83ff01
  select uses=[rsi, rdx, -] creates=[rsi]  => 0f45f2
  ret uses=[rax] creates=[] pre=[rsi->rax]  => 488bc6c3

--- codegen: materialized ---
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  imm uses=[] creates=[-]  => 
  cmp_ilt uses=[rdi, -] creates=[rdi]  => 85ff400f9cc7400fb6ff
  imm uses=[] creates=[-]  => 
  add uses=[rsi, -] creates=[rsi]  => ffc6
  select uses=[rsi, rdx, rdi] creates=[rsi]  => 85ff0f44f2
  ret uses=[rax] creates=[] pre=[rsi->rax]  => 488bc6c3

--- codegen: crossBlock ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  imm uses=[] creates=[-]  => 
  cmp_ige uses=[rdi, -] creates=[rdi]  => 83ff0a400f9dc7400fb6ff
  jmp uses=[] creates=[]  => 
block next:
  select uses=[rsi, rdx, rdi] creates=[rsi]  => 85ff0f44f2
  ret uses=[rax] creates=[] pre=[rsi->rax]  => 488bc6c3
