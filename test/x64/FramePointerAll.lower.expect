
leafNoFrame<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %s = add %a, %b: Int
    ret %s
  }
}

leafWithLocal<complex>(%v: Int): Int {
  b_0 {
    %a = alloca 8, 8: Ptr
    store %a, %v, 8
    %r = load %a, 8: Int
    ret %r
  }
}

callerNoFrame<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_complex leafNoFrame, %a, %b: Int
    %s = add %r, %b: Int
    ret %s
  }
}

callerWithLocal<complex>(%v: Int): Int {
  b_0 {
    %a = alloca 8, 8: Ptr
    store %a, %v, 8
    %r = call_complex leafNoFrame, %v, %v: Int
    %s = load %a, 8: Int
    %t = add %r, %s: Int
    ret %t
  }
}


--- codegen: leafNoFrame ---
  prologue  => 55488bec
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  add uses=[rax, rsi] creates=[rax] pre=[rdi->rax]  => 8d0437
  ret uses=[rax] creates=[]  => c9c3

--- codegen: leafWithLocal ---
  prologue  => 55488bec4883ec08
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  imm uses=[] creates=[-]  => 
  alloca uses=[-] creates=[rax]  => 488d45f8
  store uses=[rax, rdi] creates=[]  => 488938
  load uses=[rax] creates=[rax]  => 488b00
  ret uses=[rax] creates=[]  => c9c3

--- codegen: callerNoFrame ---
  prologue  => 55488bec53
block #0:
  jmp uses=[] creates=[] pre=[rsi->rbx]  => 8bde
block #1:
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi, rsi] creates=[rax] pre=[rbx->rsi]  => 8bf3e8c2ffffff
  add uses=[rax, rbx] creates=[rax]  => 01d8
  ret uses=[rax] creates=[]  => 488d65f85b5dc3

--- codegen: callerWithLocal ---
  prologue  => 55488bec534883ec08
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  imm uses=[] creates=[-]  => 
  alloca uses=[-] creates=[rbx]  => 488d5df0
  store uses=[rbx, rdi] creates=[]  => 48893b
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi, rsi] creates=[rax] pre=[rdi->rsi]  => 8bf7e899ffffff
  load uses=[rbx] creates=[rbx]  => 488b1b
  add uses=[rax, rbx] creates=[rax]  => 01d8
  ret uses=[rax] creates=[]  => 488d65f85b5dc3
