
win64Callee<win64>(%a: Int, %b: Int, %c: Int, %d: Int, %e: Int): Int {
  b_0 {
    %s = add %a, %e: Int
    ret %s
  }
}

win64Caller<win64>(%x: Int): Int {
  b_0 {
    %r = call_win64 win64Callee, %x, %x, %x, %x, %x: Int
    ret %r
  }
}

simpleCallee<simple>(%a: Int, %b: Int): Int {
  b_0 {
    %s = add %a, %b: Int
    ret %s
  }
}

simpleCaller<simple>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_simple simpleCallee, %a, %b: Int
    %s = add %r, %b: Int
    ret %s
  }
}

clobberCallee<clobber>(%a: Int): Int {
  b_0 {
    ret %a
  }
}

clobberCaller<clobber>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_clobber clobberCallee, %a: Int
    %s = add %r, %b: Int
    ret %s
  }
}

complexCaller<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_complex clobberCallee, %a: Int
    %s = add %r, %b: Int
    ret %s
  }
}


--- codegen: win64Callee ---
block #0:
  jmp uses=[] creates=[] pre=[stack:0->rax]  => 488b442428
block #1:
  add uses=[rcx, rax] creates=[rcx]  => 01c1
  ret uses=[rax] creates=[] pre=[rcx->rax]  => 488bc1c3

--- codegen: win64Caller ---
  prologue  => 4883ec38
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  x86_pusharg uses=[rcx] creates=[-]  => 48894c2420
  fun uses=[] creates=[-]  => 
  call_win64 uses=[-, rcx, rdx, r8, r9, -] creates=[rax] pre=[rcx->rdx, rcx->r8, rcx->r9]  => 488bd14c8bc14c8bc9e8deffffff
  ret uses=[rax] creates=[]  => 4883c438c3

--- codegen: simpleCallee ---
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  add uses=[rdi, rsi] creates=[rdi]  => 01f7
  ret uses=[rax] creates=[] pre=[rdi->rax]  => 488bc7c3

--- codegen: simpleCaller ---
  prologue  => 51
block #0:
  jmp uses=[] creates=[] pre=[rsi->rcx]  => 488bce
block #1:
  fun uses=[] creates=[-]  => 
  call_simple uses=[-, rdi, rsi] creates=[rax] pre=[rcx->rsi]  => 488bf1e8eeffffff
  add uses=[rax, rcx] creates=[rax]  => 01c8
  ret uses=[rax] creates=[]  => 59c3

--- codegen: clobberCallee ---
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  ret uses=[rax] creates=[] pre=[rdi->rax]  => 488bc7c3

--- codegen: clobberCaller ---
  prologue  => 55
block #0:
  jmp uses=[] creates=[] pre=[rsi->rbp]  => 488bee
block #1:
  fun uses=[] creates=[-]  => 
  call_clobber uses=[-, rdi] creates=[rax]  => e8f3ffffff
  add uses=[rax, rbp] creates=[rax]  => 01e8
  ret uses=[rax] creates=[]  => 5dc3

--- codegen: complexCaller ---
  prologue  => 53
block #0:
  jmp uses=[] creates=[] pre=[rsi->rbx]  => 488bde
block #1:
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi] creates=[rax]  => e8e6ffffff
  add uses=[rax, rbx] creates=[rax]  => 01d8
  ret uses=[rax] creates=[]  => 5bc3
