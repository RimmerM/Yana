
loop<complex>(%count: I32, %add: I32): I32 {
  start {
    %v = cast 0: I32
    jmp head
  }

  head {
    %v1 = phi [start, %v], [loop, %v2]: I32
    %count1 = phi [start, %count], [loop, %count2]: I32
    %v_0 = cmp_le %count1, 0: I32
    je %v_0, loop, end
  }

  loop {
    %count2 = sub %count1, 1: I32
    %v2 = add %v1, %add: I32
    jmp head
  }

  end {
    ret %v1
  }
}


--- codegen: loop ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  imm uses=[] creates=[-]  => 
  cast uses=[-] creates=[rax]  => 31c0
  imm uses=[] creates=[-]  => 
  cmp_le uses=[rdi, -] creates=[-]  => 85ff
  je uses=[-] creates=[]  => 7712
  align  => 66660f1f840000000000
block loop:
  imm uses=[] creates=[-]  => 
  sub uses=[rdi, -] creates=[rdi]  => ffcf
  add uses=[rax, rsi] creates=[rax]  => 01f0
  imm uses=[] creates=[-]  => 
  cmp_le uses=[rdi, -] creates=[-]  => 85ff
  je uses=[-] creates=[]  => 76f8
block end:
  ret uses=[rax] creates=[]  => c3
