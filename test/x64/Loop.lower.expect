
loop<complex>(%count: Int, %add: Int): Int {
  start {
    %v = cast 0: Int
    jmp head
  }

  head {
    %v1 = phi [start, %v], [loop, %v2]: Int
    %count1 = phi [start, %count], [loop, %count2]: Int
    %v_0 = cmp_le %count1, 0: Int
    je %v_0, loop, end
  }

  loop {
    %count2 = sub %count1, 1: Int
    %v2 = add %v1, %add: Int
    jmp head
  }

  end {
    ret %v1
  }
}


--- codegen: loop ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  imm uses=[] creates=[-]  => 
  cast uses=[-] creates=[rax]  => 31c0
  imm uses=[] creates=[-]  => 
  cmp_le uses=[rdi, -] creates=[-]  => 85ff
  je uses=[-] creates=[]  => 7708
block loop:
  imm uses=[] creates=[-]  => 
  sub uses=[rdi, -] creates=[rdi]  => ffcf
  add uses=[rax, rsi] creates=[rax]  => 01f0
  imm uses=[] creates=[-]  => 
  cmp_le uses=[rdi, -] creates=[-]  => 85ff
  je uses=[-] creates=[]  => 76f8
block end:
  ret uses=[rax] creates=[]  => c3
