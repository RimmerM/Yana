
callee<complex>(%x: Int, %y: Int): Int {
  b_0 {
    ret %x
  }
}

many<complex>(%a0: Int, %a1: Int, %a2: Int, %a3: Int, %a4: Int, %a5: Int, %a6: Int, %a7: Int, %a8: Int): Int {
  b_0 {
    ret %a0
  }
}

simpleCall<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_complex callee, %a, %b: Int
    ret %r
  }
}

liveAcrossCall<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_complex callee, %a, %b: Int
    %s = add %r, %a: Int
    ret %s
  }
}

pushCall<complex>(%a: Int, %b: Int): Int {
  b_0 {
    %r = call_complex many, %a, %a, %a, %a, %a, %a, %a, %a, %b: Int
    ret %r
  }
}

sysCall<complex>(%n: Int, %fd: Int, %buf: Int, %len: Int): Int {
  b_0 {
    %r = syscall %n, %fd, %buf, %len: Int
    ret %r
  }
}


--- codegen: callee ---
block #0:
  jmp uses=[] creates=[] pre=[rdi->rax]  => 8bc7
block #1:
  ret uses=[rax] creates=[]  => c3

--- codegen: many ---
block #0:
  jmp uses=[] creates=[] pre=[rdi->rax]  => 8bc7
block #1:
  ret uses=[rax] creates=[]  => c3

--- codegen: simpleCall ---
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi, rsi] creates=[rax]  => e8dbffffff
  ret uses=[rax] creates=[]  => c3

--- codegen: liveAcrossCall ---
  prologue  => 53
block #0:
  jmp uses=[] creates=[] pre=[rdi->rbx]  => 8bdf
block #1:
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi, rsi] creates=[rax] pre=[rbx->rdi]  => 8bfbe8c6ffffff
  add uses=[rax, rbx] creates=[rax]  => 01d8
  ret uses=[rax] creates=[]  => 5bc3

--- codegen: pushCall ---
  prologue  => 4883ec08
block #0:
  jmp uses=[] creates=[]  => 
block #1:
  x86_pusharg uses=[rsi] creates=[-]  => 48893424
  fun uses=[] creates=[-]  => 
  call_complex uses=[-, rdi, rsi, rdx, rcx, r8, r9, r10, r11, -] creates=[rax] pre=[rdi->rsi, rdi->rdx, rdi->rcx, rdi->r8, rdi->r9, rdi->r10, rdi->r11]  => 8bf78bd78bcf448bc7448bcf448bd7448bdfe8b1ffffff
  ret uses=[rax] creates=[]  => 4883c408c3

--- codegen: sysCall ---
block #0:
  jmp uses=[] creates=[] pre=[rdi->rax, rsi->rdi, rdx->rsi, rcx->rdx]  => 8bc78bfe8bf28bd1
block #1:
  syscall uses=[rax, rdi, rsi, rdx] creates=[rax]  => 0f05
  ret uses=[rax] creates=[]  => c3
