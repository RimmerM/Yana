
branch<complex>(%v: Int, %out: Int): Int, Int {
  start {
    %v_0 = cmp_le %v, 0: Int
    je %v_0, a, b
  }

  a {
    %v1 = add %v, 1: Int
    jmp end
  }

  b {
    %v2 = mul %v, 2: Int
    jmp end
  }

  end {
    %v_1 = phi [a, %v1], [b, %v2]: Int
    ret %v_1, %out
  }
}

flagWindow<complex>(%v: Int, %w: Int): Int {
  start {
    %cmp = cmp_ilt %v, %w: Int
    %x = add %v, 1: Int
    %y = mul %w, 3: Int
    je %cmp, a, b
  }

  a {
    ret %x
  }

  b {
    ret %y
  }
}

flagWindowLoad<complex>(%v: Int, %w: Int, %p: Ptr): Int {
  start {
    %cmp = cmp_ilt %v, %w: Int
    %l = load %p, 4: Int
    %x = add %l, 1: Int
    je %cmp, a, b
  }

  a {
    ret %x
  }

  b {
    ret %w
  }
}

wideBranch<complex>(%v: Long, %w: Long): Long {
  start {
    %v_0 = cmp_ilt %v, %w: Int
    je %v_0, a, b
  }

  a {
    %s = add %v, %w: Long
    ret %s
  }

  b {
    %v_1 = cmp_ilt %w, 0: Int
    je %v_1, c, d
  }

  c {
    ret %w
  }

  d {
    %e = sub %v, 1: Long
    ret %e
  }
}


--- codegen: branch ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  imm uses=[] creates=[-]  => 
  cmp_le uses=[rdi, -] creates=[-]  => 85ff
  je uses=[-] creates=[]  => 0f860f000000
block b:
  imm uses=[] creates=[rcx]  => b902000000
  mul uses=[rax, rcx] creates=[rax] pre=[rdi->rax]  => 488bc7f7e1
  jmp uses=[] creates=[]  => e905000000
block a:
  imm uses=[] creates=[-]  => 
  add uses=[rax, -] creates=[rax] pre=[rdi->rax]  => 488bc7ffc0
  jmp uses=[] creates=[]  => 
block end:
  ret uses=[rax, rdx] creates=[] pre=[rsi->rdx]  => 488bd6c3

--- codegen: flagWindow ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  imm uses=[] creates=[-]  => 
  add uses=[rcx, -] creates=[rcx] pre=[rdi->rcx]  => 488bcfffc1
  imm uses=[] creates=[r8]  => 41b803000000
  mul uses=[rax, r8] creates=[rax] pre=[rsi->rax]  => 488bc641f7e0
  cmp_ilt uses=[rdi, rsi] creates=[-]  => 39f7
  je uses=[-] creates=[]  => 0f8c01000000
block b:
  ret uses=[rax] creates=[]  => c3
block a:
  ret uses=[rax] creates=[] pre=[rcx->rax]  => 488bc1c3

--- codegen: flagWindowLoad ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  cmp_ilt uses=[rdi, rsi] creates=[rdi]  => 39f7400f9cc7400fb6ff
  load uses=[rdx] creates=[rdx]  => 8b12
  imm uses=[] creates=[-]  => 
  add uses=[rdx, -] creates=[rdx]  => ffc2
  je uses=[rdi] creates=[]  => 85ff0f8504000000
block b:
  ret uses=[rax] creates=[] pre=[rsi->rax]  => 488bc6c3
block a:
  ret uses=[rax] creates=[] pre=[rdx->rax]  => 488bc2c3

--- codegen: wideBranch ---
block #0:
  jmp uses=[] creates=[]  => 
block start:
  cmp_ilt uses=[rdi, rsi] creates=[-]  => 4839f7
  je uses=[-] creates=[]  => 0f8c14000000
block b:
  imm uses=[] creates=[-]  => 
  cmp_ilt uses=[rsi, -] creates=[-]  => 4885f6
  je uses=[-] creates=[]  => 0f8c07000000
block d:
  imm uses=[] creates=[-]  => 
  sub uses=[rax, -] creates=[rax] pre=[rdi->rax]  => 488bc748ffc8
  ret uses=[rax] creates=[]  => c3
block c:
  ret uses=[rax] creates=[] pre=[rsi->rax]  => 488bc6c3
block a:
  add uses=[rdi, rsi] creates=[rdi]  => 4801f7
  ret uses=[rax] creates=[] pre=[rdi->rax]  => 488bc7c3
