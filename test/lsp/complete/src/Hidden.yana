-- Names reached under an import's own rules rather than plainly. `hiddenTwin` is excluded by
-- Main.yana's import list and must never be offered; `hiddenPair` is included and must be.

fn hiddenPair() -> Int = 2

fn hiddenTwin() -> Int = 3
