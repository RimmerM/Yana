{-
   The ordinary half of a module whose test file is the point of it - see `GlobalInit.test.yana`,
   and `TestFileGlobals.yana`, which is the fixture that imports this.
-}
pub fn doubled(x: Int) -> Int = x * 2
