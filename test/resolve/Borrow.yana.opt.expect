let image$base: () = <none>

fn +=(Int)(%target: Int, %amount: Int) -> () {
  b0:
    %v2 = load %target : Int
    %v3 = add %v2, %amount : Int
    assign %target, %v3
    ret
}

fn bump(%counter: Int, %by: Int) -> () {
  b0:
    %v2 = load %counter : Int
    %v3 = add %v2, %by : Int
    assign %counter, %v3
    ret
}

fn swap(%a: Int, %b: Int) -> () {
  b0:
    %old = load %a : Int
    %v3 = load %b : Int
    assign %a, %v3
    assign %b, %old
    ret
}

fn moveBy(%p: Point, %dx: Int, %dy: Int) -> () {
  b0:
    %v5 = borrow_mut %p@Point.x : &mut Int
    call +=(Int), %v5, %dx
    %v9 = borrow_mut %p@Point.y : &mut Int
    call +=(Int), %v9, %dy
    ret
}

fn copyThroughBorrow(%target: Int, %amount: Int) -> () {
  b0:
    %v2 = load %target : Int
    %v3 = add %v2, %amount : Int
    assign %target, %v3
    ret
}

fn main() -> Int {
  b0:
    %v28 = alloc : Point
    aggregate %here@Point.x = 10, %here@Point.y = 20
    %v66 = borrow_mut %here@Point.x : &mut Int
    call +=(Int), %v66, 1
    %v68 = borrow_mut %here@Point.y : &mut Int
    call +=(Int), %v68, 2
    %v47 = load %here@Point.x : Int
    %v48 = add %v47, 33 : Int
    %v50 = load %here@Point.y : Int
    %v51 = add %v48, %v50 : Int
    ret %v51
}
