"use strict";
// ResolveTest
function read(it$x, it$y) {
  return it$x + it$y | 0;
}

function bump(it) {
  it.x = it.x + 1 | 0;
  return;
}

function onlyRead() {
  return read(1, 2) + read(1, 2) | 0;
}

function mutableOnOneArm(flag) {
  var v38;
  if (flag) {
    v38 = 2;
  } else {
    v38 = 1;
  }
  return read(v38, 2);
}

function copyIsIndependent() {
  return read(1, 2) + read(2, 2) | 0;
}

function addressed() {
  var at = { x: 1, y: 2 };
  return at.x + at.y | 0;
}

function main() {
  return (((read(1, 2) + read(1, 2) | 0) + mutableOnOneArm(1) | 0) + copyIsIndependent() | 0) + addressed() | 0;
}
