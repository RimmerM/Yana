"use strict";
// Collections
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

// ResolveTest
function total(xs$items, xs$length, xs$offset) {
  var sum = 0;
  var i = 0;
  var v19 = xs$length >>> 0;
  L0: for (;;) {
    if (i < xs$length) {
      var v16 = xs$offset + i | 0;
      checkCondition(i >>> 0 >= v19);
      sum = sum + xs$items[v16] | 0;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  return sum;
}

function middle(xs$items, xs$length, xs$offset) {
  var start = 1;
  if (1 > xs$length) {
    start = xs$length;
  }
  var end = 3;
  if (3 < start) {
    end = start;
  }
  if (end > xs$length) {
    end = xs$length;
  }
  return total(xs$items, end - start | 0, xs$offset + start | 0);
}

function padRead() {
  var v12 = [{ wide: 10n, narrow: 1 }, { wide: 20n, narrow: 2 }, { wide: 30n, narrow: 3 }];
  return BigInt.asIntN(64, BigInt.asIntN(64, v12[0].wide + v12[1].wide) + v12[2].wide);
}

function main() {
  var v24 = [1, 2, 3, 4];
  if (total(v24, 4, 0) !== 10) {
    return -3;
  } else {
    if (middle(v24, 4, 0) !== 5) {
      return -4;
    } else {
      var i = 0;
      L0: for (;;) {
        if (i < 4) {
          checkCondition(i >>> 0 >= 4);
          var value = v24[i];
          v24[i] = value + value | 0;
          i = i + 1 | 0;
          continue L0;
        }
        break L0;
      }
      if (total(v24, 4, 0) !== 20) {
        return -5;
      } else {
        if (total([7, 8], 2, 0) !== 15) {
          return -6;
        } else {
          if (padRead() !== 60n) {
            return -8;
          } else {
            return total(v24, 4, 0);
          }
        }
      }
    }
  }
}
main();
