"use strict";
var sinkCalls = 0n;
const closure$ResolveTest_lambda$0 = [teardown$none, null];

// Core
function teardown$none(value) {
  return;
}

function add_eq_I64(target, amount) {
  target.$v = BigInt.asIntN(64, target.$v + amount);
  return;
}

// ResolveTest
function direct() {
  var h = { value: 10n };
  var g = { value: 0n };
  Sink_Handle_sink(g, h);
  return g.value;
}

function derived() {
  var p = { left: { value: 20n }, right: 3n };
  var q = { left: { value: 0n }, right: 0n };
  moveInit$Pair(q, p);
  return BigInt.asIntN(64, q.left.value + q.right);
}

function nested() {
  var n = { inner: { left: { value: 40n }, right: 1n }, tag: 4n };
  var m = { inner: { left: { value: 0n }, right: 0n }, tag: 0n };
  moveInit$Nest(m, n);
  return BigInt.asIntN(64, BigInt.asIntN(64, m.inner.left.value + m.inner.right) + m.tag);
}

function unwrap(e) {
  var v12;
  if (e.$tag === 0) {
    v12 = e.n;
  } else {
    v12 = e.h.value;
  }
  return v12;
}

function tagged(flag) {
  var e;
  var v22;
  if (flag) {
    e = { $tag: 1, n: 0n, h: { value: 7n } };
  } else {
    e = { $tag: 0, n: 100n, h: { value: 0n } };
  }
  var f = { $tag: 0, n: 0n, h: { value: 0n } };
  moveInit$Either(f, e);
  if (f.$tag === 0) {
    v22 = f.n;
  } else {
    v22 = f.h.value;
  }
  return v22;
}

function consume(it) {
  return it.value;
}

function captured(h) {
  var v1 = { h: { value: 0n } };
  Sink_Handle_sink(v1.h, h);
  return ResolveTest_lambda$0$make(v1);
}

function retag(p) {
  var v1 = { left: { value: 0n }, right: 0n };
  var v5 = BigInt.asIntN(64, p.right + 1n);
  moveInit$Pair(v1, p);
  v1.right = v5;
  return v1;
}

function main() {
  var result = direct();
  if (sinkCalls !== 1n) {
    return -1n;
  } else {
    result = BigInt.asIntN(64, result + derived());
    if (sinkCalls !== 2n) {
      return -2n;
    } else {
      result = BigInt.asIntN(64, result + nested());
      if (sinkCalls !== 3n) {
        return -3n;
      } else {
        result = BigInt.asIntN(64, result + tagged(1));
        if (sinkCalls !== 4n) {
          return -4n;
        } else {
          result = BigInt.asIntN(64, result + tagged(0));
          if (sinkCalls !== 4n) {
            return -5n;
          } else {
            result = BigInt.asIntN(64, result + consume({ value: 5n }));
            if (sinkCalls !== 4n) {
              return -6n;
            } else {
              var closed = captured({ value: 30n });
              if (sinkCalls !== 5n) {
                drop$_sub_gt_I64({ $v: closed });
                return -7n;
              } else {
                var v109 = closed();
                drop$_sub_gt_I64({ $v: closed });
                result = BigInt.asIntN(64, result + v109);
                var retagged = retag({ left: { value: 60n }, right: 6n });
                if (sinkCalls !== 6n) {
                  return -8n;
                } else {
                  result = BigInt.asIntN(64, result + BigInt.asIntN(64, retagged.left.value + retagged.right));
                  var relocated = relocate_Handle({ value: 70n });
                  if (sinkCalls !== 7n) {
                    return -9n;
                  } else {
                    result = BigInt.asIntN(64, result + relocated.value);
                    var relocatedPair = relocate_Pair({ left: { value: 80n }, right: 8n });
                    if (sinkCalls !== 8n) {
                      return -10n;
                    } else {
                      var v218 = BigInt.asIntN(64, result + BigInt.asIntN(64, relocatedPair.left.value + relocatedPair.right));
                      result = v218;
                      return BigInt.asIntN(64, v218 + sinkCalls);
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}

function Sink_Handle_sink(to, from) {
  var v5 = { $v: sinkCalls };
  add_eq_I64(v5, 1n);
  sinkCalls = v5.$v;
  to.value = from.value;
  return;
}

function moveInit$Pair(to, from) {
  to.left = { value: from.left.value };
  to.right = from.right;
  Sink_Handle_sink(to.left, from.left);
  return;
}

function moveInit$Nest(to, from) {
  to.inner = { left: { value: from.inner.left.value }, right: from.inner.right };
  to.tag = from.tag;
  moveInit$Pair(to.inner, from.inner);
  return;
}

function moveInit$Either(to, from) {
  to.$tag = from.$tag;
  to.n = from.n;
  to.h = { value: from.h.value };
  if (from.$tag === 1) {
    Sink_Handle_sink(to.h, from.h);
  }
  return;
}

function ResolveTest_lambda$0$make(env) {
  return function() {
    return env.h.value;
  };
}

function relocate_Handle(x) {
  var y = { value: 0n };
  Sink_Handle_sink(y, x);
  return y;
}

function relocate_Pair(x) {
  var y = { left: { value: 0n }, right: 0n };
  moveInit$Pair(y, x);
  return y;
}

function drop$_sub_gt_I64(value) {
  var v1 = value.$v.$e;
  if (v1 != null) {
    value.$v.$h[0](v1);
  }
  return;
}

function reclaim$_sub_gt_I64(value) {
  var v1 = value.$v.$e;
  if (v1 != null) {
    value.$v.$h[8](v1);
  }
  return;
}
