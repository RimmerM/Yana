"use strict";
// ResolveTest
function main() {
  var v46;
  var v71;
  var v87;
  var v103;
  var v119;
  var v137;
  var v157;
  var v33 = ("red" === "red" ? 1 : 0) + ("green" === "green" ? 2 : 0) + ("blue" === "blue" ? 4 : 0) + ("violet" === "violet" ? 8 : 0) + ("white" === "white" ? 16 : 0);
  if ("warm" === "warm") {
    v46 = "warm" === "warm" ? 1 : 0;
  } else {
    v46 = 0;
  }
  B0: {
    B1: {
      var v49 = v33 + (v46 ? 32 : 0);
      if ("cool" === "cool") {
        if ("cool" === "cool") {
          v71 = "cool" === "cool" ? 1 : 0;
          break B0;
        }
      }
    }
    v71 = 0;
  }
  var v74 = v49 + (v71 ? 64 : 0);
  if ("colour" === "colour") {
    v87 = "colour" === "colour" ? 1 : 0;
  } else {
    v87 = 0;
  }
  var v90 = v74 + (v87 ? 128 : 0);
  if ("zero" === "zero") {
    v103 = "one" === "one" ? 1 : 0;
  } else {
    v103 = 0;
  }
  var v106 = v90 + (v103 ? 256 : 0);
  if ("-" === "-") {
    v119 = "+" === "+" ? 1 : 0;
  } else {
    v119 = 0;
  }
  var v122 = v106 + (v119 ? 512 : 0);
  if ("" === "") {
    v137 = "".length === 0 ? 1 : 0;
  } else {
    v137 = 0;
  }
  var v140 = v122 + (v137 ? 1024 : 0);
  if ("violet".length === 6) {
    v157 = "red".length === 3 ? 1 : 0;
  } else {
    v157 = 0;
  }
  var v160 = v140 + (v157 ? 2048 : 0);
  var first = "green";
  var second = "green";
  return v160 + ((first === second ? 1 : 0) + (first.length === second.length ? 2 : 0) === 3 ? 4096 : 0);
}
main();
