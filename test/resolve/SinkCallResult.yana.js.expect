"use strict";
// ResolveTest
function joined(a, b) {
  return a + b;
}

function threeParts(a, b, c) {
  return joined(joined(a, b), c);
}

function main() {
  if (joined("ab", "cd") === "abcd" ^ 1) {
    return -1;
  } else {
    var three = threeParts("a", "bc", "def");
    if (three === "abcdef" ^ 1) {
      return -2;
    } else {
      var v20 = [];
      var v29 = joined("x", "y");
      v20[0] = v29;
      var v37 = threeParts("p", "q", "r");
      v20[1] = v37;
      if (v20.length !== 2) {
        v20.length = 2;
      }
      if (v20[0] === "xy" ^ 1) {
        return -3;
      } else {
        if (v20[1] === "pqr" ^ 1) {
          return -4;
        } else {
          return three.length + v20[1].length | 0;
        }
      }
    }
  }
}
main();
