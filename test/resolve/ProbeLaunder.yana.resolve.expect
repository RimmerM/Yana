let image$base: () = <none>

fn make(%x: &Int, %y: &Int) -> Pair(&Int, &Int) {
  b0:
    %v2 = alloc : Pair(&Int, &Int)
    aggregate %local0@Pair.first = %x, %local0@Pair.second = %y
    ret %v2
}

fn main() -> Int {
  b0:
    %outer = alloc : Int
    init %outer, 0
    je True, b1, b2

  b1:
    %a = alloc : Int
    init %a, 3
    %b = alloc : Int
    init %b, 4
    %v11 = load %a : Int
    %v12 = borrow %a : &Int
    %v13 = load %b : Int
    %v14 = borrow %b : &Int
    %v15 = call make, %v12, %v14 : Pair(&Int, &Int)
    %v16 = load %local3@Pair.first : &Int
    %v17 = load [%v16] : Int
    assign %outer, %v17
    jmp b3

  b2:
    jmp b3

  b3:
    %v21 = load %outer : Int
    ret %v21
}
