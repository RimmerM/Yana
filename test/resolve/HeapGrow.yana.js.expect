"use strict";
// Core
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

function growJsArray_Int(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

// ResolveTest
function main() {
  var xs = { items: new Int32Array([]), length: 0 };
  var i = 0;
  L0: for (;;) {
    if (i < 200000) {
      var count = xs.length;
      var v157 = count + 1;
      if (v157 > xs.items.length) {
        growJsArray_Int(xs, v157);
      }
      xs.items[count] = i;
      xs.length = v157 & 1073741823;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  var v23 = xs.length;
  if (v23 !== 200000) {
    return -1;
  } else {
    checkCondition(0 >= v23 ? 1 : 0);
    var v42 = xs.items;
    if (v42[0]) {
      return -2;
    } else {
      checkCondition(131071 >= v23 ? 1 : 0);
      if (v42[131071] !== 131071) {
        return -3;
      } else {
        checkCondition(131072 >= v23 ? 1 : 0);
        if (v42[131072] !== 131072) {
          return -4;
        } else {
          checkCondition(199999 >= v23 ? 1 : 0);
          if (v42[199999] !== 199999) {
            return -5;
          } else {
            var total = 0;
            var j = 0;
            L1: for (;;) {
              if (j < 200000) {
                checkCondition(j >>> 0 >= v23 ? 1 : 0);
                total = total + v42[j] | 0;
                j = j + 100 | 0;
                continue L1;
              }
              break L1;
            }
            return total;
          }
        }
      }
    }
  }
}

// a typed array's growth - see Implementation-Containers.md, section 14
function $grow(a, n) {
  const b = new a.constructor(n);
  b.set(a);
  return b;
}
main();
