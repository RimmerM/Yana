27:8: error: an authored `Reclaim` may only release storage - it cannot call recordClose, because a region discharges every `Reclaim` inside it in bulk and this would then run somewhere the program never asked for. Write a `Drop` for an effect that has to happen at last use
40:40: error: an authored `Reclaim` may only release storage - it cannot call closeLedger, because a region discharges every `Reclaim` inside it in bulk and this would then run somewhere the program never asked for. Write a `Drop` for an effect that has to happen at last use
