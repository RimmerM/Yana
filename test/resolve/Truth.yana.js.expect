"use strict";
// ResolveTest
function fromBool(value) {
  return value ? 1 : 0;
}

function compared(a, b) {
  return a > b ? 1 : 0;
}

function fromInt(value) {
  return value !== 0 ? 1 : 0;
}

function fromLong(value) {
  return value !== 0n ? 1 : 0;
}

function fromFloat(value) {
  return value !== 0 ? 1 : 0;
}

function fromLiteral() {
  return 1;
}

function classify(value) {
  return (value - 1 | 0) !== 0 ? 2 : value !== 0 ? 1 : 0;
}

function loop(value) {
  if (value !== 0) {
    return 5;
  } else {
    return 6;
  }
}

function main() {
  return 11583;
}

function present_Int(value) {
  return value !== 0 ? 1 : 0;
}

function present_Float(value) {
  return value !== 0 ? 1 : 0;
}
