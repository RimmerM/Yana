"use strict";
// Collections
function remove_Int(self, index) {
  if (index >>> 0 >= self.length >>> 0) {
    return null;
  } else {
    var v16 = self[index];
    self.splice(index, 1);
    return v16;
  }
}

// ResolveTest
function sum(xs$items, xs$length, xs$offset) {
  var total = 0;
  var i = 0;
  L0: for (;;) {
    if (i < xs$length) {
      total = total + xs$items[xs$offset + i | 0] | 0;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  return total;
}

function make() {
  return [7, 8, 9];
}

function escaping() {
  return 24;
}

function main() {
  var v4 = [1, 2, 3];
  v4[0] = 10;
  v4[v4.length] = 4;
  v4[v4.length] = 5;
  var v35 = remove_Int(v4, 1);
  var v40 = remove_Int(v4, 99);
  var v48 = remove_Int(v4, -1);
  return (sum(v4, v4.length, 0) + v4.length | 0) + 24 | 0;
}
