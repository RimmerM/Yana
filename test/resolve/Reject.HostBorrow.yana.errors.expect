25:4: error: cannot borrow this field - its record is `@layout(js)`, so the field holds what a host reader expects rather than what a reference reads and writes through. Reading or assigning the field works; only the reference has nowhere to convert
30:4: error: cannot borrow this field - its record is `@layout(js)`, so the field holds what a host reader expects rather than what a reference reads and writes through. Reading or assigning the field works; only the reference has nowhere to convert
34:0: error: cannot borrow this field - its record is `@layout(js)`, so the field holds what a host reader expects rather than what a reference reads and writes through. Reading or assigning the field works; only the reference has nowhere to convert
