let image$base: () = <none>

fn pairOf(%x: 'Int, %y: 'Int) -> Pair('Int, 'Int) {
  b0:
    %v2 = alloc : Pair('Int, 'Int)
    aggregate %local0@Pair.first = %x, %local0@Pair.second = %y
    ret %v2
}

fn firstOf(%x: 'Int, %y: 'Int) -> 'Int {
  b0:
    ret %x
}

fn passThrough(%p: %U8) -> %U8 {
  b0:
    ret %p
}

fn offsetOf(%p: %U8) -> %U8 {
  b0:
    %v2 = strideof U8 : Size
    %v3 = mul 1, %v2 : Size
    %v4 = add %p, %v3 : %U8
    ret %v4
}

fn maybeOf(%x: 'Int) -> Maybe('Int) {
  b0:
    %v1 = alloc : Maybe('Int)
    aggregate %local0.discriminant = 1, %local0@Just = %x
    ret %v1
}

fn main() -> Int {
  b0:
    %a = alloc : Int
    init %a, 3
    %b = alloc : Int
    init %b, 4
    %v6 = load %a : Int
    %v7 = borrow %a : 'Int
    %v8 = load %b : Int
    %v9 = borrow %b : 'Int
    %p = call pairOf, %v7, %v9 : Pair('Int, 'Int)
    %v11 = load %a : Int
    %v12 = borrow %a : 'Int
    %v13 = load %b : Int
    %v14 = borrow %b : 'Int
    %v15 = call firstOf, %v12, %v14 : 'Int
    %v16 = load %a : Int
    %v17 = borrow %a : 'Int
    %m = call maybeOf, %v17 : Maybe('Int)
    %v19 = load %local3.discriminant : Int
    %v21 = cmp_eq %v19, 1 : Bool
    je %v21, b2, b1

  b1:
    jmp b3

  b2:
    %v = load %local3@Just : 'Int
    %v = load [%v] : Int
    %v27 = add %v, 0 : Int
    jmp b3

  b3:
    %seen = phi [b2, %v27], [b1, 0] : Int
    %v33 = load %local2@Pair.first : 'Int
    %v34 = load %local2@Pair.second : 'Int
    %v35 = load [%v33] : Int
    %v36 = load [%v34] : Int
    %v37 = add %v35, %v36 : Int
    %v38 = load [%v15] : Int
    %v39 = add %v37, %v38 : Int
    %v40 = add %v39, %seen : Int
    ret %v40
}
