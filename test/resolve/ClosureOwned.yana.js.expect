"use strict";
// Core
function teardown$none(value) {
}

// ResolveTest
function total(xs$items, xs$length, xs$offset) {
  var sum = 0;
  var i = 0;
  var v21 = xs$length >>> 0;
  L0: for (;;) {
    if (i < xs$length) {
      var v18 = xs$offset + i | 0;
      if (i >>> 0 >= v21) {
        throw "yana: a runtime check failed";
      } else {
        sum = sum + xs$items[v18] | 0;
        i = i + 1 | 0;
        continue L0;
      }
    } else {
      return sum;
    }
    break L0;
  }
}

function makeSummer(xs) {
  return { $c: ResolveTest_lambda$18, $e: xs };
}

function main() {
  var first = makeSummer({ items: new Int32Array([1, 2, 3, 4]), length: 4 });
  var second = makeSummer({ items: new Int32Array([10, 20]), length: 2 });
  var v28 = first.$c(first.$e);
  drop$_sub_gt_Int(first.$c, first.$e);
  var v29 = second.$c(second.$e);
  drop$_sub_gt_Int(second.$c, second.$e);
  return v28 + v29 | 0;
}

function ResolveTest_lambda$18(env) {
  return total(env.items, env.length, 0);
}

function drop$_sub_gt_Int(value$c, value$e) {
  var v4 = value$c.$h;
  if (v4 != null) {
    v4[0](value$e);
  }
}
main();
