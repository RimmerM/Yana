fn sum(%both: Same(U64)) -> U64 {
  b0:
    %v1 = load %both@Same.left : U64
    %v2 = load %both@Same.right : U64
    %v3 = add %v1, %v2 : U64
    ret %v3
}

fn narrowFirst(%narrow: @bits(53) U64, %plain: U64) -> U64 {
  b0:
    %both = alloc : Same(U64)
    %narrow = cast %narrow : U64
    init %local0@Same.left, %narrow
    init %local0@Same.right, %plain
    %v6 = call sum, %both : U64
    ret %v6
}

fn plainFirst(%plain: U64, %narrow: @bits(53) U64) -> U64 {
  b0:
    %both = alloc : Same(U64)
    %narrow = cast %narrow : U64
    init %local0@Same.left, %plain
    init %local0@Same.right, %narrow
    %v6 = call sum, %both : U64
    ret %v6
}

fn main() -> Int {
  b0:
    %v2 = call narrowFirst, 21, 4000 : U64
    %v3 = cast %v2 : Int
    %v6 = call plainFirst, 300, 5 : U64
    %v7 = cast %v6 : Int
    %v8 = add %v3, %v7 : Int
    ret %v8
}
