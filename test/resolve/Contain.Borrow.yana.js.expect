"use strict";
// Core
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

function growJsArray_Int(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

// ResolveTest
function mutate(f) {
  f.$o[f.$k] = $b1u$set(f.$o[f.$k], f.$s, 1);
  return 0;
}

function fill(n) {
  var v1 = { items: new Int32Array([]), length: 0 };
  var i = 0;
  L0: for (;;) {
    if (i < n) {
      var count = v1.length;
      var v88 = count + 1;
      if (v88 > v1.items.length) {
        growJsArray_Int(v1, v88);
      }
      v1.items[count] = i;
      v1.length = v88 & 1073741823;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  var sum = 0;
  var k = 0;
  var v39 = v1.length;
  var v42 = v1.items;
  L1: for (;;) {
    if (k < n) {
      checkCondition(k >>> 0 >= v39 ? 1 : 0);
      var v58 = v42[k] + 1 | 0;
      v42[k] = v58;
      sum = sum + v58 | 0;
      k = k + 1 | 0;
      continue L1;
    }
    break L1;
  }
  return sum;
}

function main() {
  var v5 = { $v: 2 };
  return ((mutate({ $o: v5, $k: "$v", $s: 1 }) + 10 | 0) + (v5.$v & 1 ? 100 : 200) | 0) + fill(3) | 0;
}

// bit ranges of a word wider than the host's operators - see codegen/js/place.cpp
function $b1u$set(w, s, v) {
  return w + ((v & 1) - (Math.floor(w / s) & 1)) * s;
}

// a typed array's growth - see Implementation-Containers.md, section 14
function $grow(a, n) {
  const b = new a.constructor(n);
  b.set(a);
  return b;
}
main();
