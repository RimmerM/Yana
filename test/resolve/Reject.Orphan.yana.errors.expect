16:0: error: this instance of Eq is an orphan - it is in ResolveTest, which declares neither Eq nor any type in the instance head, so a third module could declare the same instance and which one a call site sees would depend on what happened to be compiled. Put it in the module declaring the class or in the one declaring the type, or wrap the type in a qualified alias
