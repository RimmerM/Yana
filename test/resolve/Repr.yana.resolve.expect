fn useId(%it: @bits(53) U64) -> @bits(53) U64 {
  b0:
    ret %it
}

fn useFlags(%it: Flags) -> Bool {
  b0:
    %v1 = load %it@Flags.optionA : Bool
    ret %v1
}

fn useWrapped(%it: Wrapped) -> @bits(53) U64 {
  b0:
    %v1 = load %it@Wrapped.value : @bits(53) U64
    ret %v1
}

fn useRaw(%it: Raw) -> Bool {
  b0:
    %v1 = load %it@Raw.at : %U8
    %v2 = cast %v1 : Long
    %v4 = cmp_eq %v2, 0 : Bool
    ret %v4
}

fn main() -> Int {
  b0:
    %flags = alloc : Flags
    init %local0@Flags.optionA, True
    init %local0@Flags.optionB, False
    %wrapped = alloc : Wrapped
    init %local1@Wrapped.value, 7
    %v8 = call useWrapped, %wrapped : @bits(53) U64
    %widened = cast %v8 : U64
    %v10 = call useFlags, %flags : Bool
    je %v10, b1, b2

  b1:
    jmp b3

  b2:
    jmp b3

  b3:
    %v18 = phi [b1, 1], [b2, 0] : Int
    %v19 = cast %widened : Int
    %v20 = add %v18, %v19 : Int
    ret %v20
}
