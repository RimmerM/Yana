23:8: error: an operator cannot be declared under a namespace - it is only ever written bare, so nothing could name this one
25:0: error: String.mine adds to the namespace of a type this module does not declare - a type's methods have to be one set, or `x.f(y)` would mean different things in two files. Write a plain function and call it with a dot
