"use strict";
// ResolveTest
function floatDefault() {
  return 1.5;
}

function doubleFromContext() {
  return 1.5;
}

function floatFromInteger() {
  return 2;
}

function doubleFromInteger() {
  return 3;
}

function ascribedDouble() {
  return 1.5;
}

function ascribedFloat() {
  return 4;
}

function mixedIf(c, a, b) {
  var v;
  if (c) {
    v = a;
  } else {
    v = b;
  }
  return v;
}

function mixedMultiIf(c, a, b) {
  var v;
  if (c) {
    v = BigInt.asIntN(64, BigInt(a));
  } else {
    v = b;
  }
  return v;
}

function mixedMatch(c, a, b) {
  var v;
  if (c) {
    v = Math.fround(a);
  } else {
    v = b;
  }
  return v;
}

function divergingArm(c, a) {
  if (c) {
    return 7;
  } else {
    return a;
  }
}

function main() {
  return ((((((((Math.trunc(mixedIf(1, 3, 4.5)) | 0) + 12 | 0) + (Math.trunc(mixedIf(0, 3, 4.5)) | 0) | 0) + (Number(mixedMultiIf(1, 5, 6n)) | 0) | 0) + (Number(mixedMultiIf(0, 5, 6n)) | 0) | 0) + (Math.trunc(mixedMatch(1, 8, 9.25)) | 0) | 0) + (Math.trunc(mixedMatch(0, 8, 9.25)) | 0) | 0) + divergingArm(1, 1) | 0) + divergingArm(0, 2) | 0;
}
