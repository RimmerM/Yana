"use strict";
var seed = 3;

// ResolveTest
function main() {
  var v117$0 = seed + seed | 0;
  var v117$1 = seed + seed | 0;
  var v119$0 = Math.imul(seed, 2);
  var v119$1 = Math.imul(seed, 2);
  if (v117$0 !== 6) {
    return -1;
  } else {
    if (v119$1 !== 6) {
      return -2;
    } else {
      var mixed$0 = 10;
      if (Math.max(Math.max(10, seed), Math.max(seed, seed)) !== 10) {
        return -3;
      } else {
        if (Math.min(Math.min(mixed$0, seed), Math.min(seed, seed)) !== 3) {
          return -4;
        } else {
          var total = [seed, seed, seed, seed];
          var i = 0;
          L0: for (;;) {
            if (i < 4) {
              total = [total[0] + seed | 0, total[1] + seed | 0, total[2] + seed | 0, total[3] + seed | 0];
              i = i + 1 | 0;
              continue L0;
            }
            break L0;
          }
          if (total[0] !== 15) {
            return -5;
          } else {
            if (total[1] !== 15) {
              return -6;
            } else {
              if (((total[0] + total[1] | 0) + (total[2] + total[3] | 0) | 0) !== 60) {
                return -7;
              } else {
                return (Math.imul(v117$1, 100) + v119$0 | 0) + mixed$0 | 0;
              }
            }
          }
        }
      }
    }
  }
}
main();
