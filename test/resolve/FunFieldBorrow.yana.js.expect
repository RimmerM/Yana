"use strict";
// ResolveTest
function retarget(slot$o, slot$k, slot$ke) {
  slot$o[slot$k] = funvalue$triple;
  slot$o[slot$ke] = null;
  return;
}

function main() {
  var v0 = { weight: 1, run$c: funvalue$double, run$e: null };
  var before = Math.imul(v0.run$c(v0.run$e, 10), v0.weight);
  retarget(v0, "run$c", "run$e");
  var after = Math.imul(v0.run$c(v0.run$e, 10), v0.weight);
  v0.weight = 4;
  v0.run$c = funvalue$negate;
  v0.run$e = null;
  return (before + after | 0) + Math.imul(v0.run$c(v0.run$e, 10), v0.weight) | 0;
}

function funvalue$triple(env, x) {
  return Math.imul(x, 3);
}

function funvalue$negate(env, x) {
  return 0 - x | 0;
}

function funvalue$double(env, x) {
  return Math.imul(x, 2);
}
main();
