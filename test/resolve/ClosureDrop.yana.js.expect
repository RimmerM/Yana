"use strict";
var dropCalls = 0;
const closure$ResolveTest_lambda$0 = [dropAt$_g_Guard, null];

// ResolveTest
function guarded(id) {
  return ResolveTest_lambda$0$make({ $v: id });
}

function plain() {
  return ResolveTest_lambda$1;
}

function main() {
  var f = guarded(7);
  var a = f();
  drop$_sub_gt_Int(f);
  if (dropCalls !== 1) {
    return -1;
  } else {
    var b = ResolveTest_lambda$1();
    drop$_sub_gt_Int(ResolveTest_lambda$1);
    if (dropCalls !== 1) {
      return -2;
    } else {
      return (a + b | 0) + dropCalls | 0;
    }
  }
}

function Drop_Guard_drop(self) {
  dropCalls = dropCalls + 1 | 0;
  return;
}

function ResolveTest_lambda$0$make(env) {
  var closure = function() {
    return env.$v;
  };
  closure.$e = env;
  closure.$h = closure$ResolveTest_lambda$0;
  return closure;
}

function drop$_g_Guard(value) {
  dropCalls = dropCalls + 1 | 0;
  return;
}

function dropAt$_g_Guard(value) {
  dropCalls = dropCalls + 1 | 0;
  return;
}

function ResolveTest_lambda$1() {
  return 5;
}

function drop$_sub_gt_Int(value) {
  var v1 = value.$e;
  if (v1 != null) {
    value.$h[0](v1);
  }
  return;
}

function reclaim$_sub_gt_Int(value) {
  var v1 = value.$e;
  if (v1 != null) {
    value.$h[8](v1);
  }
  return;
}
