26:16: error: cannot pass Array(I32) where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
26:11: error: cannot pass Array(I32) where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
34:4: error: cannot pass Array(I32) where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
40:17: error: cannot pass Array(I32) where Flat(U8) is wanted: its elements are I32 and the window's are U8. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
40:11: error: cannot pass Array(I32) where Flat(U8) is wanted: its elements are I32 and the window's are U8. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
47:16: error: cannot pass [I32 *2] where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
47:11: error: cannot pass [I32 *2] where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
51:16: error: cannot pass @inline(2) @capacity(2) Array(I32) where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
51:11: error: cannot pass @inline(2) @capacity(2) Array(I32) where Flat(I64) is wanted: its elements are I32 and the window's are I64. A window is the array's own buffer read at the window's element type, so the two have to be the same type - a difference in width is not something a window can convert across, because it does not copy anything
