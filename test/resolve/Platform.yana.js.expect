"use strict";
// ResolveTest
function backend() {
  return 2;
}

function emptyStorage() {
  return { count: 0 };
}

function capacityOf(s) {
  return 0;
}

function shared(x) {
  return Math.imul(x, 10) | 0;
}

function main() {
  var s = emptyStorage();
  return (backend() + capacityOf(s) | 0) + shared(4) | 0;
}
