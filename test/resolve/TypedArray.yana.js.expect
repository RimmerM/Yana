"use strict";
// Core
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

function push_Int(self, item) {
  var count = self.length;
  var v7 = count + 1;
  if (v7 > self.items.length) {
    growJsArray_Int(self, v7);
  }
  self.items[count] = item;
  self.length = v7 & 1073741823;
}

function growJsArray_Int(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

function push_U8(self, item) {
  var count = self.length;
  var v7 = count + 1;
  if (v7 > self.items.length) {
    growJsArray_U8(self, v7);
  }
  self.items[count] = item;
  self.length = v7 & 1073741823;
}

function growJsArray_U8(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

function growJsArray_I8(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

function growJsArray_Float(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

function growJsArray_Double(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

// ResolveTest
function sumInts(xs$items, xs$length, xs$offset) {
  var total = 0;
  var v9 = 0;
  var v21 = xs$length >>> 0;
  L0: for (;;) {
    var i = v9;
    if (i < xs$length) {
      var v18 = xs$offset + i | 0;
      checkCondition(i >>> 0 >= v21 ? 1 : 0);
      total = total + xs$items[v18] | 0;
      var v29 = v9;
      if ((xs$length - v29 | 0) > 1) {
        v9 = v29 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return total;
}

function ints() {
  var v3 = { items: new Int32Array([1, 2, 3]), length: 3 };
  push_Int(v3, 4);
  push_Int(v3, 5);
  var count = v3.length;
  if (0 < count) {
    v3.items.copyWithin(0, 1, count);
    v3.length = count - 1 & 1073741823;
  }
  var v30 = v3.items;
  var v31 = v3.length;
  return (sumInts(v30, v31, 0) + v31 | 0) + v30.length | 0;
}

function bytes() {
  var v3 = { items: new Uint8Array([250, 251, 252]), length: 3 };
  push_U8(v3, 253);
  push_U8(v3, 254);
  var total = 0;
  var v26 = v3.length;
  var v32 = 0;
  var v46 = v3.items;
  L0: for (;;) {
    var i = v32;
    if (i < v26) {
      checkCondition(i >>> 0 >= v26 ? 1 : 0);
      total = total + v46[i] | 0;
      var v52 = v32;
      if ((v26 - v52 | 0) > 1) {
        v32 = v52 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return total;
}

function signedBytes() {
  var v13 = { items: new Int8Array([-1, -128, 127]), length: 3 };
  growJsArray_I8(v13, 4);
  v13.items[3] = -2;
  v13.length = 4;
  var total = 0;
  var v41 = 0;
  var v55 = v13.items;
  L0: for (;;) {
    var i = v41;
    if (i < 4) {
      checkCondition(i >>> 0 >= 4 ? 1 : 0);
      total = total + v55[i] | 0;
      var v61 = v41;
      if ((4 - v61 | 0) > 1) {
        v41 = v61 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return total;
}

function floats() {
  var v3 = { items: new Float32Array([0.5, 0.25, 0.125]), length: 3 };
  growJsArray_Float(v3, 4);
  v3.items[3] = 0.0625;
  v3.length = 4;
  var total = 0;
  var v27 = 0;
  var v41 = v3.items;
  L0: for (;;) {
    var i = v27;
    if (i < 4) {
      checkCondition(i >>> 0 >= 4 ? 1 : 0);
      total = Math.fround(total + v41[i]);
      var v46 = v27;
      if ((4 - v46 | 0) > 1) {
        v27 = v46 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return Math.min(Math.max(Math.trunc(Math.fround(total * 16)), -2.147483648E9), 2.147483647E9) | 0;
}

function doubles() {
  var v2 = { items: new Float64Array([1.5, 2.25]), length: 2 };
  growJsArray_Double(v2, 3);
  v2.items[2] = 4.125;
  var v97 = v2.items;
  v97.copyWithin(1, 2, 3);
  v2.length = 2;
  var total = 0;
  var v30 = 0;
  L0: for (;;) {
    var i = v30;
    if (i < 2) {
      checkCondition(i >>> 0 >= 2 ? 1 : 0);
      total = total + v97[i];
      var v49 = v30;
      if ((2 - v49 | 0) > 1) {
        v30 = v49 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return Math.min(Math.max(Math.trunc(total * 8), -2.147483648E9), 2.147483647E9) | 0;
}

function records() {
  var v9 = [{ a: 1, b: 2 }, { a: 3, b: 4 }];
  var v17 = { a: 5, b: 6 };
  v9[2] = { a: v17.a, b: v17.b };
  if (v9.length !== 3) {
    v9.length = 3;
  }
  v9.copyWithin(0, 1, 3);
  if (v9.length !== 2) {
    v9.length = 2;
  }
  var total = 0;
  var v39 = 0;
  L0: for (;;) {
    var i = v39;
    if (i < 2) {
      checkCondition(i >>> 0 >= 2 ? 1 : 0);
      total = (total + v9[i].a | 0) + v9[i].b | 0;
      var v72 = v39;
      if ((2 - v72 | 0) > 1) {
        v39 = v72 + 1 | 0;
        continue L0;
      }
    }
    break L0;
  }
  return total;
}

function main() {
  return ((((ints() + bytes() | 0) + signedBytes() | 0) + floats() | 0) + doubles() | 0) + records() | 0;
}

// a typed array's growth - see Implementation-Containers.md, section 14
function $grow(a, n) {
  const b = new a.constructor(n);
  b.set(a);
  return b;
}
main();
