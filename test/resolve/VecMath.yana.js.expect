"use strict";
// ResolveTest
function scalars() {
  if (Math.sqrt(16) !== 4) {
    return -1;
  } else {
    if (Math.sqrt(2.25) !== 1.5) {
      return -2;
    } else {
      if (Math.fround(Math.sqrt(6.25)) !== 2.5) {
        return -4;
      } else {
        if (Math.fround(1 / Math.fround(Math.sqrt(0))) !== Math.fround(1 / 0)) {
          return -5;
        } else {
          return 0;
        }
      }
    }
  }
}

function lanes() {
  var roots$0 = Math.fround(Math.sqrt(6.25));
  var roots$1 = Math.fround(Math.sqrt(6.25));
  var roots$2 = Math.fround(Math.sqrt(6.25));
  var roots$3 = Math.fround(Math.sqrt(6.25));
  if (roots$0 !== 2.5) {
    return -1;
  } else {
    if (Math.min(Math.min(roots$0, roots$1), Math.min(roots$2, roots$3)) !== 2.5) {
      return -2;
    } else {
      var scaled$0 = Math.fround(Math.fround(0) + 1);
      var scaled$1 = Math.fround(Math.fround(3) + 1);
      var scaled$2 = Math.fround(Math.fround(6) + 1);
      var scaled$3 = Math.fround(Math.fround(9) + 1);
      if (scaled$0 !== 1) {
        return -3;
      } else {
        if (scaled$1 !== 4) {
          return -4;
        } else {
          if (scaled$2 !== 7) {
            return -5;
          } else {
            if (Math.fround(Math.fround(scaled$0 + scaled$1) + Math.fround(scaled$2 + scaled$3)) !== 22) {
              return -6;
            } else {
              return 0;
            }
          }
        }
      }
    }
  }
}

function main() {
  if (scalars() !== 0) {
    return scalars();
  } else {
    if (lanes() !== 0) {
      return Math.imul(lanes(), 10);
    } else {
      return 7;
    }
  }
}
main();
