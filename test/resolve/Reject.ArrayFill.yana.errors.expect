36:21: error: a count written `_` is taken from the literal the type is written at, and there is no literal here - write the number, or move the type onto a `::` in front of one
32:17: error: a count written `_` is taken from the literal the type is written at, and there is no literal here - write the number, or move the type onto a `::` in front of one
23:13: error: this literal has 2 elements and [I32 *5] holds exactly 5 - one element fills the whole array, and any other count has to be written out
28:14: error: a fill writes one value into every element, which only a type whose duplicate is its bytes may do - String is not one of those, so this array's elements have to be written out
41:17: error: a count written `_` is taken from the array literal the type is written at, and this is not one - write the number instead
