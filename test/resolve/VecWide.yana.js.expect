"use strict";
var seed = 3;

// ResolveTest
function main() {
  var v245$0 = seed + seed | 0;
  var v245$1 = seed + seed | 0;
  var v247$0 = Math.imul(seed, 2);
  var v247$1 = Math.imul(seed, 2);
  if (v245$0 !== 6) {
    return -1;
  } else {
    if (v247$1 !== 6) {
      return -2;
    } else {
      var lanes = 4;
      var mixed$0 = 10;
      if (Math.max(Math.max(10, seed), Math.max(seed, seed)) !== 10) {
        return -3;
      } else {
        if (Math.min(Math.min(mixed$0, seed), Math.min(seed, seed)) !== 3) {
          return -4;
        } else {
          var total = [seed, seed, seed, seed];
          var i = 0;
          L0: for (;;) {
            if (i < 4) {
              total = [total[0] + seed | 0, total[1] + seed | 0, total[2] + seed | 0, total[3] + seed | 0];
              i = i + 1 | 0;
              continue L0;
            }
            break L0;
          }
          if (total[0] !== 15) {
            return -5;
          } else {
            if (total[1] !== 15) {
              return -6;
            } else {
              if (((total[0] + total[1] | 0) + (total[2] + total[3] | 0) | 0) !== Math.imul(lanes, 15)) {
                return -7;
              } else {
                if (((seed + v245$0 | 0) + (v247$0 + total[0] | 0) | 0) !== 30) {
                  return -8;
                } else {
                  var above$0 = mixed$0 > seed;
                  var above$1 = seed > seed;
                  var above$2 = seed > seed;
                  var above$3 = seed > seed;
                  if ((above$0 ? 1 : 0) + (above$1 ? 1 : 0) + ((above$2 ? 1 : 0) + (above$3 ? 1 : 0)) !== 1) {
                    return -9;
                  } else {
                    if ((above$0 || above$1 || (above$2 || above$3)) ^ 1) {
                      return -10;
                    } else {
                      if (above$0 && above$1 && (above$2 && above$3)) {
                        return -11;
                      } else {
                        var picked$0 = above$0 ? 7 : 2;
                        var picked$1 = above$1 ? 7 : 2;
                        if (picked$0 !== 7) {
                          return -12;
                        } else {
                          if (picked$1 !== 2) {
                            return -13;
                          } else {
                            if ((seed > seed ? 0 : 10 > seed ? 1 : seed > seed ? 2 : seed > seed ? 3 : 4) !== 1) {
                              return -14;
                            } else {
                              if ((mixed$0 < seed ? 0 : seed < seed ? 1 : seed < seed ? 2 : seed < seed ? 3 : 4) !== lanes) {
                                return -15;
                              } else {
                                if (mixed$0 >= seed ? 0 : seed >= seed ? 1 : seed >= seed ? 2 : seed >= seed ? 3 : 4) {
                                  return -16;
                                } else {
                                  return (Math.imul(v245$1, 100) + v247$0 | 0) + mixed$0 | 0;
                                }
                              }
                            }
                          }
                        }
                      }
                    }
                  }
                }
              }
            }
          }
        }
      }
    }
  }
}
main();
