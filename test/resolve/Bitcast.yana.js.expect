"use strict";
// ResolveTest
function opaqueFloat(x) {
  return x;
}

function opaqueInt(x) {
  return x;
}

function main() {
  var score = 63;
  if (($bc$f2i(opaqueFloat(1)) | 0) === 1065353216) {
    score = score + 64 | 0;
  }
  if (Math.fround($bc$i2f(opaqueInt(1065353216))) === 1) {
    score = score + 128 | 0;
  }
  if (Math.fround($bc$i2f($bc$f2i(opaqueFloat(-2.5)) | 0)) === -2.5) {
    score = score + 256 | 0;
  }
  if (opaqueInt(-1) >>> 0 > opaqueInt(2147483647) >>> 0) {
    score = score + 512 | 0;
  }
  if (opaqueInt(1) >>> 0 === 1) {
    score = score + 1024 | 0;
  }
  var v165 = score + 2048 | 0;
  score = v165;
  return v165;
}

// a float's bits, which no host operator reaches - see codegen/js/inst.cpp
const $bcf = new Float32Array(1);
const $bci = new Int32Array($bcf.buffer);

function $bc$f2i(v) {
  $bcf[0] = v;
  return $bci[0];
}

function $bc$i2f(v) {
  $bci[0] = v;
  return $bcf[0];
}
main();
