13:19: error: only `@inline(n) @capacity(n)` is built - the rows that spill need a discriminant saying whether the inline bytes hold elements or a pointer to them, and that is Implementation-Containers.md §7.1's unbuilt half
17:18: error: only `@inline(n) @capacity(n)` is built - the rows that spill need a discriminant saying whether the inline bytes hold elements or a pointer to them, and that is Implementation-Containers.md §7.1's unbuilt half
20:16: error: only `@inline(n) @capacity(n)` is built - the rows that spill need a discriminant saying whether the inline bytes hold elements or a pointer to them, and that is Implementation-Containers.md §7.1's unbuilt half
28:16: error: `@inline(0) @capacity(0)` is an array that can hold nothing - write `[T]` for one that allocates, or a larger bound
24:20: error: `@inline` and `@capacity` refine a growable array - `[T]` - and this is I32
34:13: error: this array holds 5 elements and its type bounds it at 4 - `@capacity(n)` is a bound rather than a starting size
