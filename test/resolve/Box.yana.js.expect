"use strict";
const closure$ResolveTest_continuation$0 = [teardown$none, teardown$none];

// Core
function add_eq_Int(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
  return;
}

function teardown$none(value) {
  return;
}

// ResolveTest
function levelOf(d$count, d$level) {
  return d$level;
}

function raise(d) {
  add_eq_Int(d, "count", 1, 1);
  return;
}

function mutated() {
  var v0 = { hot: 0, cold: { count: 0, level: 0 } };
  var v2 = { count: 1, level: 20 };
  v0.hot = 3;
  v0.cold = { count: 0, level: 0 };
  v0.cold = v2;
  raise(v0.cold);
  var v21 = v0.cold.level + 1 | 0;
  v0.cold.level = v21;
  return ((Math.imul(v0.hot, 1000) | 0) + (Math.imul(v0.cold.count, 100) | 0) | 0) + v21 | 0;
}

function pinnedValue() {
  var p = { tag: 0, detail: { count: 0, level: 0 } };
  var v2 = { count: 2, level: 3 };
  p.tag = 5;
  p.detail = { count: 0, level: 0 };
  p.detail = v2;
  return ((Math.imul(p.detail.count, 10) | 0) + 500 | 0) + p.detail.level | 0;
}

function throughLens() {
  var v0 = { hot: 0, cold: { count: 0, level: 0 } };
  var v2 = { count: 6, level: 40 };
  v0.hot = 3;
  v0.cold = { count: 0, level: 0 };
  v0.cold = v2;
  return ResolveTest_continuation$0({ $v: v0 }, v0.cold.level);
}

function countOf(h) {
  return h.n;
}

function main() {
  var m = mutated();
  if (m !== 3221) {
    return -2;
  } else {
    var l = throughLens();
    if (l !== 46) {
      return -3;
    } else {
      return (m + l | 0) + pinnedValue() | 0;
    }
  }
}

function ResolveTest_continuation$0(env, value) {
  return value + env.$v.cold.count | 0;
}
ResolveTest_continuation$0.$h = closure$ResolveTest_continuation$0;

function levelIn_Int(c, body$c, body$e) {
  return body$c(body$e, c.cold.level);
}

function reclaim$Config(value) {
  return;
}

function reclaim$Pinned(value) {
  return;
}
