"use strict";
// ResolveTest
function main() {
  return (orElse_Int(5, 0) + 39 | 0) + orElse_Int(null, 7) | 0;
}

function identity_Int(value) {
  return value;
}

function swap_Bool_Int(value$first, value$second) {
  return { first: value$second, second: value$first };
}

function leftOf_Int_Bool(value$first, value$second) {
  return value$first;
}

function twice_Int(x) {
  return x + x | 0;
}

function smaller_Int(x, y) {
  var v2;
  if (x < y) {
    v2 = x;
  } else {
    v2 = y;
  }
  return v2;
}

function middle_Int(x, y, z) {
  return smaller_Int(smaller_Int(x, y), z);
}

function wrap_Int(value) {
  return value;
}

function orElse_Int(value, fallback) {
  var v2;
  if (!(value === null)) {
    v2 = value;
  } else {
    v2 = fallback;
  }
  return v2;
}

function empty_Int() {
  return null;
}
