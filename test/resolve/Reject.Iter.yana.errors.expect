32:54: error: cannot convert Int to a
32:49: error: cannot convert Int to a
32:0: warning: this `iter fn` never yields, so every `for` loop over it runs its body no times
38:4: error: an `iter fn` ends by running out of values rather than by `return` - what it produces is the loop's own signal, which is not something the body names
47:4: error: `step` belongs to a counted `for` and needs the interval it steps over - write `a .. b step n`, or drop it to iterate what the call yields one at a time
56:25: error: a `for` step of zero never reaches the end of its range
66:13: error: upTo is an iterator, so this call has no body to hand its values to - write it as the source of a `for` loop, which is the only thing that supplies one
68:13: error: `for` runs a call of a named `iter fn`, and no rule finds one from the type of a value - name the traversal, as in `for x in items(xs)`. A value that is already an iterator is the other reading of this, and stepping one rather than running it is external iteration, which this version does not have
77:13: error: `for` runs a call of a named `iter fn`, and no rule finds one from the type of a value - name the traversal, as in `for x in items(xs)`. A value that is already an iterator is the other reading of this, and stepping one rather than running it is external iteration, which this version does not have
85:13: error: a `for` loop reaches its iterator by name in this version - an iterator passed or returned as a value, which is what an adaptor chain like `xs.filter(p).map(f)` is made of, needs the erased callback ABI
92:13: error: plain is not an `iter fn`, so a `for` loop has nothing to be the body of - a collection is iterated by an `iter fn` over it rather than directly
100:13: error: upTo takes 1 arguments before the loop body, but this call was given 0
109:8: error: this pattern can fail - it does not match Nothing - and a `for` loop has no alternative to take for an element it does not match
116:8: error: this iterator hands over nothing, so the loop's pattern has nothing to bind - an `iter fn` a `for` loop names has to declare what it yields
124:8: error: a `for` loop does not produce a value in this version, so `break` cannot carry one - the loop's own value is what the iterator's result would have to hold, which is Analysis-Lens.md's V3
136:8: error: `break` and `continue` cannot cross a lens call yet - the loop is in the function this block was split out of, and only `return` carries the exit signal past a lens
145:15: error: cannot convert Int to a
145:8: error: cannot convert Int to a
145:8: error: cannot convert Int to a
