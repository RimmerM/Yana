let image$base: () = <none>

let &log: Int = 0

let &dropped: Int = 0

fn Try(Maybe(a), a, ()).toOutcome(Int)(%value: Maybe(Int)) -> Outcome(Int, ()) {
  b0:
    %v2 = load %value.discriminant : Int
    %v4 = cmp_eq %v2, 1 : Bool
    je %v4, b2, b1

  b1:
    %v6 = alloc : Outcome(Int, ())
    init %local3.discriminant, 1
    jmp b3

  b2:
    %v10 = load %value@Just : Int
    %v11 = move %value@Just : Int
    %inner = alloc : Int
    init %local1, %v11
    %v14 = alloc : Outcome(Int, ())
    %inner = load %local1 : Int
    aggregate %local2.discriminant = 0, %local2@Proceed = %inner
    jmp b3

  b3:
    %v1 = phi [b2, %v14], [b1, %v6] : Outcome(Int, ())
    ret %v1
}

fn Try(Maybe(a), a, ()).fromExit(Int)(%reason: ()) -> Maybe(Int) {
  b0:
    %v1 = alloc : Maybe(Int)
    init %local0.discriminant, 0
    ret %v1
}

fn Try(Maybe(a), a, ()).toOutcome(Token)(%value: Maybe(Token)) -> Outcome(Token, ()) {
  b0:
    %v2 = load %value.discriminant : Int
    %v4 = cmp_eq %v2, 1 : Bool
    je %v4, b2, b4

  b1:
    %v6 = alloc : Outcome(Token, ())
    init %local3.discriminant, 1
    jmp b3

  b2:
    %v10 = load %value@Just : Token
    %v11 = move %value@Just : Token
    %inner = alloc : Token
    init %local1, %v11
    %v14 = alloc : Outcome(Token, ())
    aggregate %local2.discriminant = 0, %local2@Proceed = %inner
    jmp b3

  b3:
    %v1 = phi [b2, %v14], [b1, %v6] : Outcome(Token, ())
    ret %v1

  b4:
    drop_derived %value via drop$Maybe(Token)
    jmp b1
}

fn Try(Result(e, a), a, e).toOutcome(Int, Int)(%value: Result(Int, Int)) -> Outcome(Int, Int) {
  b0:
    %v2 = load %value.discriminant : Int
    %v4 = cmp_eq %v2, 1 : Bool
    je %v4, b2, b1

  b1:
    %v6 = load %value@Err : Int
    %v7 = move %value@Err : Int
    %reason = alloc : Int
    init %local3, %v7
    %v10 = alloc : Outcome(Int, Int)
    %reason = load %local3 : Int
    aggregate %local4.discriminant = 1, %local4@Exit = %reason
    jmp b3

  b2:
    %v15 = load %value@Ok : Int
    %v16 = move %value@Ok : Int
    %inner = alloc : Int
    init %local1, %v16
    %v19 = alloc : Outcome(Int, Int)
    %inner = load %local1 : Int
    aggregate %local2.discriminant = 0, %local2@Proceed = %inner
    jmp b3

  b3:
    %v1 = phi [b2, %v19], [b1, %v10] : Outcome(Int, Int)
    ret %v1
}

fn Try(Result(e, a), a, e).fromExit(Int, Int)(%reason: Int) -> Result(Int, Int) {
  b0:
    %v1 = alloc : Result(Int, Int)
    aggregate %local1.discriminant = 0, %local1@Err = %reason
    ret %v1
}

fn Try(Result(e, a), a, e).fromExit(Int, Bool)(%reason: Int) -> Result(Int, Bool) {
  b0:
    %v1 = alloc : Result(Int, Bool)
    aggregate %local1.discriminant = 0, %local1@Err = %reason
    ret %v1
}

fn drop$Maybe(Token)(%value: Maybe(Token)) -> () {
  b0:
    %v1 = load %value.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b3

  b1:
    ret

  b2:
    drop %value@Just via Drop(Token).drop
    jmp b1

  b3:
    jmp b1
}

fn parse(%n: Int) -> Maybe(Int) {
  b0:
    %v1 = load log : Int
    %v4 = add %v1, 1 : Int
    assign log, %v4
    %v8 = cmp_gt %n, 0 : Bool
    je %v8, b1, b2

  b1:
    %v10 = alloc : Maybe(Int)
    %v14 = mul %n, 2 : Int
    aggregate %local0.discriminant = 1, %local0@Just = %v14
    ret %v10

  b2:
    jmp b3

  b3:
    %v18 = alloc : Maybe(Int)
    init %local1.discriminant, 0
    ret %v18
}

fn checked(%n: Int) -> Result(Int, Int) {
  b0:
    %v1 = load log : Int
    %v4 = add %v1, 10 : Int
    assign log, %v4
    %v8 = cmp_gt %n, 9 : Bool
    je %v8, b1, b2

  b1:
    %v10 = alloc : Result(Int, Int)
    aggregate %local0.discriminant = 0, %local0@Err = 1
    ret %v10

  b2:
    jmp b3

  b3:
    %v18 = cmp_lt %n, 0 : Bool
    je %v18, b4, b5

  b4:
    %v20 = alloc : Result(Int, Int)
    aggregate %local1.discriminant = 0, %local1@Err = 2
    ret %v20

  b5:
    jmp b6

  b6:
    %v26 = alloc : Result(Int, Int)
    aggregate %local2.discriminant = 1, %local2@Ok = %n
    ret %v26
}

fn oneStep(%n: Int) -> Maybe(Int) {
  b0:
    %v1 = call parse, %n : Maybe(Int)
    %v2 = copy %local0 : Maybe(Int)
    %v3 = call Try(Maybe(a), a, ()).toOutcome(Int), %v2 : Outcome(Int, ())
    %v4 = load %local2.discriminant : Int
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    alloc
    %v9 = call Try(Maybe(a), a, ()).fromExit(Int), %v8 : Maybe(Int)
    ret %v9

  b2:
    %v = load %local2@Proceed : Int
    %v12 = alloc : Maybe(Int)
    %v16 = add %v, 1 : Int
    aggregate %local5.discriminant = 1, %local5@Just = %v16
    ret %v12
}

fn twoSteps(%n: Int) -> Maybe(Int) {
  b0:
    %v1 = call parse, %n : Maybe(Int)
    %v2 = copy %local0 : Maybe(Int)
    %v3 = call Try(Maybe(a), a, ()).toOutcome(Int), %v2 : Outcome(Int, ())
    %v4 = load %local2.discriminant : Int
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    alloc
    %v9 = call Try(Maybe(a), a, ()).fromExit(Int), %v8 : Maybe(Int)
    ret %v9

  b2:
    %a = load %local2@Proceed : Int
    %v14 = sub %a, 10 : Int
    %v15 = call parse, %v14 : Maybe(Int)
    %v16 = copy %local5 : Maybe(Int)
    %v17 = call Try(Maybe(a), a, ()).toOutcome(Int), %v16 : Outcome(Int, ())
    %v18 = load %local7.discriminant : Int
    %v20 = cmp_eq %v18, 1 : Bool
    je %v20, b3, b4

  b3:
    alloc
    %v23 = call Try(Maybe(a), a, ()).fromExit(Int), %v22 : Maybe(Int)
    ret %v23

  b4:
    %b = load %local7@Proceed : Int
    %v26 = alloc : Maybe(Int)
    %v28 = add %a, %b : Int
    aggregate %local10.discriminant = 1, %local10@Just = %v28
    ret %v26
}

fn add(%a: Int, %b: Int) -> Int {
  b0:
    %v2 = add %a, %b : Int
    ret %v2
}

fn valuePosition(%x: Int, %y: Int) -> Maybe(Int) {
  b0:
    %v2 = alloc : Maybe(Int)
    %v4 = call parse, %x : Maybe(Int)
    %v5 = copy %local1 : Maybe(Int)
    %v6 = call Try(Maybe(a), a, ()).toOutcome(Int), %v5 : Outcome(Int, ())
    %v7 = load %local3.discriminant : Int
    %v9 = cmp_eq %v7, 1 : Bool
    je %v9, b1, b2

  b1:
    alloc
    %v12 = call Try(Maybe(a), a, ()).fromExit(Int), %v11 : Maybe(Int)
    ret %v12

  b2:
    %v14 = load %local3@Proceed : Int
    %v15 = call parse, %y : Maybe(Int)
    %v16 = copy %local6 : Maybe(Int)
    %v17 = call Try(Maybe(a), a, ()).toOutcome(Int), %v16 : Outcome(Int, ())
    %v18 = load %local8.discriminant : Int
    %v20 = cmp_eq %v18, 1 : Bool
    je %v20, b3, b4

  b3:
    alloc
    %v23 = call Try(Maybe(a), a, ()).fromExit(Int), %v22 : Maybe(Int)
    ret %v23

  b4:
    %v25 = load %local8@Proceed : Int
    %v26 = call add, %v14, %v25 : Int
    aggregate %local0.discriminant = 1, %local0@Just = %v26
    ret %v2
}

fn token(%n: Int) -> Maybe(Token) {
  b0:
    %v3 = cmp_gt %n, 0 : Bool
    je %v3, b1, b2

  b1:
    %v5 = alloc : Maybe(Token)
    %v7 = alloc : Token
    aggregate %local1@Token.id = %n
    aggregate %local0.discriminant = 1, %local0@Just = %v7
    ret %v5

  b2:
    jmp b3

  b3:
    %v12 = alloc : Maybe(Token)
    init %local2.discriminant, 0
    ret %v12
}

fn combine(%x: Token, %y: Token) -> Int {
  b0:
    %v2 = load %x@Token.id : Int
    drop %x via Drop(Token).drop
    %v3 = load %y@Token.id : Int
    drop %y via Drop(Token).drop
    %v4 = add %v2, %v3 : Int
    ret %v4
}

fn paired(%a: Int, %b: Int) -> Maybe(Int) {
  b0:
    %v2 = alloc : Maybe(Int)
    %v4 = call token, %a : Maybe(Token)
    %v5 = move %local1 : Maybe(Token)
    %v6 = call Try(Maybe(a), a, ()).toOutcome(Token), %v5 : Outcome(Token, ())
    %v7 = load %local2.discriminant : Int
    %v9 = cmp_eq %v7, 1 : Bool
    je %v9, b5, b2

  b1:
    alloc
    %v12 = call Try(Maybe(a), a, ()).fromExit(Int), %v11 : Maybe(Int)
    ret %v12

  b2:
    %v14 = load %local2@Proceed : Token
    %v15 = call token, %b : Maybe(Token)
    %v16 = move %local5 : Maybe(Token)
    %v17 = call Try(Maybe(a), a, ()).toOutcome(Token), %v16 : Outcome(Token, ())
    %v18 = load %local6.discriminant : Int
    %v20 = cmp_eq %v18, 1 : Bool
    je %v20, b6, b4

  b3:
    alloc
    %v23 = call Try(Maybe(a), a, ()).fromExit(Int), %v22 : Maybe(Int)
    ret %v23

  b4:
    %v25 = load %local6@Proceed : Token
    %v26 = move %local2@Proceed : Token
    %v27 = move %local6@Proceed : Token
    %v28 = call combine, %v26, %v27 : Int
    aggregate %local0.discriminant = 1, %local0@Just = %v28
    ret %v2

  b5:
    drop_derived %local2 via drop$Outcome(Token, ())
    jmp b1

  b6:
    drop_derived %local2 via drop$Outcome(Token, ())
    drop_derived %local6 via drop$Outcome(Token, ())
    jmp b3
}

fn operands(%x: Int, %y: Int) -> Maybe(Int) {
  b0:
    %v2 = alloc : Maybe(Int)
    %v4 = call parse, %x : Maybe(Int)
    %v5 = copy %local1 : Maybe(Int)
    %v6 = call Try(Maybe(a), a, ()).toOutcome(Int), %v5 : Outcome(Int, ())
    %v7 = load %local3.discriminant : Int
    %v9 = cmp_eq %v7, 1 : Bool
    je %v9, b1, b2

  b1:
    alloc
    %v12 = call Try(Maybe(a), a, ()).fromExit(Int), %v11 : Maybe(Int)
    ret %v12

  b2:
    %v14 = load %local3@Proceed : Int
    %v15 = call parse, %y : Maybe(Int)
    %v16 = copy %local6 : Maybe(Int)
    %v17 = call Try(Maybe(a), a, ()).toOutcome(Int), %v16 : Outcome(Int, ())
    %v18 = load %local8.discriminant : Int
    %v20 = cmp_eq %v18, 1 : Bool
    je %v20, b3, b4

  b3:
    alloc
    %v23 = call Try(Maybe(a), a, ()).fromExit(Int), %v22 : Maybe(Int)
    ret %v23

  b4:
    %v25 = load %local8@Proceed : Int
    %v26 = add %v14, %v25 : Int
    aggregate %local0.discriminant = 1, %local0@Just = %v26
    ret %v2
}

fn reasoned(%n: Int) -> Result(Int, Int) {
  b0:
    %v1 = call checked, %n : Result(Int, Int)
    %v2 = copy %local0 : Result(Int, Int)
    %v3 = call Try(Result(e, a), a, e).toOutcome(Int, Int), %v2 : Outcome(Int, Int)
    %v4 = load %local2.discriminant : Int
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    %v8 = load %local2@Exit : Int
    %v9 = call Try(Result(e, a), a, e).fromExit(Int, Int), %v8 : Result(Int, Int)
    ret %v9

  b2:
    %v = load %local2@Proceed : Int
    %v12 = alloc : Result(Int, Int)
    %v16 = add %v, 100 : Int
    aggregate %local4.discriminant = 1, %local4@Ok = %v16
    ret %v12
}

fn viaHelper(%n: Int) -> Result(Int, Bool) {
  b0:
    %v1 = call checked, %n : Result(Int, Int)
    %v2 = copy %local0 : Result(Int, Int)
    %v3 = call Try(Result(e, a), a, e).toOutcome(Int, Int), %v2 : Outcome(Int, Int)
    %v4 = load %local2.discriminant : Int
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    %v8 = load %local2@Exit : Int
    %v9 = call Try(Result(e, a), a, e).fromExit(Int, Bool), %v8 : Result(Int, Bool)
    ret %v9

  b2:
    %v = load %local2@Proceed : Int
    %v12 = alloc : Result(Int, Bool)
    %v16 = cmp_gt %v, 5 : Bool
    aggregate %local4.discriminant = 1, %local4@Ok = %v16
    ret %v12
}

fn conditional(%n: Int) -> Maybe(Int) {
  b0:
    %v1 = call parse, %n : Maybe(Int)
    %v2 = copy %local0 : Maybe(Int)
    %v3 = call Try(Maybe(a), a, ()).toOutcome(Int), %v2 : Outcome(Int, ())
    %v4 = load %local2.discriminant : Int
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    alloc
    %v9 = call Try(Maybe(a), a, ()).fromExit(Int), %v8 : Maybe(Int)
    ret %v9

  b2:
    %v11 = load %local2@Proceed : Int
    %v14 = cmp_gt %v11, 10 : Bool
    je %v14, b3, b4

  b3:
    %v16 = alloc : Maybe(Int)
    aggregate %local5.discriminant = 1, %local5@Just = 1
    ret %v16

  b4:
    jmp b5

  b5:
    %v22 = alloc : Maybe(Int)
    aggregate %local6.discriminant = 1, %local6@Just = 0
    ret %v22
}

fn looped(%n: Int) -> Maybe(Int) {
  b0:
    %total = alloc : Int
    init %total, 0
    %v9 = alloc : Int
    init %local1, 0
    jmp b1

  b1:
    %i = load %local1 : Int
    %v13 = cmp_lt %i, 3 : Bool
    je %v13, b2, b7

  b2:
    %v14 = load %total : Int
    %v15 = sub %n, %i : Int
    %v16 = call parse, %v15 : Maybe(Int)
    %v17 = copy %local2 : Maybe(Int)
    %v18 = call Try(Maybe(a), a, ()).toOutcome(Int), %v17 : Outcome(Int, ())
    %v19 = load %local4.discriminant : Int
    %v21 = cmp_eq %v19, 1 : Bool
    je %v21, b3, b4

  b3:
    alloc
    %v24 = call Try(Maybe(a), a, ()).fromExit(Int), %v23 : Maybe(Int)
    ret %v24

  b4:
    %v26 = load %local4@Proceed : Int
    %v27 = add %v14, %v26 : Int
    assign %total, %v27
    jmp b5

  b5:
    %v29 = load %local1 : Int
    %v30 = sub 3, %v29 : Int
    %v31 = cmp_le %v30, 1 : Bool
    je %v31, b7, b6

  b6:
    %v33 = add %v29, 1 : Int
    assign %local1, %v33
    jmp b1

  b7:
    %v38 = alloc : Maybe(Int)
    %v40 = load %total : Int
    aggregate %local7.discriminant = 1, %local7@Just = %v40
    ret %v38
}

fn doubled(%x: Int, %body: (value$: Int) -> $r) -> $r {
  schema {
    #0 type $r
  }

  b0:
    %v2 = load log : Int
    %v5 = add %v2, 1000 : Int
    assign log, %v5
    %v9 = mul %x, 2 : Int
    %v10 = calldyn %body, %v9 : $r
    %v11 = load log : Int
    %v14 = add %v11, 10000 : Int
    assign log, %v14
    ret %v10
}

fn throughLens(%n: Int) -> Maybe(Int) {
  b0:
    %v1 = alloc : (value$: Int) -> Maybe(Int)
    %v2 = symbol ResolveTest.continuation$14 : %()
    init %local0.code, %v2
    %v5 = cast 0 : %()
    init %local0.env, %v5
    %v7 = call doubled(Maybe(Int)), %n, %v1 : Maybe(Int)
    ret %v7
}

(Try(Maybe(a), a, ())) fn firstPositive(%n: Int, %body: (found: Int) -> a) -> Maybe(a) {
  schema {
    #0 type a
    #1 type Maybe(a)
    #2 class Try(Maybe(a), a, ())
  }

  b0:
    %v2 = call parse, %n : Maybe(Int)
    %v3 = copy %local1 : Maybe(Int)
    %v4 = call Try(Maybe(a), a, ()).toOutcome(Int), %v3 : Outcome(Int, ())
    %v5 = load %local3.discriminant : Int
    %v7 = cmp_eq %v5, 1 : Bool
    je %v7, b1, b2

  b1:
    alloc
    %v9 = gencall Try(Maybe(a), a, ()).fromExit, %v10 : Maybe(a)
    ret %v9

  b2:
    %v = load %local3@Proceed : Int
    %v13 = alloc : Maybe(a)
    %v15 = calldyn %body, %v : a
    aggregate %local6.discriminant = 1, %local6@Just = %v15
    ret %v13
}

fn viaSkipping(%n: Int) -> Int {
  b0:
    %v1 = alloc : (found: Int) -> Int
    %v2 = symbol ResolveTest.continuation$15 : %()
    init %local0.code, %v2
    %v5 = cast 0 : %()
    init %local0.env, %v5
    %v7 = call firstPositive(Int), %n, %v1 : Maybe(Int)
    %v8 = copy %local1 : Maybe(Int)
    %v9 = call Try(Maybe(a), a, ()).toOutcome(Int), %v8 : Outcome(Int, ())
    %v10 = load %local3.discriminant : Int
    %v12 = cmp_eq %v10, 1 : Bool
    je %v12, b1, b2

  b1:
    ret 0

  b2:
    %v16 = load %local3@Proceed : Int
    ret %v16
}

fn main() -> Int {
  b0:
    assign log, 0
    %a = call oneStep, 3 : Maybe(Int)
    %v6 = neg 1 : Int
    %b = call oneStep, %v6 : Maybe(Int)
    %c = call twoSteps, 11 : Maybe(Int)
    %d = call twoSteps, 3 : Maybe(Int)
    %e = call valuePosition, 1, 2 : Maybe(Int)
    %v18 = neg 2 : Int
    %f = call valuePosition, 1, %v18 : Maybe(Int)
    %g = call operands, 2, 3 : Maybe(Int)
    %h = call reasoned, 4 : Result(Int, Int)
    %i = call reasoned, 20 : Result(Int, Int)
    %v29 = neg 1 : Int
    %j = call viaHelper, %v29 : Result(Int, Bool)
    %k = call conditional, 6 : Maybe(Int)
    %l = call looped, 4 : Maybe(Int)
    %m = call looped, 1 : Maybe(Int)
    %n = call throughLens, 6 : Maybe(Int)
    %o = call viaSkipping, 5 : Int
    %v43 = neg 5 : Int
    %p = call viaSkipping, %v43 : Int
    assign dropped, 0
    %q = call paired, 3, 4 : Maybe(Int)
    %afterBoth = load dropped : Int
    assign dropped, 0
    %v56 = neg 1 : Int
    %r = call paired, 3, %v56 : Maybe(Int)
    %afterExit = load dropped : Int
    assign dropped, 0
    %v63 = neg 1 : Int
    %s = call paired, %v63, 4 : Maybe(Int)
    %afterEarly = load dropped : Int
    %v67 = call unwrap, %a : Int
    %v68 = call unwrap, %c : Int
    %v69 = add %v67, %v68 : Int
    %v70 = call unwrap, %e : Int
    %v71 = add %v69, %v70 : Int
    %v72 = call unwrap, %g : Int
    %v73 = add %v71, %v72 : Int
    %v74 = call unwrapOk, %h : Int
    %v75 = add %v73, %v74 : Int
    %v76 = call unwrap, %k : Int
    %v77 = add %v75, %v76 : Int
    %v78 = call unwrap, %l : Int
    %v79 = add %v77, %v78 : Int
    %v80 = call unwrap, %n : Int
    %v81 = add %v79, %v80 : Int
    %v82 = add %v81, %o : Int
    %v83 = add %v82, %p : Int
    %v84 = call unwrap, %q : Int
    %sum = add %v83, %v84 : Int
    %v86 = call empty, %b : Int
    %v87 = call empty, %d : Int
    %v88 = add %v86, %v87 : Int
    %v89 = call empty, %f : Int
    %v90 = add %v88, %v89 : Int
    %v91 = call empty, %m : Int
    %v92 = add %v90, %v91 : Int
    %v93 = call err, %i : Int
    %v94 = add %v92, %v93 : Int
    %v95 = call errBool, %j : Int
    %v96 = add %v94, %v95 : Int
    %v97 = call empty, %r : Int
    %v98 = add %v96, %v97 : Int
    %v99 = call empty, %s : Int
    %failures = add %v98, %v99 : Int
    %v103 = mul %failures, 1000 : Int
    %v104 = add %sum, %v103 : Int
    %v107 = mul %afterBoth, 100000 : Int
    %v108 = add %v104, %v107 : Int
    %v111 = mul %afterExit, 10000000 : Int
    %v112 = add %v108, %v111 : Int
    %v115 = mul %afterEarly, 1000000000 : Int
    %v116 = add %v112, %v115 : Int
    %v117 = load log : Int
    %v118 = add %v116, %v117 : Int
    ret %v118
}

fn unwrap(%m: Maybe(Int)) -> Int {
  b0:
    %v1 = load %m.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b1

  b1:
    jmp b3

  b2:
    %v = load %m@Just : Int
    jmp b3

  b3:
    %v9 = phi [b2, %v], [b1, 0] : Int
    ret %v9
}

fn unwrapOk(%r: Result(Int, Int)) -> Int {
  b0:
    %v1 = load %r.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b1

  b1:
    %v6 = load %r@Err : Int
    jmp b3

  b2:
    %v = load %r@Ok : Int
    jmp b3

  b3:
    %v10 = phi [b2, %v], [b1, 0] : Int
    ret %v10
}

fn empty(%m: Maybe(Int)) -> Int {
  b0:
    %v1 = load %m.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b1

  b1:
    jmp b3

  b2:
    %v5 = load %m@Just : Int
    jmp b3

  b3:
    %v10 = phi [b2, 0], [b1, 1] : Int
    ret %v10
}

fn err(%r: Result(Int, Int)) -> Int {
  b0:
    %v1 = load %r.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b1

  b1:
    %v7 = load %r@Err : Int
    jmp b3

  b2:
    %v5 = load %r@Ok : Int
    jmp b3

  b3:
    %v11 = phi [b2, 0], [b1, 1] : Int
    ret %v11
}

fn errBool(%r: Result(Int, Bool)) -> Int {
  b0:
    %v1 = load %r.discriminant : Int
    %v3 = cmp_eq %v1, 1 : Bool
    je %v3, b2, b1

  b1:
    %v7 = load %r@Err : Int
    jmp b3

  b2:
    %v5 = load %r@Ok : Bool
    jmp b3

  b3:
    %v11 = phi [b2, 0], [b1, 1] : Int
    ret %v11
}

fn Drop(Token).drop(%value: Token) -> () {
  b0:
    %v1 = load dropped : Int
    %v4 = add %v1, 1 : Int
    assign dropped, %v4
    ret
}

fn ResolveTest.continuation$14(%env: %{}, %value$: Int) -> Maybe(Int) {
  b0:
    %v4 = sub %value$, 8 : Int
    %v5 = call parse, %v4 : Maybe(Int)
    %v6 = copy %local0 : Maybe(Int)
    %v7 = call Try(Maybe(a), a, ()).toOutcome(Int), %v6 : Outcome(Int, ())
    %v8 = load %local2.discriminant : Int
    %v10 = cmp_eq %v8, 1 : Bool
    je %v10, b1, b2

  b1:
    alloc
    %v13 = call Try(Maybe(a), a, ()).fromExit(Int), %v12 : Maybe(Int)
    ret %v13

  b2:
    %v = load %local2@Proceed : Int
    %v15 = alloc : Maybe(Int)
    %v17 = add %v, %value$ : Int
    aggregate %local5.discriminant = 1, %local5@Just = %v17
    ret %v15
}

fn doubled(Maybe(Int))(%x: Int, %body: (value$: Int) -> Maybe(Int)) -> Maybe(Int) {
  b0:
    %v2 = load log : Int
    %v4 = add %v2, 1000 : Int
    assign log, %v4
    %v7 = mul %x, 2 : Int
    %v8 = calldyn %body, %v7 : Maybe(Int)
    %v9 = load log : Int
    %v11 = add %v9, 10000 : Int
    assign log, %v11
    ret %v8
}

fn ResolveTest.continuation$15(%env: %{}, %found: Int) -> Int {
  b0:
    %v4 = add %found, 1 : Int
    ret %v4
}

fn firstPositive(Int)(%n: Int, %body: (found: Int) -> Int) -> Maybe(Int) {
  b0:
    %v2 = call parse, %n : Maybe(Int)
    %v3 = copy %local1 : Maybe(Int)
    %v4 = copy %local2 : Maybe(Int)
    %v5 = call Try(Maybe(a), a, ()).toOutcome(Int), %v4 : Outcome(Int, ())
    %v6 = load %local3.discriminant : Int
    %v8 = cmp_eq %v6, 1 : Bool
    je %v8, b1, b2

  b1:
    alloc
    %v11 = call Try(Maybe(a), a, ()).fromExit(Int), %v10 : Maybe(Int)
    ret %v11

  b2:
    %v = load %local3@Proceed : Int
    %v14 = alloc : Maybe(Int)
    %v15 = calldyn %body, %v : Int
    aggregate %local6.discriminant = 1, %local6@Just = %v15
    ret %v14
}

fn drop$Outcome(Token, ())(%value: Outcome(Token, ())) -> () {
  b0:
    %v1 = load %value.discriminant : Int
    %v3 = cmp_eq %v1, 0 : Bool
    je %v3, b2, b3

  b1:
    ret

  b2:
    drop %value@Proceed via Drop(Token).drop
    jmp b1

  b3:
    jmp b1
}
