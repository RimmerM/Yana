"use strict";
// ResolveTest
function score(f) {
  return f & 1 ? 10 : 20;
}

function mutate(f$o, f$k, f$s) {
  f$o[f$k] = $b1u$set(f$o[f$k], f$s, 1);
  return 0;
}

function main() {
  var v2 = (((score(1) + 1 | 0) + score(1) | 0) + 8 | 0) + (score(2) + 100 | 0) | 0;
  var v26 = { $v: 0 };
  var zero = mutate(v26, "$v", 1);
  return v2 + ((v26.$v & 1 ? 1000 : 2000) + zero | 0) | 0;
}

// bit ranges of a word wider than the host's operators - see codegen/js/place.cpp
function $b1u$set(w, s, v) {
  return w + ((v & 1) - (Math.floor(w / s) & 1)) * s;
}
main();
