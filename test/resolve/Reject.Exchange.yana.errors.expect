22:4: error: a `&` argument must name storage that can be written back to
27:14: error: a `&` argument must name mutable storage - declare it with `let &`, or with `let &->` where the binding also has to take its value out of a place that keeps a name for it
28:11: error: unknown scalar value old
28:4: error: non-unit function must return a value
14:4: error: this use conflicts with a mutable borrow of the same storage, which is exclusive while it is live
14:4: message: the borrow it conflicts with is here
52:17: error: two exclusive borrows of elements of one container are live at once, and nothing here says the two subscripts differ - `swap` and `exchange` are the operations that need no such proof
52:10: message: the borrow it conflicts with is here
56:17: error: two exclusive borrows of elements of one container are live at once, and nothing here says the two subscripts differ - `swap` and `exchange` are the operations that need no such proof
56:10: message: the borrow it conflicts with is here
61:25: error: two exclusive borrows of elements of one container are live at once, and nothing here says the two subscripts differ - `swap` and `exchange` are the operations that need no such proof
61:10: message: the borrow it conflicts with is here
68:17: error: two exclusive borrows of elements of one container are live at once, and nothing here says the two subscripts differ - `swap` and `exchange` are the operations that need no such proof
68:10: message: the borrow it conflicts with is here
