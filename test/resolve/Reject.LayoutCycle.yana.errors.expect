5:4: error: Slot and Cell contain each other and are declared in different files, so which of them holds the pointer would be decided by the order their file names sort in - write `@box` on the field that should be the indirection
