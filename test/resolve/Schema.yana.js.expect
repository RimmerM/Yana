"use strict";
// ResolveTest
function double(n) {
  return Math.imul(n, 2) | 0;
}

function main() {
  return 53;
}

function identity_Int(value) {
  return value;
}

function scale_Int(x, y) {
  return (Math.imul(x, y) | 0) + x | 0;
}

function smallest_Int(x, y, z) {
  return x < y ? x < z ? x : z : y < z ? y : z;
}

function bothEqual_Int_Bool(p, q, r, s) {
  var v5 = p === q;
  return v5 ? r === s : v5;
}

function pairUp_Int_Bool(x, y) {
  return { first: x, second: y };
}

function tagOf_Labelled(value$tag, value$weight) {
  return 7;
}

function doubled_Labelled(value$tag, value$weight) {
  return 8;
}
