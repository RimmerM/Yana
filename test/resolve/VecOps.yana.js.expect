"use strict";
// ResolveTest
function reductions() {
  var sevens$2 = 7;
  var sevens$3 = 7;
  if (Math.min(Math.min(7, 7), Math.min(7, 7)) !== 7) {
    return -4;
  } else {
    if (Math.max(Math.max(9, 7), Math.max(sevens$2, sevens$3)) !== 9) {
      return -5;
    } else {
      if (Math.min(Math.min(7, 2), Math.min(sevens$2, sevens$3)) !== 2) {
        return -6;
      } else {
        return 0;
      }
    }
  }
}

function floats() {
  var a$0 = 1.5;
  if (Math.fround(a$0 * 2) !== 3) {
    return -1;
  } else {
    if (Math.fround(1.5 + 2) !== 3.5) {
      return -2;
    } else {
      if (Math.fround(-a$0) !== -1.5) {
        return -3;
      } else {
        return 0;
      }
    }
  }
}

function main() {
  if (reductions() !== 0) {
    return reductions();
  } else {
    if (floats() !== 0) {
      return Math.imul(floats(), 10);
    } else {
      return 2042;
    }
  }
}
main();
