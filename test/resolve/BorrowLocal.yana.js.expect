"use strict";
// ResolveTest
function addTo(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
  return;
}

function accumulate(n) {
  return Math.imul(n, 2);
}

function shared(n) {
  return n + 1 | 0;
}

function main() {
  return 16;
}
