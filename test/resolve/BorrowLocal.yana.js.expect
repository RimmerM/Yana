"use strict";
// ResolveTest
function addTo(target, amount) {
  target.$v = target.$v + amount | 0;
  return;
}

function accumulate(n) {
  return Math.imul(n, 2) | 0;
}

function shared(n) {
  return n + 1 | 0;
}

function main() {
  return 16;
}
