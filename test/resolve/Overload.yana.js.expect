"use strict";
// ResolveTest
function and(a$bits, b$bits) {
  return { bits: a$bits & b$bits | 0 };
}

function mask(a, b) {
  return a & b | 0;
}

function combined(a, b) {
  return a & b | 0;
}

function bothTrue(a, b) {
  return a & b;
}

function maskQualified(a, b) {
  return a & b | 0;
}

function bothQualified(a, b) {
  return a & b;
}

function negated(a) {
  return -a | 0;
}

function difference(a, b) {
  return a - b | 0;
}

function main() {
  return 1023;
}

function maskGeneric_Int(x, y) {
  return x & y | 0;
}

function bothGeneric_Bool(x, y) {
  return x & y;
}
