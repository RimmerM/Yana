"use strict";
const typeDesc$Handle = [teardown$none, moveInit$Handle, copyInit$Handle];
const genEnv$relocate_Handle = [typeDesc$Handle];
const typeDesc$Pair = [teardown$none, moveInit$Pair, copyInit$Pair];
const genEnv$relocate_Pair = [typeDesc$Pair];

// Core
function teardown$none(value) {
}

// ResolveTest
function relocate(genEnv, x) {
  var y = {};
  genEnv[0][1](y, x);
  return y;
}

function main() {
  var moved = relocate(genEnv$relocate_Handle, { $v: 10n }).$v;
  if (moved !== 10n) {
    return -1n;
  } else {
    var p = { left: 20n, right: 5n };
    var movedPair = relocate(genEnv$relocate_Pair, { left: p.left, right: p.right });
    var v22 = movedPair.left;
    if (v22 !== 20n) {
      return -2n;
    } else {
      var v30 = movedPair.right;
      if (v30 !== 5n) {
        return -3n;
      } else {
        return BigInt.asIntN(64, BigInt.asIntN(64, moved + v22) + v30);
      }
    }
  }
}

function moveInit$Handle(to, from) {
  to.$v = from.$v;
}

function copyInit$Handle(to, from) {
  to.$v = from.$v;
}

function moveInit$Pair(to, from) {
  to.left = from.left;
  to.right = from.right;
}

function copyInit$Pair(to, from) {
  to.left = from.left;
  to.right = from.right;
}
main();
