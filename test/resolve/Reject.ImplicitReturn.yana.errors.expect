5:44: error: fact is recursive, so the `=` form cannot infer its result type from its body - write it out, as in `-> Int`
10:45: error: isEven is recursive, so the `=` form cannot infer its result type from its body - write it out, as in `-> Int`
14:0: warning: `bump` is written with `=` but its body produces no value, so it returns `()` - use the `:` block form for a function that runs statements rather than producing a result
