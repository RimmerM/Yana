"use strict";
// ResolveTest
function flags() {
  var score = 0;
  if (Eq_Flags_eq_eq(((true ? 1 : 0) | (false ? 1 : 0) << 1) >>> 0, 1)) {
    score = score + 256 | 0;
  }
  if (Eq_Flags_eq_eq(3, 3)) {
    score = score + 512 | 0;
  }
  if (Eq_Flags_eq_eq(2, 2)) {
    score = score + 1024 | 0;
  }
  return score;
}

function main() {
  return flags() + 6399 | 0;
}

function Eq_Flags_eq_eq(lhs, rhs) {
  var v11;
  var v4 = (lhs & 1) !== 0 === ((rhs & 1) !== 0);
  if (v4) {
    v11 = (lhs >>> 1 & 1) !== 0 === ((rhs >>> 1 & 1) !== 0);
  } else {
    v11 = v4;
  }
  return v11;
}
main();
