"use strict";
const typeDesc$Person = [8, 8, 1155, moveInit$Person, copyInit$Person, teardown$none, teardown$none];
const property$Person_name = [read$Person_name, set$Person_name];
const genEnv$nameOf_Person = [typeDesc$Person, property$Person_name];
const typeDesc$Item = [8, 8, 1155, moveInit$Item, copyInit$Item, teardown$none, teardown$none];
const property$Item_name = [read$Item_name, set$Item_name];
const genEnv$nameOf_Item = [typeDesc$Item, property$Item_name];
const genEnv$rename_Person = [typeDesc$Person, property$Person_name];
const typeDesc$Left = [8, 8, 1155, moveInit$Left, copyInit$Left, teardown$none, teardown$none];
const property$Left_name = [read$Left_name, set$Left_name];
const property$Left_tag = [read$Left_tag, set$Left_tag];
const genEnv$labelled_Left = [typeDesc$Left, property$Left_name, property$Left_tag];
const typeDesc$Right = [8, 8, 1155, moveInit$Right, copyInit$Right, teardown$none, teardown$none];
const property$Right_name = [read$Right_name, set$Right_name];
const property$Right_tag = [read$Right_tag, set$Right_tag];
const genEnv$labelled_Right = [typeDesc$Right, property$Right_name, property$Right_tag];

// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function nameOf(genEnv, p) {
  var v1 = { $v: 0 };
  genEnv[1][0](p, v1);
  return v1.$v;
}

function rename(genEnv, p, to) {
  genEnv[1][1](p, { $v: to });
  return;
}

function labelled(genEnv, p) {
  var v4 = Math.imul(nameOf([genEnv[0], genEnv[1]], p), 100);
  var v5 = { $v: 0 };
  genEnv[2][0](p, v5);
  return v4 + v5.$v | 0;
}

function main() {
  var people = nameOf(genEnv$nameOf_Person, { name: 5, age: 90 });
  var items = nameOf(genEnv$nameOf_Item, { name: 2, cost: 7 });
  var v10 = { name: 1, age: 2 };
  rename(genEnv$rename_Person, v10, 8);
  var left = labelled(genEnv$labelled_Left, { name: 1, tag: 3 });
  var right = labelled(genEnv$labelled_Right, { tag: 4, name: 6 });
  return (((Math.imul(people, 1000) + Math.imul(items, 100) | 0) + Math.imul(nameOf(genEnv$nameOf_Person, v10), 10) | 0) + left | 0) + right | 0;
}

function moveInit$Person(to, from) {
  to.name = from.name;
  to.age = from.age;
  return;
}

function copyInit$Person(to, from) {
  to.name = from.name;
  to.age = from.age;
  return;
}

function read$Person_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Person_name(owner, value) {
  owner.name = value.$v;
  return;
}

function moveInit$Item(to, from) {
  to.name = from.name;
  to.cost = from.cost;
  return;
}

function copyInit$Item(to, from) {
  to.name = from.name;
  to.cost = from.cost;
  return;
}

function read$Item_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Item_name(owner, value) {
  owner.name = value.$v;
  return;
}

function moveInit$Left(to, from) {
  to.name = from.name;
  to.tag = from.tag;
  return;
}

function copyInit$Left(to, from) {
  to.name = from.name;
  to.tag = from.tag;
  return;
}

function read$Left_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Left_name(owner, value) {
  owner.name = value.$v;
  return;
}

function read$Left_tag(owner, out) {
  out.$v = owner.tag;
  return;
}

function set$Left_tag(owner, value) {
  owner.tag = value.$v;
  return;
}

function moveInit$Right(to, from) {
  to.tag = from.tag;
  to.name = from.name;
  return;
}

function copyInit$Right(to, from) {
  to.tag = from.tag;
  to.name = from.name;
  return;
}

function read$Right_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Right_name(owner, value) {
  owner.name = value.$v;
  return;
}

function read$Right_tag(owner, out) {
  out.$v = owner.tag;
  return;
}

function set$Right_tag(owner, value) {
  owner.tag = value.$v;
  return;
}
main();
