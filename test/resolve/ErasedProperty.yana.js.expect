"use strict";
const typeDesc$Person = [38508, 8, 4, 8, 131, moveInit$Person, teardown$none, teardown$none];
const property$Person_name = [typeDesc$Person, null, read$Person_name, set$Person_name];
const typeDesc$Int = [64, 4, 4, 4, 131, moveInit$Int, teardown$none, teardown$none];
const genEnv$nameOf_Person = [0, typeDesc$Person, property$Person_name];
const typeDesc$Item = [38796, 8, 4, 8, 131, moveInit$Item, teardown$none, teardown$none];
const property$Item_name = [typeDesc$Item, typeDesc$Int, read$Item_name, set$Item_name];
const genEnv$nameOf_Item = [0, typeDesc$Item, property$Item_name];
const genEnv$rename_Person = [0, typeDesc$Person, property$Person_name];
const typeDesc$Left = [39084, 8, 4, 8, 131, moveInit$Left, teardown$none, teardown$none];
const property$Left_name = [typeDesc$Left, typeDesc$Int, read$Left_name, set$Left_name];
const property$Left_tag = [typeDesc$Left, typeDesc$Int, read$Left_tag, set$Left_tag];
const genEnv$labelled_Left = [0, typeDesc$Left, property$Left_name, property$Left_tag];
const typeDesc$Right = [39372, 8, 4, 8, 131, moveInit$Right, teardown$none, teardown$none];
const property$Right_name = [typeDesc$Right, typeDesc$Int, read$Right_name, set$Right_name];
const property$Right_tag = [typeDesc$Right, typeDesc$Int, read$Right_tag, set$Right_tag];
const genEnv$labelled_Right = [0, typeDesc$Right, property$Right_name, property$Right_tag];
property$Person_name[1] = typeDesc$Int;

// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function nameOf(genEnv, p) {
  var v1 = { $v: 0 };
  genEnv[2][2](p, v1);
  return v1.$v;
}

function rename(genEnv, p, to) {
  genEnv[2][3](p, { $v: to });
  return;
}

function labelled(genEnv, p) {
  var v4 = Math.imul(nameOf([0, genEnv[1], genEnv[2]], p), 100) | 0;
  var v5 = { $v: 0 };
  genEnv[3][2](p, v5);
  return v4 + v5.$v | 0;
}

function main() {
  var people = nameOf(genEnv$nameOf_Person, { name: 5, age: 90 });
  var items = nameOf(genEnv$nameOf_Item, { name: 2, cost: 7 });
  var v12 = { name: 1, age: 2 };
  rename(genEnv$rename_Person, v12, 8);
  var left = labelled(genEnv$labelled_Left, { name: 1, tag: 3 });
  var right = labelled(genEnv$labelled_Right, { tag: 4, name: 6 });
  return ((((Math.imul(people, 1000) | 0) + (Math.imul(items, 100) | 0) | 0) + (Math.imul(nameOf(genEnv$nameOf_Person, v12), 10) | 0) | 0) + left | 0) + right | 0;
}

function moveInit$Person(to, from) {
  to.name = from.name;
  to.age = from.age;
  return;
}

function moveInit$Int(to, from) {
  to.$v = from.$v;
  return;
}

function read$Person_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Person_name(owner, value) {
  owner.name = value.$v;
  return;
}

function moveInit$Item(to, from) {
  to.name = from.name;
  to.cost = from.cost;
  return;
}

function read$Item_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Item_name(owner, value) {
  owner.name = value.$v;
  return;
}

function moveInit$Left(to, from) {
  to.name = from.name;
  to.tag = from.tag;
  return;
}

function read$Left_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Left_name(owner, value) {
  owner.name = value.$v;
  return;
}

function read$Left_tag(owner, out) {
  out.$v = owner.tag;
  return;
}

function set$Left_tag(owner, value) {
  owner.tag = value.$v;
  return;
}

function moveInit$Right(to, from) {
  to.tag = from.tag;
  to.name = from.name;
  return;
}

function read$Right_name(owner, out) {
  out.$v = owner.name;
  return;
}

function set$Right_name(owner, value) {
  owner.name = value.$v;
  return;
}

function read$Right_tag(owner, out) {
  out.$v = owner.tag;
  return;
}

function set$Right_tag(owner, value) {
  owner.tag = value.$v;
  return;
}
