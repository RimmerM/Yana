21:0: error: simple is not a convention a declaration may name - `simple` and `complex` are the compiler's own choice between two internal shapes, and `system` describes what a syscall leaves alone rather than how anything is entered. Write `clobber`, `sysv` or `win64`
24:0: error: system is not a convention a declaration may name - `simple` and `complex` are the compiler's own choice between two internal shapes, and `system` describes what a syscall leaves alone rather than how anything is entered. Write `clobber`, `sysv` or `win64`
28:0: error: unknown calling convention fastcall - the ones a declaration may name are `clobber`, `sysv` and `win64`
32:0: error: `@convention` takes one argument - the name of a calling convention, as in `@convention(clobber)`
35:0: error: `@convention` takes one argument - the name of a calling convention, as in `@convention(clobber)`
40:0: error: this declaration already names a calling convention
53:0: error: `@x86_legacy_sse` takes no arguments - it says how this function's vector instructions are encoded, and nothing else. The vector state is cleared by calling `X86.vzeroupper()`
