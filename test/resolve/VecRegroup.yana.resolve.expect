let image$base: () = <none>

let &sink: Vec(I32) = <startup>

fn regrouping(%v: Vec(I32)) -> Vec(I16) {
  b0:
    %v1 = bitcast %v : Vec(I16)
    ret %v1
}

fn bytes(%v: Vec(I16)) -> Vec(U8) {
  b0:
    %v1 = bitcast %v : Vec(U8)
    ret %v1
}

fn back(%v: Vec(U8)) -> Vec(I32) {
  b0:
    %v1 = bitcast %v : Vec(I32)
    ret %v1
}

fn main() -> I32 {
  b0:
    %v0 = load sink : Vec(I32)
    %v1 = call regrouping, %v0 : Vec(I16)
    %v2 = call bytes, %v1 : Vec(U8)
    %v3 = call back, %v2 : Vec(I32)
    assign sink, %v3
    ret 1
}

fn main$() -> I32 {
  b0:
    %v3 = vsplat 0 : Vec(I32)
    init sink, %v3
    %v5 = call main : I32
    ret %v5
}
