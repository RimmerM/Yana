"use strict";
var log = 0;

// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function sometimesLeaves(x) {
  var v1 = { $v: x };
  log = log + 1 | 0;
  var v14 = ResolveTest_continuation$1(v1);
  log = log + 100 | 0;
  return v14;
}

function insideArm(x) {
  B0: {
    if (x > 0) {
      var v12 = scope_Outcome_Int(ResolveTest_continuation$2, { $v: x });
      if (!(v12 === null)) {
        return v12;
      }
    }
  }
  return 7;
}

function leavesWithValue(x) {
  log = log + 1 | 0;
  var v15 = ResolveTest_continuation$3(null, Math.imul(x, 2));
  log = log + 100 | 0;
  return v15;
}

function main() {
  log = 101;
  var b = sometimesLeaves(5);
  var c = sometimesLeaves(200);
  var d = insideArm(3);
  var e = insideArm(-1);
  var f = leavesWithValue(9);
  var g = leavesWithValue(2);
  var v40 = log + 202 | 0;
  log = v40;
  return (((((((b + 2 | 0) + c | 0) + d | 0) + e | 0) + f | 0) + g | 0) + 7 | 0) + v40 | 0;
}

function ResolveTest_continuation$1(env) {
  var v1 = env.$v;
  if (v1 > 100) {
    return 0;
  } else {
    return v1 + 2 | 0;
  }
}

function ResolveTest_continuation$2(env) {
  if (env.$v > 100) {
    return 0;
  } else {
    log = log + 1000 | 0;
    return null;
  }
}

function scope_Outcome_Int(body$c, body$e) {
  log = log + 1 | 0;
  var v5 = body$c(body$e);
  log = log + 100 | 0;
  return v5;
}

function ResolveTest_continuation$3(env, value) {
  if (value > 10) {
    return value;
  } else {
    return 0;
  }
}
main();
