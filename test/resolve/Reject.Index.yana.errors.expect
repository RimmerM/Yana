13:33: error: cannot index Point - it has no instance of `Index`, so it says nothing about what a key or an element of it is
15:32: error: cannot index Int - it has no instance of `Index`, so it says nothing about what a key or an element of it is
25:34: error: get needs to know what Index determines here, and this function does not require it - declare the constraint, as `fn (Index(...)) undeclared(...)`
30:4: error: a `&` argument must name mutable storage - declare it with `let &`, or with `let &->` where the binding also has to take its value out of a place that keeps a name for it
34:4: error: a range of an array cannot be assigned to - `xs[a..b]` produces a slice, which is a value naming someone else's storage
36:39: error: a subscript takes exactly one index or range
