"use strict";
// ResolveTest
function payloadOf(s) {
  var v14;
  if (s.$tag === 0) {
    v14 = s.$p | 0;
  } else {
    v14 = (s.$p | 0) + 1000 | 0;
  }
  return v14;
}

function colourOf(c) {
  var v27;
  if (c.$tag === 0) {
    v27 = ((c.r | 0) + Math.imul(c.g | 0, 10) | 0) + Math.imul(c.b | 0, 100) | 0;
  } else {
    v27 = Math.imul(c.$p | 0, 1000);
  }
  return v27;
}

function fiveOf(f) {
  var v44;
  var v1 = f.$tag;
  if (v1 === 0) {
    v44 = f.$p | 0;
  } else {
    if (v1 === 1) {
      v44 = Math.imul(f.$p | 0, 10);
    } else {
      if (v1 === 2) {
        v44 = Math.imul(f.$p | 0, 100);
      } else {
        if (v1 === 3) {
          v44 = Math.imul(f.$p | 0, 1000);
        } else {
          v44 = Math.imul(f.$p | 0, 10000);
        }
      }
    }
  }
  return v44;
}

function flagsValue(f) {
  return (f & 1) + (f >>> 1 & 1 ? 2 : 0);
}

function taggedOf(t) {
  var v14;
  if (t.$tag === 0) {
    v14 = flagsValue(t.$p);
  } else {
    v14 = flagsValue(t.$p) + 100 | 0;
  }
  return v14;
}

function widthOf(w) {
  var v14;
  if (w.$tag === 0) {
    v14 = w.$p | 0;
  } else {
    v14 = Math.imul(w.$p | 0, 10);
  }
  return v14;
}

function bump(x$o, x$k, x$s) {
  x$o[x$k] = x$o[x$k] + 1 & 255;
  return;
}

function mutated() {
  var v0 = { $tag: 0, $p: 41 };
  bump(v0, "$p", 1);
  return payloadOf(v0);
}

function maybeOf(m) {
  var v10;
  if (m === null) {
    v10 = 500;
  } else {
    v10 = payloadOf(m);
  }
  return v10;
}

function main() {
  return (((((((payloadOf({ $tag: 0, $p: 7 }) + payloadOf({ $tag: 1, $p: 9 }) | 0) + (colourOf({ $tag: 0, r: 1, g: 2, b: 3, $p: 0 }) + colourOf({ $tag: 1, r: 0, g: 0, b: 0, $p: 4 }) | 0) | 0) + (fiveOf({ $tag: 0, $p: 1 }) + fiveOf({ $tag: 4, $p: 1 }) | 0) | 0) + (taggedOf({ $tag: 0, $p: 3 }) + taggedOf({ $tag: 1, $p: 2 }) | 0) | 0) + mutated() | 0) + taggedOf({ $tag: 1, $p: 1 }) | 0) + ((maybeOf(null) + maybeOf({ $tag: 0, $p: 2 }) | 0) + maybeOf({ $tag: 1, $p: 3 }) | 0) | 0) + (widthOf({ $tag: 0, $p: 5 }) + widthOf({ $tag: 1, $p: 6 }) | 0) | 0;
}
main();
