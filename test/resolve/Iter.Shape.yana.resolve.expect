let image$base: () = <none>

table closure$ResolveTest.continuation$14 before ResolveTest.continuation$14 {
  .0 teardown$none
  .1 teardown$none
}

table closure$ResolveTest.continuation$15 before ResolveTest.continuation$15 {
  .0 teardown$none
  .1 teardown$none
}

fn teardown$none(%value: %()) -> () {
  b0:
    ret
}

fn stream(%b: Box, %times: Int, %body: (value$: Int) -> Outcome((), $r)) -> Outcome((), $r) {
  schema {
    #0 type $r
    #1 type Outcome((), $r)
  }

  b0:
    %v3 = load %b@Box.v : Int
    %v4 = add %v3, %times : Int
    %v5 = calldyn %body, %v4 : Outcome((), $r)
    %v6 = load %local2.discriminant : Int
    %v8 = cmp_eq %v6, 1 : Bool
    je %v8, b1, b2

  b1:
    ret %v5

  b2:
    %v11 = alloc : Outcome((), $r)
    init %local3.discriminant, 0
    ret %v11
}

fn main() -> Int {
  b0:
    %total = alloc : Int
    init %total, 0
    %v3 = alloc : Box
    aggregate %local1@Box.v = 1
    %v8 = alloc : {total: &mut Int}
    %v9 = borrow_mut %total : &mut Int
    init %local2.total, %v9
    %v11 = addressof %local2 : %()
    %v12 = alloc : (value$: Int) -> Outcome((), ())
    %v13 = symbol ResolveTest.continuation$14 : %()
    init %local3.code, %v13
    init %local3.env, %v11
    %v16 = call stream(()), %v3, 2, %v12 : Outcome((), ())
    %v17 = alloc : Bag
    aggregate %local5@Bag.w = 40
    %v22 = alloc : {total: &mut Int}
    %v23 = borrow_mut %total : &mut Int
    init %local6.total, %v23
    %v25 = addressof %local6 : %()
    %v26 = alloc : (value$: Int) -> Outcome((), ())
    %v27 = symbol ResolveTest.continuation$15 : %()
    init %local7.code, %v27
    init %local7.env, %v25
    %v30 = cast 5 : I64
    %v31 = call Streamed(Bag).stream(()), %v17, %v30, %v26 : Outcome((), ())
    %v32 = load %total : Int
    ret %v32
}

fn Streamed(Bag).stream(%self: Bag, %times: I64, %body: (value$: Int) -> Outcome((), $r)) -> Outcome((), $r) {
  schema {
    #0 type $r
    #1 type Outcome((), $r)
  }

  b0:
    %v3 = load %self@Bag.w : Int
    %v4 = calldyn %body, %v3 : Outcome((), $r)
    %v5 = load %local2.discriminant : Int
    %v7 = cmp_eq %v5, 1 : Bool
    je %v7, b1, b2

  b1:
    ret %v4

  b2:
    %v10 = alloc : Outcome((), $r)
    init %local3.discriminant, 0
    ret %v10
}

fn ResolveTest.continuation$14(%env: %{total: &mut Int}, %value$: Int) -> Outcome((), ()) {
  b0:
    %v2 = load [%env].total : &mut Int
    %v3 = load [%env].total : &mut Int
    %v4 = load [%v3] : Int
    %v5 = add %v4, %value$ : Int
    assign [%v2], %v5
    %v7 = alloc : Outcome((), ())
    init %local0.discriminant, 0
    ret %v7
}

fn stream(())(%b: Box, %times: Int, %body: (value$: Int) -> Outcome((), ())) -> Outcome((), ()) {
  b0:
    %v3 = load %b@Box.v : Int
    %v4 = add %v3, %times : Int
    %v5 = calldyn %body, %v4 : Outcome((), ())
    %v6 = load %local2.discriminant : Int
    %v8 = cmp_eq %v6, 1 : Bool
    je %v8, b1, b2

  b1:
    ret %v5

  b2:
    %v11 = alloc : Outcome((), ())
    init %local3.discriminant, 0
    ret %v11
}

fn ResolveTest.continuation$15(%env: %{total: &mut Int}, %value$: Int) -> Outcome((), ()) {
  b0:
    %v2 = load [%env].total : &mut Int
    %v3 = load [%env].total : &mut Int
    %v4 = load [%v3] : Int
    %v5 = add %v4, %value$ : Int
    assign [%v2], %v5
    %v7 = alloc : Outcome((), ())
    init %local0.discriminant, 0
    ret %v7
}

fn Streamed(Bag).stream(())(%self: Bag, %times: I64, %body: (value$: Int) -> Outcome((), ())) -> Outcome((), ()) {
  b0:
    %v3 = load %self@Bag.w : Int
    %v4 = calldyn %body, %v3 : Outcome((), ())
    %v5 = load %local2.discriminant : Int
    %v7 = cmp_eq %v5, 1 : Bool
    je %v7, b1, b2

  b1:
    ret %v4

  b2:
    %v10 = alloc : Outcome((), ())
    init %local3.discriminant, 0
    ret %v10
}
