"use strict";
var seed = 2;

// ResolveTest
function oneSource() {
  return 0;
}

function twoSources() {
  return 0;
}

function byteLane() {
  return 0;
}

function floatLane() {
  return 0;
}

function runtime(v) {
  var backwards$0 = v[3];
  var backwards$2 = v[1];
  var backwards$3 = v[0];
  var mixed$0 = v[0];
  if (backwards$0 !== v[3]) {
    return -1;
  } else {
    var v41 = v[0];
    if (mixed$0 !== v41) {
      return -2;
    } else {
      if (backwards$2 !== v[1]) {
        return -3;
      } else {
        if (backwards$3 !== v41) {
          return -4;
        } else {
          return 0;
        }
      }
    }
  }
}

function main() {
  var v9 = seed >>> 0;
  var v$0 = 0 + v9;
  var v$1 = 1 + v9 >>> 0;
  var v$2 = 2 + v9 >>> 0;
  var v$3 = 3 + v9 >>> 0;
  if (oneSource() !== 0) {
    return oneSource();
  } else {
    if (twoSources() !== 0) {
      return Math.imul(twoSources(), 10);
    } else {
      if (byteLane() !== 0) {
        return Math.imul(byteLane(), 100);
      } else {
        if (floatLane() !== 0) {
          return Math.imul(floatLane(), 1000);
        } else {
          if (runtime([v$0, v$1, v$2, v$3]) !== 0) {
            return Math.imul(runtime([v$0, v$1, v$2, v$3]), 10000);
          } else {
            return 314;
          }
        }
      }
    }
  }
}
main();
