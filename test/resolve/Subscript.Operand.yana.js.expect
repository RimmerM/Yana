"use strict";
// Core
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

// ResolveTest
function total(xs$items, xs$length, xs$offset) {
  var sum = 0;
  var i = 0;
  var v21 = xs$length >>> 0;
  L0: for (;;) {
    if (i < xs$length) {
      var v18 = xs$offset + i | 0;
      checkCondition(i >>> 0 >= v21 ? 1 : 0);
      sum = sum + xs$items[v18] | 0;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  return sum;
}

function main() {
  var v6 = new Int32Array([1, 2, 3, 4, 5]);
  var v21 = new Int32Array([2, 0, 1]);
  if ((total(v6, 2, 1) + 1 | 0) !== 6) {
    return -1;
  } else {
    var v269 = v6[2];
    if ((v269 + v269 | 0) !== 6) {
      return -2;
    } else {
      var v283 = v21[0];
      checkCondition(v283 >>> 0 >= 5 ? 1 : 0);
      if (v6[v283] !== 3) {
        return -3;
      } else {
        if (total(v6, 5, 0) !== 15) {
          return -4;
        } else {
          if (total(v6, 5, 0) !== 15) {
            return -5;
          } else {
            v6[0] = 10;
            return total(v6, 5, 0);
          }
        }
      }
    }
  }
}
main();
