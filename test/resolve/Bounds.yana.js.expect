"use strict";
// Core
function growJsArray_I32(self, wanted) {
  var v3 = self.items;
  var room = v3.length;
  var v5 = room + room | 0;
  var wide = v5;
  if (v5 < wanted) {
    wide = wanted;
  }
  if (wide < 4) {
    wide = 4;
  }
  var v25 = $grow(v3, wide);
  self.items = v25;
}

// ResolveTest
function main() {
  var v17 = { items: new Int32Array([10, 20, 30]), length: 3 };
  growJsArray_I32(v17, 4);
  v17.items[3] = 40;
  var v36 = v17.items;
  v36.copyWithin(3, 4, 4);
  v17.length = 3;
  return Math.imul(v36[0] + v36[2] | 0, 1000) + 1002 | 0;
}

// a typed array's growth - see Implementation-Containers.md, section 14
function $grow(a, n) {
  const b = new a.constructor(n);
  b.set(a);
  return b;
}
main();
