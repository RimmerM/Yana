27:9: error: cannot bind a shared window with `let &` - it names storage somebody else owns, and `&` here would only make this frame's copy of the descriptor writable. Take the window from `elementsMut`, or from a `&` borrow of the container
28:4: error: a `&` argument must name mutable storage - declare it with `let &`, or with `let &->` where the binding also has to take its value out of a place that keeps a name for it
33:9: error: cannot bind a shared window with `let &` - it names storage somebody else owns, and `&` here would only make this frame's copy of the descriptor writable. Take the window from `elementsMut`, or from a `&` borrow of the container
34:4: error: a `&` argument must name mutable storage - declare it with `let &`, or with `let &->` where the binding also has to take its value out of a place that keeps a name for it
