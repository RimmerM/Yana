fn widen(%value: Int) -> Long {
  b0:
    %v1 = cast %value : Long
    ret %v1
}

fn longLiteral() -> Long {
  b0:
    ret 9
}

fn floatLiteral() -> Float {
  b0:
    ret 1.5
}

fn doubleLiteral() -> Double {
  b0:
    ret 1.5
}

fn floatProduct(%a: Float, %b: Float) -> Float {
  b0:
    %v2 = mul %a, %b : Float
    ret %v2
}

fn doubleSum(%a: Double, %b: Double) -> Double {
  b0:
    %v2 = add %a, %b : Double
    ret %v2
}

fn both(%a: Bool, %b: Bool) -> Bool {
  b0:
    %v2 = and %a, %b : Bool
    ret %v2
}

fn main() -> Int {
  b0:
    %v2 = call both, True, False : Bool
    je %v2, b1, b2

  b1:
    jmp b3

  b2:
    %v6 = call widen, 7 : Long
    %v7 = cast %v6 : Int
    jmp b3

  b3:
    %v10 = phi [b1, 0], [b2, %v7] : Int
    ret %v10
}
