let image$base: () = <none>

fn add(%a: I32, %b: I32) -> I32 {
  b0:
    %v2 = add %a, %b : I32
    ret %v2
}

fn noop() -> () {
  b0:
    ret
}

fn local(%value: I32) -> I32 {
  b0:
    %next = add %value, 1 : I32
    %v6 = mul %next, 2 : I32
    ret %v6
}

fn choose(%a: I32, %b: I32) -> I32 {
  b0:
    %v2 = cmp_gt %a, %b : Bool
    je %v2, b1, b2

  b1:
    %v5 = call add, %a, 1 : I32
    jmp b3

  b2:
    jmp b3

  b3:
    %v8 = phi [b1, %v5], [b2, %b] : I32
    ret %v8
}

fn classify(%value: I32) -> I32 {
  b0:
    %v3 = cmp_lt %value, 0 : Bool
    je %v3, b1, b2

  b1:
    jmp b5

  b2:
    %v8 = cmp_eq %value, 0 : Bool
    je %v8, b3, b4

  b3:
    jmp b5

  b4:
    jmp b5

  b5:
    %v15 = phi [b1, -1], [b3, 0], [b4, 1] : I32
    ret %v15
}

fn main() -> I32 {
  b0:
    call noop
    %value = call choose, 2, 1 : I32
    jmp b1

  b1:
    je False, b3, b2

  b2:
    %v11 = mul %value, 2 : I32
    %v13 = call classify, 5 : I32
    %v14 = add %v11, %v13 : I32
    %v16 = call local, 0 : I32
    %v17 = add %v14, %v16 : I32
    %v20 = sub %v17, 2 : I32
    ret %v20

  b3:
    ret 99
}
