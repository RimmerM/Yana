"use strict";
var calls = 0;

// ResolveTest
function larger(a, b) {
  return a > b ? a : b;
}

function sign(value) {
  return value < 0 ? -1 : value === 0 ? 0 : 1;
}

function either(a, b) {
  var v4 = a > 0;
  return (v4 ? v4 : b > 0) ? 1 : 0;
}

function count() {
  calls = calls + 1 | 0;
  return 1;
}

function skipped(a) {
  var v8;
  var v3 = a > 0;
  if (v3) {
    calls = calls + 1 | 0;
    v8 = 1;
  } else {
    v8 = v3;
  }
  return v8 ? 1 : 0;
}

function effects() {
  calls = 1;
  return 6;
}

function same(c) {
  return 7;
}

function lowered() {
  return -3;
}

function magnitude(value) {
  return value > 0 ? value : Math.fround(0 - value);
}

function positive() {
  var v0 = lowered();
  return (v0 > 0 ? v0 : Math.fround(0 - v0)) > 2.5 ? 5 : 0;
}

function smallest(limit) {
  var best = 100;
  var i = 0;
  L0: for (;;) {
    if (i < limit) {
      var candidate = 40 - (Math.imul(i, 3) | 0) | 0;
      best = candidate < best ? candidate : best;
      i = i + 1 | 0;
      continue L0;
    }
    break L0;
  }
  return best;
}

function zero() {
  return 0;
}

function ratio(value, divisor) {
  var v10;
  if (divisor !== 0) {
    v10 = value / divisor | 0;
  } else {
    v10 = 0;
  }
  return v10;
}

function doubled(value) {
  return Math.imul(value, 2) | 0;
}

function guarded(value) {
  var v9;
  if (value > 0) {
    v9 = doubled(value);
  } else {
    v9 = 0;
  }
  return v9;
}

function main() {
  var v208;
  var v36 = ((Math.imul(either(0, 4), 16) | 0) + 14 | 0) + (Math.imul(either(0, 0), 32) | 0) | 0;
  calls = 1;
  var v47 = v36 + 1280 | 0;
  var v136 = lowered();
  var v58 = (v47 + (Math.imul((v136 > 0 ? v136 : Math.fround(0 - v136)) > 2.5 ? 5 : 0, 256) | 0) | 0) + (Math.imul(smallest(5), 512) | 0) | 0;
  var v60 = zero();
  if (v60 !== 0) {
    v208 = 10 / v60 | 0;
  } else {
    v208 = 0;
  }
  return ((v58 + (Math.imul(v208, 1024) | 0) | 0) + 10240 | 0) + (Math.imul(doubled(3), 4096) | 0) | 0;
}
