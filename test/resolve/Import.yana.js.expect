"use strict";
// ResolveTest
function main() {
  return (styleValue(0, 1) + styleValue(1, 3) | 0) + 51 | 0;
}

// Shapes
function styleValue(it$filled, it$weight) {
  return (it$filled ? 10 : 0) + it$weight | 0;
}
main();
