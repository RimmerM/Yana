19:42: error: implicit conversion from U32 to Size would lose precision
19:42: message: Size is the width the *target* picks, so what the language guarantees is 32 bits and signed - 31 of them for a positive value, and U32 needs 32. `USize` is the same word read unsigned and holds every U32 on every target; `truncate(x) :: Size` is the low bits where a signed index really is what was wanted
19:32: error: implicit conversion from U32 to Size would lose precision
19:32: message: Size is the width the *target* picks, so what the language guarantees is 32 bits and signed - 31 of them for a positive value, and U32 needs 32. `USize` is the same word read unsigned and holds every U32 on every target; `truncate(x) :: Size` is the low bits where a signed index really is what was wanted
22:30: error: cannot ascribe U32 to Size: the conversion loses precision. `::` may widen but not narrow - write `truncate(x)` for the low bits, or `bitcast(x)` to reinterpret them
22:30: message: Size is the width the *target* picks, so what the language guarantees is 32 bits and signed - 31 of them for a positive value, and U32 needs 32. `USize` is the same word read unsigned and holds every U32 on every target; `truncate(x) :: Size` is the low bits where a signed index really is what was wanted
22:0: error: implicit conversion from U32 to Size would lose precision
22:0: message: Size is the width the *target* picks, so what the language guarantees is 32 bits and signed - 31 of them for a positive value, and U32 needs 32. `USize` is the same word read unsigned and holds every U32 on every target; `truncate(x) :: Size` is the low bits where a signed index really is what was wanted
26:27: error: cannot ascribe U64 to Size: the conversion loses precision. `::` may widen but not narrow - write `truncate(x)` for the low bits, or `bitcast(x)` to reinterpret them
26:0: error: implicit conversion from U64 to Size would lose precision
