"use strict";
// ResolveTest
function outOfRange() {
  return Math.trunc(1E30) | 0;
}

function main() {
  return (outOfRange() === outOfRange() ? 40 : 0) + 60 + (1 / 0 > 1000000 ? 20 : 0);
}
main();
