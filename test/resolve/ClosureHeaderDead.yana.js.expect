"use strict";
var dropCalls = 0;
const closure$ResolveTest_lambda$1 = [dropAt$_g_Guard, teardown$none];

// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function consume(f$c, f$e) {
  var v2 = f$c(f$e, 1);
  drop$_Int_sub_gt_Int(f$c, f$e);
  return v2;
}

function inFrame(id) {
  var v12 = ResolveTest_lambda$0({ $v: id }, 10);
  dropCalls = dropCalls + 1 | 0;
  return v12;
}

function handedOff(id) {
  var v11 = { $c: ResolveTest_lambda$1, $e: { $v: id } };
  return consume(v11.$c, v11.$e);
}

function main() {
  return (inFrame(3) + handedOff(5) | 0) + dropCalls | 0;
}

function ResolveTest_lambda$0(env, x) {
  return x + env.$v | 0;
}

function ResolveTest_lambda$1(env, x) {
  return x + env.$v | 0;
}
ResolveTest_lambda$1.$h = closure$ResolveTest_lambda$1;

function dropAt$_g_Guard(value) {
  dropCalls = dropCalls + 1 | 0;
  return;
}

function drop$_Int_sub_gt_Int(value$c, value$e) {
  if (value$c.$h != null) {
    value$c.$h[0](value$e);
  }
  return;
}
main();
