"use strict";
var log = 0;

// ResolveTest
function note(n) {
  var v5 = Math.imul(log, 10) + n | 0;
  log = v5;
  if (v5 > 1000000000) {
    return note(0);
  } else {
    return n;
  }
}

function main() {
  var first = note(1);
  var v2 = ((Math.imul(note(2), 10) + 9 | 0) + first | 0) + (Math.imul(note(3) + 1 | 0, 10) + 8 | 0) | 0;
  var v29 = new Int32Array([0, 7]);
  var first$1 = note(4);
  var v40 = note(5);
  v29[0] = v40;
  return ((v2 + ((Math.imul(v40, 10) + v29[1] | 0) + first$1 | 0) | 0) + 56 | 0) + log | 0;
}
main();
