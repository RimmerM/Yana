fn identity(%value: a) -> a {
  schema {
    #0 type a
  }

  b0:
    ret %value
}

(TrivialCopy(a), TrivialCopy(b)) fn swap(%value: Pair(a, b)) -> Pair(b, a) {
  schema {
    #0 type a
    #1 type b
    #2 type Pair(b, a)
    #3 class TrivialCopy(a)
    #4 class TrivialCopy(b)
  }

  b0:
    %v1 = alloc : Pair(b, a)
    %v2 = load %value@Pair.second : b
    %v3 = load %value@Pair.first : a
    init %local1@Pair.first, %v2
    init %local1@Pair.second, %v3
    ret %v1
}

(TrivialCopy(a)) fn leftOf(%value: Pair(a, b)) -> a {
  schema {
    #0 type a
    #1 type b
    #2 class TrivialCopy(a)
  }

  b0:
    %v1 = load %value@Pair.first : a
    ret %v1
}

(Ord(a)) fn smaller(%x: a, %y: a) -> a {
  schema {
    #0 type a
    #1 class Ord(a)
  }

  b0:
    %v2 = gencall Ord(a).<, %x, %y : Bool
    je %v2, b1, b2

  b1:
    jmp b3

  b2:
    jmp b3

  b3:
    %v6 = phi [b1, %x], [b2, %y] : a
    ret %v6
}

(Num(a)) fn twice(%x: a) -> a {
  schema {
    #0 type a
    #1 class Num(a)
  }

  b0:
    %v1 = gencall Num(a).+, %x, %x : a
    ret %v1
}

(Ord(a)) fn middle(%x: a, %y: a, %z: a) -> a {
  schema {
    #0 type a
    #1 class Ord(a)
  }

  b0:
    %v3 = gencall smaller(a), %x, %y : a
    %v4 = gencall smaller(a), %v3, %z : a
    ret %v4
}

fn orElse(%value: Maybe(a), %fallback: a) -> a {
  schema {
    #0 type a
  }

  b0:
    %v2 = load %value.discriminant : Int
    %v4 = cmp_eq %v2, 1 : Bool
    je %v4, b2, b1

  b1:
    jmp b3

  b2:
    %v6 = load %value@Just : a
    %v7 = move %value@Just : a
    %inner = alloc : a
    init %local2, %v7
    jmp b3

  b3:
    %v12 = phi [b2, %inner], [b1, %fallback] : a
    ret %v12
}

fn wrap(%value: a) -> Maybe(a) {
  schema {
    #0 type a
    #1 type Maybe(a)
  }

  b0:
    %v1 = alloc : Maybe(a)
    init %local1.discriminant, 1
    init %local1@Just, %value
    ret %v1
}

fn empty() -> Maybe(a) {
  schema {
    #0 type a
    #1 type Maybe(a)
  }

  b0:
    %v0 = alloc : Maybe(a)
    init %local0.discriminant, 0
    ret %v0
}

fn main() -> Int {
  b0:
    %v2 = call identity(Int), 1 : Int
    %v6 = alloc : Pair(Bool, Int)
    init %local0@Pair.first, True
    init %local0@Pair.second, 20
    %v9 = call swap(Bool, Int), %v6 : Pair(Int, Bool)
    %v10 = call leftOf(Int, Bool), %v9 : Int
    %v11 = add %v2, %v10 : Int
    %v14 = call twice(Int), 3 : Int
    %v15 = add %v11, %v14 : Int
    %v20 = call smaller(Int), 9, 4 : Int
    %v21 = add %v15, %v20 : Int
    %v28 = call middle(Int), 30, 8, 12 : Int
    %v29 = add %v21, %v28 : Int
    %v32 = call wrap(Int), 5 : Maybe(Int)
    %v35 = copy %local2 : Maybe(Int)
    %v36 = call orElse(Int), %v35, 0 : Int
    %v37 = add %v29, %v36 : Int
    %v38 = call empty(Int) : Maybe(Int)
    %v41 = copy %local4 : Maybe(Int)
    %v42 = call orElse(Int), %v41, 7 : Int
    %v43 = add %v37, %v42 : Int
    ret %v43
}

fn identity(Int)(%value: Int) -> Int {
  b0:
    ret %value
}

fn swap(Bool, Int)(%value: Pair(Bool, Int)) -> Pair(Int, Bool) {
  b0:
    %v1 = alloc : Pair(Int, Bool)
    %v2 = load %value@Pair.second : Int
    %v3 = load %value@Pair.first : Bool
    init %local1@Pair.first, %v2
    init %local1@Pair.second, %v3
    ret %v1
}

fn leftOf(Int, Bool)(%value: Pair(Int, Bool)) -> Int {
  b0:
    %v1 = load %value@Pair.first : Int
    ret %v1
}

fn twice(Int)(%x: Int) -> Int {
  b0:
    %v1 = add %x, %x : Int
    ret %v1
}

fn smaller(Int)(%x: Int, %y: Int) -> Int {
  b0:
    %v3 = cmp_lt %x, %y : Bool
    je %v3, b1, b2

  b1:
    jmp b3

  b2:
    jmp b3

  b3:
    %v2 = phi [b1, %x], [b2, %y] : Int
    ret %v2
}

fn middle(Int)(%x: Int, %y: Int, %z: Int) -> Int {
  b0:
    %v3 = call smaller(Int), %x, %y : Int
    %v4 = call smaller(Int), %v3, %z : Int
    ret %v4
}

fn wrap(Int)(%value: Int) -> Maybe(Int) {
  b0:
    %v1 = alloc : Maybe(Int)
    init %local1.discriminant, 1
    init %local1@Just, %value
    ret %v1
}

fn orElse(Int)(%value: Maybe(Int), %fallback: Int) -> Int {
  b0:
    %v3 = load %value.discriminant : Int
    %v5 = cmp_eq %v3, 1 : Bool
    je %v5, b2, b1

  b1:
    jmp b3

  b2:
    %v8 = load %value@Just : Int
    %v9 = move %value@Just : Int
    %inner = alloc : Int
    init %local2, %v9
    %inner = load %local2 : Int
    jmp b3

  b3:
    %v2 = phi [b2, %inner], [b1, %fallback] : Int
    ret %v2
}

fn empty(Int)() -> Maybe(Int) {
  b0:
    %v0 = alloc : Maybe(Int)
    init %local0.discriminant, 0
    ret %v0
}
