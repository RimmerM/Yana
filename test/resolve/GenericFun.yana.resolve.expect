let image$base: () = <none>

fn identity(%value: a) -> a {
  schema {
    #0 type a
  }

  b0:
    ret %value
}

(TrivialCopy(a), TrivialCopy(b)) fn swap(%value: Pair(a, b)) -> Pair(b, a) {
  schema {
    #0 type a
    #1 type b
    #2 type Pair(b, a)
    #3 class TrivialCopy(a)
    #4 class TrivialCopy(b)
  }

  b0:
    %v1 = alloc : Pair(b, a)
    %v2 = load %value@Pair.second : b
    %v3 = load %value@Pair.first : a
    aggregate %local1@Pair.first = %v2, %local1@Pair.second = %v3
    ret %v1
}

(TrivialCopy(a)) fn leftOf(%value: Pair(a, b)) -> a {
  schema {
    #0 type a
    #1 type b
    #2 class TrivialCopy(a)
  }

  b0:
    %v1 = load %value@Pair.first : a
    ret %v1
}

(Ord(a)) fn smaller(%x: a, %y: a) -> a {
  schema {
    #0 type a
    #1 class Ord(a)
  }

  b0:
    %v2 = gencall Ord(a).<, %x, %y : Bool
    je %v2, b1, b2

  b1:
    jmp b3

  b2:
    jmp b3

  b3:
    %v6 = phi [b1, %x], [b2, %y] : a
    ret %v6
}

(Num(a)) fn twice(%x: a) -> a {
  schema {
    #0 type a
    #1 class Num(a)
  }

  b0:
    %v1 = gencall Num(a).+, %x, %x : a
    ret %v1
}

(Ord(a)) fn middle(%x: a, %y: a, %z: a) -> a {
  schema {
    #0 type a
    #1 class Ord(a)
  }

  b0:
    %v3 = gencall smaller(a), %x, %y : a
    %v4 = gencall smaller(a), %v3, %z : a
    ret %v4
}

fn orElse(%value: Maybe(a), %fallback: a) -> a {
  schema {
    #0 type a
  }

  b0:
    %v2 = load %value.discriminant : I32
    %v4 = cmp_eq %v2, 1 : Bool
    je %v4, b2, b1

  b1:
    jmp b3

  b2:
    %v6 = load %value@Just : a
    %v7 = move %value@Just : a
    %inner = alloc : a
    init %local2, %v7
    jmp b3

  b3:
    %v12 = phi [b2, %inner], [b1, %fallback] : a
    ret %v12
}

fn wrap(%value: a) -> Maybe(a) {
  schema {
    #0 type a
    #1 type Maybe(a)
  }

  b0:
    %v1 = alloc : Maybe(a)
    aggregate %local1.discriminant = 1, %local1@Just = %value
    ret %v1
}

fn empty() -> Maybe(a) {
  schema {
    #0 type a
    #1 type Maybe(a)
  }

  b0:
    %v0 = alloc : Maybe(a)
    init %local0.discriminant, 0
    ret %v0
}

fn main() -> I32 {
  b0:
    %v2 = call identity(I32), 1 : I32
    %v6 = alloc : Pair(Bool, I32)
    init %local0@Pair.first, True
    init %local0@Pair.second, 20
    %v9 = call swap(Bool, I32), %v6 : Pair(I32, Bool)
    %v10 = call leftOf(I32, Bool), %v9 : I32
    %v11 = add %v2, %v10 : I32
    %v14 = call twice(I32), 3 : I32
    %v15 = add %v11, %v14 : I32
    %v20 = call smaller(I32), 9, 4 : I32
    %v21 = add %v15, %v20 : I32
    %v28 = call middle(I32), 30, 8, 12 : I32
    %v29 = add %v21, %v28 : I32
    %v32 = call wrap(I32), 5 : Maybe(I32)
    %v35 = copy %local2 : Maybe(I32)
    %v36 = call orElse(I32), %v35, 0 : I32
    %v37 = add %v29, %v36 : I32
    %v38 = call empty(I32) : Maybe(I32)
    %v41 = copy %local4 : Maybe(I32)
    %v42 = call orElse(I32), %v41, 7 : I32
    %v43 = add %v37, %v42 : I32
    ret %v43
}

fn identity(I32)(%value: I32) -> I32 {
  b0:
    ret %value
}

fn swap(Bool, I32)(%value: Pair(Bool, I32)) -> Pair(I32, Bool) {
  b0:
    %v1 = alloc : Pair(I32, Bool)
    %v2 = load %value@Pair.second : I32
    %v3 = load %value@Pair.first : Bool
    aggregate %local1@Pair.first = %v2, %local1@Pair.second = %v3
    ret %v1
}

fn leftOf(I32, Bool)(%value: Pair(I32, Bool)) -> I32 {
  b0:
    %v1 = load %value@Pair.first : I32
    ret %v1
}

fn twice(I32)(%x: I32) -> I32 {
  b0:
    %v1 = add %x, %x : I32
    ret %v1
}

fn smaller(I32)(%x: I32, %y: I32) -> I32 {
  b0:
    %v3 = cmp_lt %x, %y : Bool
    je %v3, b1, b2

  b1:
    jmp b3

  b2:
    jmp b3

  b3:
    %v2 = phi [b1, %x], [b2, %y] : I32
    ret %v2
}

fn middle(I32)(%x: I32, %y: I32, %z: I32) -> I32 {
  b0:
    %v3 = call smaller(I32), %x, %y : I32
    %v4 = call smaller(I32), %v3, %z : I32
    ret %v4
}

fn wrap(I32)(%value: I32) -> Maybe(I32) {
  b0:
    %v1 = alloc : Maybe(I32)
    aggregate %local1.discriminant = 1, %local1@Just = %value
    ret %v1
}

fn orElse(I32)(%value: Maybe(I32), %fallback: I32) -> I32 {
  b0:
    %v3 = load %value.discriminant : I32
    %v5 = cmp_eq %v3, 1 : Bool
    je %v5, b2, b1

  b1:
    jmp b3

  b2:
    %v8 = load %value@Just : I32
    %v9 = move %value@Just : I32
    %inner = alloc : I32
    init %local2, %v9
    %inner = load %local2 : I32
    jmp b3

  b3:
    %v2 = phi [b2, %inner], [b1, %fallback] : I32
    ret %v2
}

fn empty(I32)() -> Maybe(I32) {
  b0:
    %v0 = alloc : Maybe(I32)
    init %local0.discriminant, 0
    ret %v0
}
