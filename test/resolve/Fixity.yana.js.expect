"use strict";
var counter = { $v: 0 };

// Core
function add_eq_Int(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
}

// ResolveTest
function main() {
  return ((ResolveTest_deferred$16(null) + 219 | 0) + ((ResolveTest_deferred$20(null) + 1 | 0) + counter.$v | 0) | 0) + 1 | 0;
}

function ResolveTest_deferred$16(env) {
  return ResolveTest_deferred$17(null);
}

function ResolveTest_deferred$17(env) {
  return 3;
}

function ResolveTest_deferred$20(env) {
  add_eq_Int(counter, "$v", 1, 1);
  return ResolveTest_deferred$21(null);
}

function ResolveTest_deferred$21(env) {
  add_eq_Int(counter, "$v", 1, 1);
  return 20;
}
main();
