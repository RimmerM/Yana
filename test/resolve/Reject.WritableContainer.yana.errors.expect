25:4: error: ReadOnly(I32) is `Contiguous` and not `Writable`, so it can be passed where `[I32]` is read but not where one is written - `elements` promises a buffer address to read and says nothing about writing through it. Declare `instance Writable(ReadOnly(I32), I32)` with `elementsMut`, or take the parameter without `&`
29:4: error: no instance of Writable for (ReadOnly(I32)), required by sort - the class is what would decide the rest of this call's type arguments, so there is nothing left to infer them from
