17:8: error: this pattern always matches, so the `|` after the initializer can never run - if this was meant as a bitwise or, write ``a `or` b``; after a `let` initializer `|` introduces the alternatives that only a pattern which can *fail* needs
22:8: error: this pattern always matches, so the `|` after the initializer can never run - if this was meant as a bitwise or, write ``a `or` b``; after a `let` initializer `|` introduces the alternatives that only a pattern which can *fail* needs
26:28: error: an alternative of a declaration has to leave the block - return, break, or continue
