let image$base: () = <none>

table closure$ResolveTest.continuation$22 before ResolveTest.continuation$22 {
  .0 teardown$none
}

table closure$ResolveTest.continuation$23 before ResolveTest.continuation$23 {
  .0 teardown$none
}

fn teardown$none(%value: %()) -> () {
  b0:
    ret
}

fn walk(%b: Box, %body: (value$: I32) -> Outcome((), $r)) -> Outcome((), $r) {
  schema {
    #0 type $r
    #1 type Outcome((), $r)
  }

  b0:
    %v2 = load %b@Box.v : I32
    %v3 = calldyn %body, %v2 : Outcome((), $r)
    %v4 = load %local2.discriminant : I32
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    ret %v3

  b2:
    %v9 = alloc : Outcome((), $r)
    init %local3.discriminant, 0
    ret %v9
}

fn main() -> I32 {
  b0:
    %total = alloc : I32
    init %total, 0
    %v3 = alloc : Box
    aggregate %local1@Box.v = 7
    %v6 = alloc : {total: &I32}
    %v7 = borrow_mut %total : &I32
    init %local2.total, %v7
    %v9 = addressof %local2 : %()
    %v10 = alloc : (value$: I32) -> Outcome((), ())
    %v11 = symbol ResolveTest.continuation$22 : %()
    init %local3.code, %v11
    init %local3.env, %v9
    %v14 = call walk(()), %v3, %v10 : Outcome((), ())
    %v15 = alloc : Bag
    aggregate %local5@Bag.w = 30
    %v18 = alloc : {total: &I32}
    %v19 = borrow_mut %total : &I32
    init %local6.total, %v19
    %v21 = addressof %local6 : %()
    %v22 = alloc : (value$: I32) -> Outcome((), ())
    %v23 = symbol ResolveTest.continuation$23 : %()
    init %local7.code, %v23
    init %local7.env, %v21
    %v26 = call Streamed(Bag).walk(()), %v15, %v22 : Outcome((), ())
    %v27 = load %total : I32
    ret %v27
}

fn Streamed(Bag).walk(%self: Bag, %body: (value$: I32) -> Outcome((), $r)) -> Outcome((), $r) {
  schema {
    #0 type $r
    #1 type Outcome((), $r)
  }

  b0:
    %v2 = load %self@Bag.w : I32
    %v3 = calldyn %body, %v2 : Outcome((), $r)
    %v4 = load %local2.discriminant : I32
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    ret %v3

  b2:
    %v9 = alloc : Outcome((), $r)
    init %local3.discriminant, 0
    ret %v9
}

fn ResolveTest.continuation$22(%env: %{total: &I32}, %value$: I32) -> Outcome((), ()) {
  b0:
    %v2 = load [%env].total : &I32
    %v3 = load [%env].total : &I32
    %v4 = load [%v3] : I32
    %v5 = add %v4, %value$ : I32
    assign [%v2], %v5
    %v7 = alloc : Outcome((), ())
    init %local0.discriminant, 0
    ret %v7
}

fn walk(())(%b: Box, %body: (value$: I32) -> Outcome((), ())) -> Outcome((), ()) {
  b0:
    %v2 = load %b@Box.v : I32
    %v3 = calldyn %body, %v2 : Outcome((), ())
    %v4 = load %local2.discriminant : I32
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    ret %v3

  b2:
    %v9 = alloc : Outcome((), ())
    init %local3.discriminant, 0
    ret %v9
}

fn ResolveTest.continuation$23(%env: %{total: &I32}, %value$: I32) -> Outcome((), ()) {
  b0:
    %v2 = load [%env].total : &I32
    %v3 = load [%env].total : &I32
    %v4 = load [%v3] : I32
    %v5 = add %v4, %value$ : I32
    assign [%v2], %v5
    %v7 = alloc : Outcome((), ())
    init %local0.discriminant, 0
    ret %v7
}

fn Streamed(Bag).walk(())(%self: Bag, %body: (value$: I32) -> Outcome((), ())) -> Outcome((), ()) {
  b0:
    %v2 = load %self@Bag.w : I32
    %v3 = calldyn %body, %v2 : Outcome((), ())
    %v4 = load %local2.discriminant : I32
    %v6 = cmp_eq %v4, 1 : Bool
    je %v6, b1, b2

  b1:
    ret %v3

  b2:
    %v9 = alloc : Outcome((), ())
    init %local3.discriminant, 0
    ret %v9
}
