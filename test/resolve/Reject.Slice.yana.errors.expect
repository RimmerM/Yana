15:0: error: a borrow returned from this function is rooted in storage the caller does not own - it must come from an argument, and the argument's type must say which loan it hands back
24:26: error: this borrow of an array outlives the frame that owns it - a slice is a view into the container's storage, and the container is released when this function returns
29:20: error: a borrow returned from this function is rooted in xs, which the signature did not put in a loan group - write a leading `'` on that parameter's type, so that the caller is told the result is rooted in it
34:44: error: this closure outlives the frame that built it, so it cannot capture xs - it is a slice, which is a view into a container someone else owns, and copying the descriptor copies the address inside it (Design-Memory §8)
46:18: error: a borrow returned from this function is rooted in xs, which the signature did not put in a loan group - write a leading `'` on that parameter's type, so that the caller is told the result is rooted in it
