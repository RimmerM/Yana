"use strict";
// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function applyUnit(f$c, f$e) {
  f$c(f$e);
  return;
}

function main() {
  var total = { $v: 0 };
  ResolveTest_lambda$0({ $v: { $o: total, $k: "$v", $s: 1 } });
  ResolveTest_lambda$1({ $v: { $o: total, $k: "$v", $s: 1 } });
  var seen = total.$v;
  ResolveTest_lambda$2({ $v: { $o: total, $k: "$v", $s: 1 } });
  return total.$v + seen | 0;
}

function ResolveTest_lambda$0(env) {
  var v1 = env.$v;
  v1.$o[v1.$k] = v1.$o[v1.$k] + 1 | 0;
  return;
}

function ResolveTest_lambda$1(env) {
  var v1 = env.$v;
  v1.$o[v1.$k] = Math.imul(v1.$o[v1.$k], 10);
  return;
}

function ResolveTest_lambda$2(env) {
  var v1 = env.$v;
  v1.$o[v1.$k] = v1.$o[v1.$k] + 5 | 0;
  return;
}
