"use strict";
// ResolveTest
function readOnly() {
  return 25;
}

function writable() {
  return 8;
}

function passedWhole() {
  return distance(3, 4);
}

function distance(it$x, it$y) {
  return it$x + it$y | 0;
}

function main() {
  return (readOnly() + writable() | 0) + passedWhole() | 0;
}
