11:13: error: Point is not a lane type - a vector holds machine lanes, which are the integer types of 8, 16, 32 and 64 bits and the two floats
13:16: error: Array(Int) is not a lane type - a vector holds machine lanes, which are the integer types of 8, 16, 32 and 64 bits and the two floats
15:11: error: String is not a lane type - a vector holds machine lanes, which are the integer types of 8, 16, 32 and 64 bits and the two floats
19:15: error: Bool is not a lane type - a vector holds machine lanes, which are the integer types of 8, 16, 32 and 64 bits and the two floats
23:12: error: a vector has a power-of-two lane count between 2 and 64, and this one asks for 3 - one lane is a scalar and is spelled as one
25:10: error: a vector has a power-of-two lane count between 2 and 64, and this one asks for 1 - one lane is a scalar and is spelled as one
27:15: error: a vector has a power-of-two lane count between 2 and 64, and this one asks for 128 - one lane is a scalar and is spelled as one
30:11: error: a vector may be at most 64 bytes wide, and 64 lanes of I64 is 512
34:26: error: this parameter is a const parameter, so it takes an integer literal or a const parameter and not Int
40:19: error: a number is not a type - a number is a type argument only where the declaration declared a const parameter there
46:16: error: a vector has a power-of-two lane count between 2 and 64, and this one asks for 3 - one lane is a scalar and is spelled as one
50:10: error: `return` on Vec(Float) has nothing to root a borrow in - it arrives in a register, so the body sees a copy of its own; write `return &` when the caller's storage must be the root
