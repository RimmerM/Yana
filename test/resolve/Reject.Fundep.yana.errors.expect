22:16: error: expected the parameters determined by the ones before the `->`
26:22: error: a class head may write only one `->`: the parameters before it determine every one after it
17:0: error: this instance of Elem disagrees with an existing one about what (I32) determines - the class head's `->` promises one answer, so the two cannot both hold
32:12: error: head needs to know what Elem determines here, and this function does not require it - declare the constraint, as `fn (Elem(...)) undeclared(...)`
37:36: error: no instance of Elem for (I32, I32), required by head
