"use strict";
// ResolveTest
function incLong() {
  return inc_Long(40n);
}

function defaulted() {
  return 9;
}

function mixedLiterals() {
  return 3.5;
}

function oneMetre() {
  return { value: 1 };
}

function measure(distance$value) {
  return distance$value;
}

function isFive(distance$value) {
  return distance$value === 5;
}

function main() {
  var v34;
  var v2 = inc_Int(10);
  var v5 = inc_Float(1.5);
  var v22 = (v2 + (Math.trunc(v5) | 0) | 0) + 19 | 0;
  if (1) {
    v34 = 1;
  } else {
    v34 = 0;
  }
  return (v22 + v34 | 0) + (Number(inc_Long(40n)) | 0) | 0;
}

function FromInt_Metres_fromInt(value) {
  return { value: Number(value) | 0 };
}

function Eq_Metres_eq_eq(lhs$value, rhs$value) {
  return lhs$value === rhs$value;
}

function Eq_Metres_not_eq(lhs$value, rhs$value) {
  return lhs$value !== rhs$value;
}

function inc_Long(x) {
  return BigInt.asIntN(64, x + 1n);
}

function inc_Int(x) {
  return x + 1 | 0;
}

function inc_Float(x) {
  return Math.fround(x + 1);
}

function two_Int() {
  return 2;
}
