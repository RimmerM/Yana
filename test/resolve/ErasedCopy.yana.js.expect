"use strict";
var copyCalls = 0;
const typeDesc$Int = [4, 4, 1155, moveInit$Int, copyInit$Int, teardown$none, teardown$none];
const witness$TrivialCopy_Int = [];
const genEnv$copyInto_Int = [typeDesc$Int, witness$TrivialCopy_Int];
const typeDesc$Point = [8, 8, 1155, moveInit$Point, copyInit$Point, teardown$none, teardown$none];
const witness$TrivialCopy_Point = [];
const genEnv$copyInto_Point = [typeDesc$Point, witness$TrivialCopy_Point];

// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function copyInto(genEnv, slot, from) {
  genEnv[0][4](slot, from);
  return;
}

function main() {
  var number = { $v: 0 };
  copyInto(genEnv$copyInto_Int, number, { $v: 7 });
  var pair = { x: 3, y: 4 };
  var v12 = { x: 0, y: 0 };
  copyInto(genEnv$copyInto_Point, v12, pair);
  if (pair.x !== 3) {
    return -1;
  } else {
    var v60 = copyCalls + 1 | 0;
    copyCalls = v60;
    if (v60 !== 1) {
      return -2;
    } else {
      return (((number.$v + v12.x | 0) + v12.y | 0) + 10 | 0) + copyCalls | 0;
    }
  }
}

function moveInit$Int(to, from) {
  to.$v = from.$v;
  return;
}

function copyInit$Int(to, from) {
  to.$v = from.$v;
  return;
}

function moveInit$Point(to, from) {
  to.x = from.x;
  to.y = from.y;
  return;
}

function copyInit$Point(to, from) {
  to.x = from.x;
  to.y = from.y;
  return;
}
main();
