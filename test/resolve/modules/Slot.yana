-- The other half - see Cell.yana. Neither writes `@box`, so which of the two holds the pointer
-- would be decided by which file the layout pass reached first, and that is a path sort.

import Cell

pub data Slot {cell: Cell}
