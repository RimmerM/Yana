"use strict";
// Core
function add_eq_Int(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
  return;
}

// ResolveTest
function bump(counter$o, counter$k, counter$s, by) {
  counter$o[counter$k] = counter$o[counter$k] + by | 0;
  return;
}

function swap(a$o, a$k, a$s, b$o, b$k, b$s) {
  var old = a$o[a$k];
  a$o[a$k] = b$o[b$k];
  b$o[b$k] = old;
  return;
}

function moveBy(p, dx, dy) {
  add_eq_Int(p, "x", 1, dx);
  add_eq_Int(p, "y", 1, dy);
  return;
}

function copyThroughBorrow(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
  return;
}

function main() {
  var v28 = { x: 10, y: 20 };
  add_eq_Int(v28, "x", 1, 1);
  add_eq_Int(v28, "y", 1, 2);
  return (v28.x + 33 | 0) + v28.y | 0;
}
