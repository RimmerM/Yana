"use strict";
// Core
function add_eq_Int(target$o, target$k, target$s, amount) {
  target$o[target$k] = target$o[target$k] + amount | 0;
}

// ResolveTest
function main() {
  var v28 = { x: 10, y: 20 };
  add_eq_Int(v28, "x", 1, 1);
  add_eq_Int(v28, "y", 1, 2);
  return (v28.x + 33 | 0) + v28.y | 0;
}
main();
