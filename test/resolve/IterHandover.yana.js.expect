"use strict";
// Core
function teardown$none(value) {
}

// ResolveTest
function consume(h) {
  return h.length;
}

function owns(times) {
  var total = { $v: 0 };
  var v4 = { $v: { $o: total, $k: "$v", $s: 1 } };
  var left = times;
  L0: for (;;) {
    if (left > 0n) {
      if (ResolveTest_continuation$19(v4, { items: new Uint8Array([1, 2, 3, 4]), length: 4 }) !== 1) {
        left = BigInt.asIntN(64, left - 1n);
        continue L0;
      }
    }
    break L0;
  }
  return total.$v;
}

function main() {
  var total = 0;
  var left = 2n;
  L0: for (;;) {
    if (left > 0n) {
      total = total + 4 | 0;
      left = BigInt.asIntN(64, left - 1n);
      continue L0;
    }
    break L0;
  }
  var v4 = total + owns(3n) | 0;
  var count = 0;
  var left$1 = 2n;
  L1: for (;;) {
    if (left$1 > 0n) {
      count = count + 1 | 0;
      left$1 = BigInt.asIntN(64, left$1 - 1n);
      continue L1;
    }
    break L1;
  }
  var v64 = count + 1 | 0;
  count = v64;
  return v4 + v64 | 0;
}

function ResolveTest_continuation$19(env, value$) {
  var v2 = env.$v;
  var v7 = v2.$o[v2.$k] + consume(value$) | 0;
  v2.$o[v2.$k] = v7;
  return 0;
}
main();
