"use strict";
var tally = 0;
var doubled = 0;
var final = 0;

// Text
function newStringOfCapacity(capacity) {
  return "";
}

function showSigned(value, to$o, to$k, to$s) {
  if (value === 0n) {
    var v91 = to$o[to$k] + String.fromCharCode(48);
    to$o[to$k] = v91;
    return;
  } else {
    var remaining = value;
    if (value > 0n) {
      remaining = BigInt.asIntN(64, 0n - remaining);
    }
    var digits = newStringOfCapacity(20);
    L0: for (;;) {
      if (remaining < 0n) {
        digits = digits + String.fromCharCode((Number(BigInt.asIntN(64, 0n - BigInt.asIntN(64, remaining % 10n))) | 0) + 48 | 0);
        remaining = BigInt.asIntN(64, remaining / 10n);
        continue L0;
      }
      break L0;
    }
    if (value < 0n) {
      var v99 = to$o[to$k] + String.fromCharCode(45);
      to$o[to$k] = v99;
    }
    var i = digits.length;
    L1: for (;;) {
      if (i > 0) {
        var v79 = i - 1 | 0;
        i = v79;
        var v102 = digits.charCodeAt(v79);
        var v105 = to$o[to$k] + String.fromCharCode(v102);
        to$o[to$k] = v105;
        continue L1;
      }
      break L1;
    }
    return;
  }
}

function formatBound(bound) {
  var v9;
  if (!(bound === null)) {
    v9 = bound;
  } else {
    v9 = 0;
  }
  return v9;
}

// ResolveTest
function main$() {
  var v0 = tally;
  doubled = v0;
  tally = 42;
  var v7 = v0 + 42 | 0;
  final = v7;
  var v23 = { $v: newStringOfCapacity(((formatBound(11) + 22 | 0) + formatBound(11) | 0) + formatBound(11) | 0) };
  v23.$v = v23.$v + "tally ";
  showSigned(42n, v23, "$v", 1);
  v23.$v = v23.$v + " doubled ";
  showSigned(BigInt.asIntN(64, BigInt(v0)), v23, "$v", 1);
  v23.$v = v23.$v + " final ";
  showSigned(BigInt.asIntN(64, BigInt(v7)), v23, "$v", 1);
  console.log(v23.$v);
  return;
}
main$();
