"use strict";
var a = [0, 0, 0, 0];
var b = [0, 0, 0, 0];

// Core
function checkCondition(failed) {
  if (failed) {
    throw "yana: a runtime check failed";
  }
}

// ResolveTest
function sum(xs$items, xs$length, xs$offset) {
  var v8 = xs$length >>> 0;
  checkCondition(0 >= v8 ? 1 : 0);
  var v13 = xs$items[xs$offset];
  var v18 = xs$offset + 1 | 0;
  checkCondition(1 >= v8 ? 1 : 0);
  var v26 = xs$items[v18];
  return [v13[0] + v26[0] | 0, v13[1] + v26[1] | 0, v13[2] + v26[2] | 0, v13[3] + v26[3] | 0];
}

function masked(xs$items, xs$length, xs$offset) {
  var v8 = xs$length >>> 0;
  checkCondition(0 >= v8 ? 1 : 0);
  var v13 = xs$items[xs$offset];
  var v18 = xs$offset + 1 | 0;
  checkCondition(1 >= v8 ? 1 : 0);
  var v26 = xs$items[v18];
  return [v13[0] & v26[0], v13[1] & v26[1], v13[2] & v26[2], v13[3] & v26[3]];
}

function main$() {
  a = [11, 11, 11, 11];
  b = [31, 31, 31, 31];
  var v15 = [[11, 11, 11, 11], [31, 31, 31, 31]];
  if (v15.length !== 2) {
    v15.length = 2;
  }
  var s = sum(v15, 2, 0);
  var m = masked(v15, 2, 0);
  return Math.imul(s[0], 100) + m[1] | 0;
}
main$();
