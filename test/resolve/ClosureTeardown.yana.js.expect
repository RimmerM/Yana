"use strict";
// Core
function teardown$none(value) {
  return;
}

// ResolveTest
function direct(base) {
  return ResolveTest_lambda$0({ $v: base }, 1);
}

function either(pick, a, b) {
  var f;
  if (pick < 1) {
    f = { $c: ResolveTest_lambda$1, $e: { $v: a } };
  } else {
    f = { $c: ResolveTest_lambda$2, $e: { $v: b } };
  }
  return f.$c(f.$e, 10);
}

function escaping(amount) {
  return { $c: ResolveTest_lambda$3, $e: { $v: amount } };
}

function main() {
  var a = direct(5);
  var b = either(0, 3, 7);
  var c = either(2, 3, 7);
  var f = escaping(100);
  var d = f.$c(f.$e, 1);
  var v21 = f.$e;
  if (f.$c.$h != null) {
    f.$c.$h[0](v21);
  }
  return ((a + b | 0) + c | 0) + d | 0;
}

function ResolveTest_lambda$0(env, x) {
  return x + env.$v | 0;
}

function ResolveTest_lambda$1(env, x) {
  return x + env.$v | 0;
}

function ResolveTest_lambda$2(env, x) {
  return Math.imul(x, env.$v);
}

function ResolveTest_lambda$3(env, x) {
  return x + env.$v | 0;
}
main();
