3:0: error: Loop and Ring import each other, directly or through another module - a cycle has no order for their declarations to be resolved in, so the two would see different halves of one another depending on which was compiled first
